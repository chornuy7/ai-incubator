# ROADMAP — AI-платформа управления Telegram-профилями

> ⚠️ **ПИВОТ В ПРОДАКШЕН (созвон 27.07).** От «инструмента» к **продаваемому SaaS**: биллинг
> (токены→доллары + estimate), перенос всего в **Supabase**, достройка **админки** (юзеры/цены/
> доступы из БД), **API/MCP для «мозгов»**, переделка **лендинга** + вкладка **Learning**. Дедлайн
> деплоя — сегодня; план — 3 недели; чек-ин — среда. План: **[docs/TASKS-2026-07-27.md](./docs/TASKS-2026-07-27.md)**;
> требования: **[TZ §10](./docs/TZ-2026-07-10.md#10-дополнения-от-27072026)**; разбор:
> **[docs/meeting-2026-07-27.md](./docs/meeting-2026-07-27.md)**. Обязательный минимум недели 1 —
> админка · цены · управление аккаунтами · всё в БД.

> ⚠️ **РАЗВОРОТ АРХИТЕКТУРЫ (созвон 22.07).** Иерархия Цель→Кампания→Задача переделана,
> добавлены Агенты, поведение аккаунтов (усталость/распорядок), балансы, лендинг, админка.
> Актуальный план работ: **[docs/TASKS-2026-07-22.md](./docs/TASKS-2026-07-22.md)** (пошагово,
> для второго агента). Архитектура: **[docs/SPEC-2026-07-22-иерархия-балансы.md](./docs/SPEC-2026-07-22-иерархия-балансы.md)**.
> Разделы ниже (по ТЗ 10.07) остаются в силе по модулям, но иерархия целей/кампаний —
> по новой спеке.

> **Единый роадмап.** Часть I — план работ по ТЗ от 10.07.2026 (куда идём, разбито на
> 2 исполнителей и фазы). Часть II — текущий статус (что уже работает и закрыто).
> Источник истины по ТЗ: [docs/TZ-2026-07-10.md](./docs/TZ-2026-07-10.md).
> Файл живой — статусы обновляем прямо здесь. Ссылки `§3.2` — на разделы ТЗ.

## Как пользоваться

- **Один роадмап, две дорожки (lane A / lane B)** — разделение по владению кодом, чтобы два
  человека не редактировали одни файлы одновременно.
- **Claude** читает `docs/TZ-2026-07-10.md` + этот файл перед задачей и обновляет статус пункта
  после выполнения (правило в `CLAUDE.md`).
- **Статусы:** `☐` не начато · `◐` в работе · `☑` готово · `🔒` заблокировано открытым вопросом (§6 ТЗ).
- Каждая задача — ветка `feat/<lane>-<имя>`, PR в `main`.

## Дорожки и владение

| Lane | Кто | Зона | Основные файлы |
|---|---|---|---|
| **A — Ядро** | Разработчик A | Профили, статусы, локи/lease, прогрев, задачи/оркестрация, прокси-модель | `accountLocks.js`, `accountsMeta.js`, `protection.js`, `accountRunner.js`, `registry.js`, `taskStore.js`, `automation/scheduler.js`, `AccountsPage.tsx`, `AccountPicker`, `AccountManagementModal` |
| **B — Кампании/UX** | Разработчик B | Цели/KB/CRM/нейрочатинг, нейрокомментинг, выбор каналов, парсер, логи, Help Center | `neuroCommenting/*`, `workers.js` (comment/chat/parser), `LogsPanel.tsx`, `HelpCenterDrawer.tsx`, `helpDocs.ts`, `ChannelParserModule.tsx`, `ParticipantsParserModule.tsx`, `NeuroCommentingModule`, `NeuroDialogsModule` |

**Правило:** `server/modules/workers.js` — общий. Lane A трогает воркеры оркестрации/прогрева/статистики,
Lane B — comment/chat/parser. Чужие файлы — только через контракт Фазы 0.

---

# Часть I. План по ТЗ (что делаем)

## 🎯 MVP — сдаём ПЕРВЫМ (можно тестить и продавать) `(дедлайн: неделя)`

> Самое первое, что сдаём. Смысл: **поставил цель → поставил задачу → всё в дашборде работает как задачи →
> один модуль реально отрабатывает** на прогретых ботах. Готовый продаваемый срез для демо/теста/продажи.
> Остальной скоуп (RBAC, Мейлинг, резолв, DarkTelegram-паритет) — ПОСЛЕ MVP.

**Что входит в MVP:**
- ☑ **A** Боты запускаются (подключение + вступление). §3.3 _(**проверено вживую 15.07**: реальные аккаунты подключаются по GramJS и вступают в группу/канал по инвайту; статусы/локи/конфликты работают. Прогрев live — по мере надобности)_
- ☑ **A** Движок задач: запуск = задача (ID/статус/прогресс), стоп/рестарт, инициатор, аудит. §3.9 _(запуск=задача с ID/статусом/прогрессом, стоп/рестарт, инициатор, аудит; **пауза/продолжение готовы** — `pauseWorker`+control-флаги в `taskStore`, гонка сохранения покрыта тестом)_
- ☑ **B** Экран **«Цели»**: создать цель (название/действие/этапы/критерий). §3.6 _(бэкенд `/api/goals` + тесты + страница `GoalsPage` в меню; live-прогон CRUD — за тобой)_
- ☑ **B** Дашборд **«Задачи»**: все задачи по всем модулям со статусом/прогрессом/целью + стоп/рестарт (`/panel/tasks`, агрегат `/api/modules/tasks`, авто-обновление 5с). §3.9
- ☑ **B** **1-й модуль сквозь поток** (нейрокомментинг/нейрочаттинг). §3.5/§4 _(выбор цели → `task.goalId` → **генерация к цели с базой знаний** → видно в дашборде. **Live 15.07 — ОБА модуля реально сработали:** нейрочаттинг отправил ИИ-ответ к цели в группу; **нейрокомментинг** вступил в канал + группу обсуждения и **опубликовал ИИ-комментарий под постом** («Да, мы тут! …будем рады обсудить!», status sent). Полный цикл цель→задача→модуль→реальное действие в Telegram)_
- ☑ **B** **2-й модуль = Нейрочаттинг-к-цели** (AI Sales Agent). §3.6 _(тот же поток: селект цели в запуске + `runNeuroChatting` генерит к цели с базой знаний; слои промпта: глоб.ограничения + стиль + цель+KB)_
- ☑ **A/B** Дашборд **видимый** (главный экран = задачи + прогресс). §8.8 _(«Дашборд задач» с воронкой Цели→Задачи→Модули + фильтры + режим «По целям»)_

**DoD MVP:** оператор ставит цель → создаёт задачу → выбирает модуль + прогретые аккаунты → запускает →
видит в дашборде статус/прогресс/результат/логи. Это можно показать клиенту и продать.

**Решено (14.07):** делаем оба, **приоритет — Нейрокомментинг** (быстрее к демо), Нейрочаттинг-к-цели — сразу после.

---

## Фаза 0 — Фундамент и контракты `(совместно)`

> «Архитектура меняется — сделать правильно». Фиксируем интерфейсы до параллельной работы.

- ☐ **A+B** Аудит текущего кода/схемы данных (см. Часть II): что есть, что переписываем → оценки в часах.
- ☑ **A** State machine профиля: `Active/Warming/Pause/FloodWait/Quarantine/SpamBlock/Reauth/Invalid` + переходы и разрешённые действия. §3.3, §4 _(код `server/lib/accountStatus.js` + юнит-тесты + контракт [docs/CONTRACT-account-state-machine.md](./docs/CONTRACT-account-state-machine.md): таблица переходов, временные статусы, авто-возврат)_
- ☑ **A** Контракт локов/lease: `lock(accountId,taskId)`, `lease(channelId,botId,ttl)`, приоритет прогрева. §3.9, §4 _(код `accountLocks.js`/`channelLease.js` + контракт [docs/CONTRACT-locks-lease.md](./docs/CONTRACT-locks-lease.md): API, самолечение, инварианты)_
- ☑ **A+B** Схема сущностей: `Profile/Account/Proxy/Task/Channel/Goal/KnowledgeBase/Lead` + граница «общий слой ↔ Telegram-специфика» (адаптер, §3.10). _(Goal/KB/Lead — [ARCH-goals-crm](./docs/ARCH-goals-crm.md); Account — [state-machine](./docs/CONTRACT-account-state-machine.md); Proxy/Task/Channel + карта связей + **граница адаптера** — [ARCH-entities.md](./docs/ARCH-entities.md))_
- ☑ **A+B** Формат единого лога/аудита: `action, module, profile, initiator, time, reason/code, affectedScope`. §3.1, §5. _(код `server/lib/auditLog.js` + контракт [docs/CONTRACT-audit-log.md](./docs/CONTRACT-audit-log.md): формат записи, словари action/initiator, ротация, инвариант append-only)_
- ☑ **B** Терминология: «Профиль/Аккаунт», «Логи» (не «История»), «Шаблоны поиска». §3.1/3.2/3.8. _(единый экран «Логи», «Шаблоны поиска» в парсере, глоссарий в CLAUDE.md)_
- ☑ **A+B** Документ Goals/KB/Leads ([docs/ARCH-goals-crm.md](./docs/ARCH-goals-crm.md)). §3.6 (P0). _(схемы Goal/KB/Lead, воронка, связка `Task.goalId`+`goalContext`, открытые вопросы §6)_

**DoD:** согласованы схемы и API-контракты — дальше дорожки идут параллельно без пересечения файлов.

## Фаза 1 — P0: Безопасность профилей + контракт целей

**Lane A — профили и статусы**
- ◐ State machine статусов; развести `Pause/FloodWait/Quarantine/SpamBlock` (причина/длительность/действия). §3.3 _(модуль `accountStatus.js` + аудит + `setAccountStatus`; переходы в `accountRunner` (quarantine/spamblock/invalid/reauth) идут через state machine с аудитом; reconciler авто-выхода на старте. `floodwait`-статус с длительностью в хот-пути (пауза→возврат/карантин) + skip floodwait/pause; ручной `pause`/возврат оператором из UI (кнопка + аудируемый роут). Остаётся: ≥5 тест-профилей (нужны реальные аккаунты в разных статусах))_
- ☑ Настраиваемое число попыток до карантина (деф. 3) + длительность от Telegram. §3.3 _(поле «FloodWait до карантина» (деф.3, min 1) в модалке ИИ-безопасности + длительность из `FLOOD_WAIT_N` Telegram в `handleFlood`)_
- ☑ Прогрев полностью блокирует профиль для ручного/модульного назначения. §3.3 _(guard на границе запуска: `canModuleUseAccount`/`assertAccountsAssignable` (409 при назначении); **UI-подсказка есть** — в AccountPicker warming-аккаунт disabled + метка «в прогреве» + toast «Профиль недоступен — дождитесь возврата в активные»)_
- ☑ Авто-стоп при падении trust score → прогрев → авто-возврат. §3.3 _(решение 15.07: полную формулу пока не строим — **жёсткий авто-стоп** по твёрдым сигналам (spamblock/quarantine/3×FloodWait/invalid/reauth — почти есть в `accountRunner`) + safety-дефолты (`server/lib/safetyLimits.js`). **Trust score (15.07):** `server/lib/trustScore.js` — взвешенный балл 0–100 строго по решению §6 (FloodWait 24ч 40% · история spamblock/quarantine 25% · чистые действия 15% · возраст/GGR 20%), `computeTrustScore`/`extractTrustSignals`/`trustRecommendation`, пороги <40/40–70/>70; сигналы берутся из реальной активности+статуса в `buildAccountStats` (`stats.trust`); карточка «Здоровье» → «Trust score (§6)» (гейдж + чип полосы + разбивка по компонентам + рекомендация); юнит-тесты (trustScore.test.js, 7); проверено вживую (Ілья score 73 → «В пул»). **Авто-стоп по trust (15.07):** `server/lib/trustCache.js` — балл кэшируется при расчёте stats; `assertAccountsAssignable` не пускает аккаунты с trust<40 в боевые модули (neuro-commenting/chatting/dialogs/mass-react/mailing) → 409 «отправьте на прогрев», прогрев/парсинг/просмотр не гейтятся (recovery-путь открыт); проверено вживую (seed trust 30 → 409, warming тем же аккаунтом → 200). В списке аккаунтов low/mid-trust помечены чипом (`trustScore`/`trustBand` в `tgListAccounts`). Кэш обновляется при просмотре карточки И **фоновым планировщиком каждые 10 мин** (`refreshAllTrustCache` в `index.js` — без сети, по мете+активности из логов; проверено: при старте «обновлён кэш trust: N акк.», гейт актуален без ручного открытия карточек). **Enforcement суточных лимитов (15.07):** `server/lib/dailyActions.js` — счётчик действий на аккаунт по дням (сброс в полночь), воркеры инкрементят по факту (`incAction`) и **пропускают аккаунт при достижении дневного потолка** (`limitReached` guard — skip+лог). Покрыты все 4 типа §6: comments (нейрокомментинг), dm (нейродиалоги — guard+инкремент, лог 1×/аккаунт т.к. модуль-ответчик простаивает, а не завершается), reactions (масс-реакции), joins (нейрокомментинг/чаттинг/масс-реакции/масс-просмотр — инкремент при `membership.status==='joined'`; парсинг исключён); юнит-тесты (dailyActions.test.js, 4) + **проверено вживую 15.07**: аккаунт с counts.comments=40 → нейрокомментинг пропускает его (commentsSent=0, лог «Суточный лимит комментариев достигнут (§6)»); mass-react с reactions=200 → пропуск. **UI-видимость:** (1) карточка аккаунта → вкладка «Здоровье» → блок «Суточные лимиты (§6)» показывает used/cap по каждому действию с цветным прогрессом и меткой «· лимит» (роут `GET /api/tg/accounts/:id/daily` → `dailySummary`); (2) **в списке аккаунтов** — чип «§6 лимит: …» на строке throttled-аккаунта с тултипом (роут `GET /api/tg/accounts/daily-all` → `dailySummaryAll`, опрос раз в 30с); проверено в браузере (Ілья: чип «комментарии» при comments=40/40). Решения зафиксированы: [docs/DECISIONS-06](./docs/DECISIONS-06-open-questions.md), бизнес-логика в Help Center «Лимиты безопасности»)_
- ☑ Исключение «горячий лид»: статус не меняем, диалог продолжается. §3.3/§4 _(guard `assertNoHotLeadConflict` в `server/leads.js`: аккаунт с лидом в статусе `hot` нельзя забрать в НЕ-диалоговый модуль (mass-*/parsing/warming…) — 409; диалоговые (neuro-chatting/neuro-dialogs) пропускаются. Подключён в `/api/modules/:key/tasks` + кампании; юнит-тесты; проверено API (mass-react→409, neuro-chatting→200))_
- ☑ Безопасный выбор профилей: только свободные/разрешённые; запрет незаметного захвата. §3.2/§5.1 _(бэкенд-guard `assertAccountsAssignable` + **UI**: в `AccountPicker` непрогретые/в карантине/на паузе/занятые не выбираются (disabled + понятная причина), «Добавить все» их пропускает)_
- ☑ Подтверждение переноса профиля + аудит инициатора/действия. §3.2/§4 _(перенос роли/проекта: подтверждение с последствиями (confirm) + запись `account.transfer` в аудит с инициатором)_
- ☑ Двухэтапное подтверждение опасных массовых действий. §3.2 _(bulk «в корзину» и «Отключить» ≥2 — подтверждение с последствиями)_
- ☑ Безопасность API управления: слушать только `127.0.0.1` по умолчанию (`API_HOST`) + аутентификация/роли. §8.1 _(бинд на localhost по умолчанию + **логин по e-mail/паролю** (scrypt) + **RBAC-роли** + **серверный enforcement** `can()` на роутах модулей (`accessGuard.js`, 403). Продакшн-шаг — подписанный токен сессии вместо `X-User-Id` (см. CONTRACT-rbac.md §8))_
- ☐ ≥5 тестовых профилей (блокировки/FloodWait/карантин/лид). §3.3

**Lane B — логи (низкий риск) + контракт целей**
- ☑ «История» → «Логи»; единый экран без дублей. §3.1 _(единый журнал «Логи» `/panel/logs` (аудит-трейл, инициатор+причина, фильтры, «Потоки») + переименованы per-модульные секции: «История»→«Логи» (LiveModule), «История комментариев»→«Логи комментариев», «История просмотров»→«Логи просмотров», «История реакций»→«Логи реакций», страница «История парсинга»→«Логи парсинга»)_
- ☑ Сущности `Goal/KnowledgeBase/Lead` (бэкенд-скелет) + страница «Цели». §3.6 _(Goal + KnowledgeBase + Lead CRUD + страницы «Цели»/«CRM·Лиды»; файлы/OCR KB — §6)_
- ☑ Расширить лог-запись полями из Фазы 0. §3.1 _(`taskStore.appendLog` авто-добавляет `module`+`initiator` (из контекста задачи) в КАЖДУЮ запись лога + опц. `code`/`reason`; тип `LogEntry` расширен. Единый аудит-лог уже несёт полный формат ([CONTRACT-audit-log.md](./docs/CONTRACT-audit-log.md)))_

**DoD:** §5.1–5.2 (нельзя назначить непрогретый/забрать чужой; всё в логах с инициатором); есть контракт целей.

## Фаза 2 — P1: UX, каналы и задачи

**Lane A — движок задач**
- ☑ Сущность `Task` на каждый запуск (ID/модуль/цель/профили/каналы/время/статус/прогресс). §3.9 _(есть id/модуль/статус/прогресс/настройки, `initiator` + аудит `task.start`/`task.stop`. **Сверка 21.07:** `goalId` больше не «когда появятся» — проставляется на запуске (`modules/routes.js`) и сохраняется в DTO (`taskStore.js`), рядом добавлен `campaignId` (§0 Цель→Кампания→Задача). Каналы задачи живут в `settings.targets`/`channels` — отдельного `channelIds` не заводили осознанно, чтобы не плодить второй источник правды)_
- ☑ Трекер задач: просмотр/пауза/продолжение/отмена/перезапуск/ошибки/результат. §3.9 _(отмена=stop, перезапуск=`/restart`, **пауза `/pause` + продолжение `/resume`** (статус `paused`, прогресс сохранён, resume перезахватывает локи и продолжает с места), просмотр/ошибки/результат — в дашборде+логах; кнопки в «Дашборде задач». Попутно устранена гонка: `saveTask` больше не затирает выставленные извне флаги stop/pause (надёжнее и стоп). **Проверено вживую 15.07:** прогрев → пауза (done 2, status paused) → resume (продолжил, done 3))_
- ☑ Запрет конфликтующих задач на профиль; приоритет прогрева. §3.9/§4 _(локи + статус-гейт + распределение пула в кампании — не дают двум модулям взять один аккаунт)_
- ☑ Планировщик/оркестрация: перераспределение профилей между модулями под одну цель. §3.9 _(**проверено вживую 15.07:** 1 цель → нейрокомментинг (канал) + нейрочаттинг (группа) на 4 аккаунтах round-robin (2/2, без конфликтов) → ОБА модуля опубликовали реальные ИИ-действия к цели. `buildCampaignPlan` прокидывает лимиты/задержки в модули (+override). «Кампания»: `/api/campaigns/launch` — одна цель → N модулей на общем пуле (round-robin, непересекающиеся подмножества) + **расписание вкл/выкл**: `campaignSchedules.js` (store + тик каждую минуту + isDue one-time/daily) + роуты `/api/campaigns/schedules` + UI на странице «Кампания» (дата/время + повтор + список с тумблером вкл/выкл и удалением); инициатор запусков по расписанию — `scheduler`; юнит-тесты)_
- ☑ Схема каналов + lease «один канал — один бот». _(примитив `server/lib/channelLease.js` + юнит-тесты, по контракту locks-lease §4. **Сверка 21.07:** lease не «примитив в вакууме» — реально применяется в `channelStats.js` (`acquireChannelLease(channel.id, acc, 'stats', 60_000)` перед проходом и `releaseChannelLease` после), схема Channel и оркестратор статистики закрыты в Фазе 3)_

**Lane B — экраны и каналы**
- ☑ Help Center: правая панель + якоря по «?». §3.1 _(«?» открывают нужную статью (`setHelpTopic`+`findHelpDoc`) + панель **не блокирующая** — основной интерфейс доступен)_
- ☑ Логи: список + фильтр (действие/инициатор) + **детальная вью записи** (клик → модалка) + **режим «Потоки»** — multi-select до 3 действий, параллельные колонки для сравнения. §3.1
- ☑ Нейрокомментинг UI: явные режимы, min/avg/max время, связка лимитов, окно постов, распределение типов = 100%. §3.5 _(расчётное **min–max время** + **окно постов** (3/5/10/20) + **распределение типов %** (веса на тип, нормируются при запуске, выбор типа по весу в воркере); **связка лимитов** (предупреждение о противоречиях макс/аккаунты/мин-на-акк); **явные режимы (15.07):** 3 подписанных режима «Режим комментирования» — Случайный (один пост из окна) / По ключевым словам (фильтр по keywords) / Все посты (комментируем всех кандидатов), каждый обрабатывается в `pickCommentCandidates`+воркере; описаны в Help Center «Нейрокомментинг»)_
- ☑ Выбор каналов: объединённый список, карточка, фильтры, multi-select, общий компонент. §3.7 _(база каналов `/panel/channels` + в «Кампании» цели из **3 источников**: ручной ввод + база каналов (multi-select) + **папки** через общий `FolderPicker` (загрузка/сохранение/управление/валидация) — используется во всех секциях с целями)_
- ☑ Парсер: «Шаблоны поиска», AND-пересечение, дедуп в общей базе, «Открыть в Telegram», прозрачная формула рейтинга. §3.8 _(дедуп в базу + «Открыть в Telegram» + прозрачная формула рейтинга + **AND-пересечение** (тумблер «Пересечение (AND)» при 2+ ключах: канал должен совпасть со ВСЕМИ ключевыми словами; трекинг совпадений в воркере + фильтр по пересечению). **Проверено вживую 15.07:** «crypto»+«news» → union 15 → пересечение 1 (@cryptowallet_news_cis))_

**DoD:** §5.4–5.6 (каналы из папок выбираются списком; парсер без дублей и с AND; запуск = задача со статусом).

## Фаза 3 — P2: Статистика каналов + инфраструктура

**Lane A**
- ☑ Модуль статистики каналов: выбор аккаунтов/каналов/задержек, пары «канал–бот», 2 прохода, lease, «обновить сейчас», дата/источник. §3.9 _(база каналов `/api/channels` с дедупом + страница «Каналы» + «Обновить сейчас» по lease + дата/источник; период (день / час-если-бот-в-группе) + авто-расписание; интеграция парсер→база (`upsertMany`); **2 прохода** (`refreshOneChannel`: проход 1 — подписчики, проход 2 — активность/свежесть: `activityFromDates` → метка `high/medium/low/stale` + `lastPostAt`, показываются на карточке канала; 2-й проход некритичен). Юнит-тесты)_
- ◐ Прокси/SIM-модель `аккаунт→страна→SIM→proxy→device/IMEI→статус`; прокси в Менеджере профилей. §3.2/3.4 _(разблокировано 14.07: своя ферма + докупаемые. **Готово:** сущность `Proxy` (`server/proxies.js`: тип static/mobile/farm, scheme, host/port/auth, страна, статус) + CRUD `/api/proxies` + аудит + страница `/panel/proxies` (CRUD + **назначение на аккаунты** через `account.proxy`=URL) + юнит-тесты. **Авто-проверка живости (15.07, §6):** `checkProxyLiveness`/`checkAllProxies` (TCP-пинг host:port) + статус ok/dead + lastCheckAt; планировщик в `index.js` (старт + каждые 30 мин) + роуты `POST /api/proxies/check-all`, `POST /:id/check`; правило «1 прокси = 1 аккаунт» — `sharedProxies`/`GET /api/proxies/shared` (нарушения 1:1); юнит-тесты (usage-map + tcpPing); Help Center «Прокси: правила». **Далее:** цепочка SIM/device/IMEI (глубже инфра))_
- ☐ 🔒 Отчёт static/mobile/своя ферма + пилот (§6).

**Lane B**
- ☑ CRM: лид/статус/ответственный/история/результат; приоритет ответившему; лимиты по активным диалогам. §3.6 _(`/api/leads` + воронка cold→answered→hot→target→closed + страница CRM; **приоритет ответившему** (`sortLeadsByPriority`: hot>answered>target>cold>closed — лиды сортируются в CRM); **лимит активных диалогов** (`assertActiveDialogLimit` + `activeLeadCount`: при `maxActiveDialogs`>0 нельзя грузить профиль в диалоговый модуль сверх лимита активных лидов — 409); юнит-тесты. Проверено API: лимит 2 при 2 диалогах → 409, лимит 5 → 200)_
- ☑ Нейрочатинг к цели из списка; разделить ограничения/KB/цель/стиль. §3.6 _(цель из списка + цель+KB в генерации + слои промпта (глоб.ограничения+стиль+цель+KB); **лимиты активных диалогов** (`assertActiveDialogLimit`/`activeLeadCount`); **приоритет ответившему (15.07)** — воркер сортирует диалоги захода по CRM-приоритету лида (`sortDialogsByLeadPriority`: hot>answered>target>cold, матч по username/id), лимит ответов тратится на важных первыми; юнит-тесты)_
- ☑ Аналитика: отправлено/ответы/активные диалоги/горячие лиды/конверсия. §3.6 _(страница `/panel/analytics`: метрики + воронка лидов + разбивка по целям; из данных задач/лидов)_
- ☑ Тональность/семантика в нейрокомментинге (фильтр/vector/оба). §3.5 _(фильтр стоп-слов (нежелательные темы/тон) + **семантический фильтр к цели (15.07)**: `server/lib/semantic.js` — embedding цели+поста (OpenAI text-embedding-3-small) + косинусная близость, коммент только если ≥ порога (деф.0.2); опция `semanticFilter` (тумблер в UI нейрокомментинга); чистые cosine/rank — юнит-тесты; **проверено вживую**: релевантный пост sim 0.52, офтоп 0.09 → порог 0.2 режет офтоп. Help Center «Нейрокомментинг»)_

**DoD:** §5.7–5.8 (кампания к цели и KB, лид не теряется; статистика — единственный исполнитель на канал + дата актуальности).

## Фаза 4 — Later: Мультиплатформа

- ☐ **A+B** Переключатель Telegram/Instagram в архитектуре; Telegram-специфика через адаптер. §3.10
- ☐ **B** 🔒 Исследование ComfyUI (место в пайплайне). §3.10

## Новый скоуп 14.07.2026 (созвон) — требует приоритизации `(§8 ТЗ)`

> Из [docs/TZ §8](./docs/TZ-2026-07-10.md). Пока НЕ распределено по фазам — ждёт ответов участников
> по ❓. Дедлайн от заказчика: **следующая неделя** — базовый цикл (см. приоритет ниже).

**🎯 Приоритет (дедлайн неделя):**
- ☑ **A** Сквозной цикл: **запустить ботов + 1 «задача для модуля»** (постановка цели→задача→выполнение). §8.9 _(**проверено вживую 15.07** на реальных аккаунтах: цель «ТЕСТ» → задача нейрочаттинга → аккаунты подключились (GramJS) + вступили в группу по инвайту + сгенерировали ответ **к цели с базой знаний** и **реально отправили** его в группу (Дмитрій Руб, статус `sent`, 0 floodwait) → видно в дашборде/логах. По ходу найден и починен баг «тихого нуля» (`resolveTotalTarget`: при min=0 seed мог дать цель 0 → задача молча простаивала))_

**Lane A — ядро/инфра:**
- ☑ RBAC: 1 админ→N юзеров, роль «Модератор», доступ по модулям/парсерам/блокам, права на папку/таймер/шаблон/канал (2 чекбокса), выделение снятого доступа. §8.1 _(матрица прав определена заказчиком 14.07 — снят 🔒. Страница ролей `/panel/roles` (матрица модули×блоки + ресурсы, 2 чекбокса allow/deny + подсветка снятого доступа, шаблон «Модератор»); сущность **«Пользователь»** (`server/users.js`, scrypt-пароль, логин, назначение роли на `/panel/users`); **enforcement** `can()` в UI — сайдбар/модули под роль + гейт прямого URL; сид тестового юзера; аудит `role.*`/`user.*`; юнит-тесты; [docs/CONTRACT-rbac.md](./docs/CONTRACT-rbac.md). Проверено в браузере: модератор видит 4 модуля, парсеры/админ-страницы скрыты, прямой заход → «Нет доступа»)_
- ☑ Карточка сотрудника + учёт рабочего времени. §8.1 _(карточка на `/panel/users`: имя/e-mail/роль/активность + **учёт рабочего времени** (`server/workLog.js`: clock-in при входе / clock-out при выходе, «в сети» + сегодня/7 дней, `GET /api/users/worktime`); юнит-тесты. Серверный **enforcement** `can()` на роутах модулей (`accessGuard.js`, 403 при отказе) — проверено)_
- ☑ Прогрев: 3 уровня — **Быстрый** (2 дня) / **Нормальный** (3–7) / **Стандартный** (7–14). §8.2 _(названия + пропорции по заказчику; `warmingPace(level)`: темп (mul) + действий/день (40/20/10) + **веса действий** (просмотр 40% · реакции 20% · чтение/ЛС 20% · вступления 10% · подписки/ping 10%), выбор действия — `pickWeightedKey` (взвешенно), `inActiveWindow` (дневное окно 9–23); юнит-тесты; бизнес-логика в Help Center «Политика прогрева». **Реальные реакции/вступления (15.07):** `runWarming` при kind==='react'/'join' выполняет настоящие действия (`sendReaction` по свежему посту публичного канала / `joinTargetOrSkip` в публичный канал) под суточными лимитами §6 (при достижении потолка — деградация в просмотр); расход виден в карточке «Здоровье». Multi-day ramp-up (activity растёт по дням) + окна активности 9–23 — есть в `warmingPace`/`inActiveWindow`; полноценный multi-day scheduler (пауза на ночь как отдельные запуски) — при live)_
- ☐ Резолв номеров → Telegram (модуль + сервер парсинга). §8.5 🔒(инфра/ToS) ❓
- ☑ Регионы Европа+Украина в GEO-модели. §8.3 _(единая GEO-модель `src/shared/config/geo.ts`: 14 стран с регионами Европа/СНГ + фильтр по региону (`reg:europe`/`reg:cis`) и стране в менеджере и пикере аккаунтов; `countryFromPhone` расширен европейскими префиксами (gb/fr/es/it/nl/cz/ro/lt/lv); юнит-тест мэппинга. Проверено в браузере)_

**Lane B — модули/UX (паритет DarkTelegram):**
- ☑ **Мейлинг / AI Mailing** (рассылка в Telegram по номерам). §8.4 _(стек решён по видео заказчика — **Node-модуль в этом проекте** (GramJS), Yii2/PHP не трогаем. **Каркас готов:** модуль `mailing` в движке (MODULE_DEFS+WORKERS+MODULE_LABELS), страница `/panel/mailing` (аккаунты + номера + текст + цель + лимиты/задержки, предупреждение о риске), задача создаётся и видна в дашборде. **Реальная отправка готова (15.07, по явному «да» заказчика):** `runMailing` резолвит каждый номер через `contacts.ImportContacts` → `sendMessage` ЛС (шаблон или ИИ к цели), НЕ «слепая» — предохранители §6: только аккаунты с trust>70 (gate через `buildAccountStats`), суточный лимит ЛС (`limitReached('dm')`/`incAction('dm')`, дефолт 25/акк), паузы 90–300с, round-robin, номер не в Telegram → skip, FloodWait→карантин (`handleFlood`). Страница: badge «live», паузы 90–300с по умолчанию, чекбокс ИИ-текста, предупреждение о реальной отправке+риске. Опция отписки в тексте — на операторе)_
- ☐ Story Mentions (упоминания в сторис) — доработка масслукинга. §8.4
- ☑ Прохождение капчи групп + 3 варианта антиспама + авто-заход. §8.6 _(типы капч уточнены заказчиком. **Готово:** классификатор `server/lib/captcha.js` — распознаёт капчи групп-ботов (кнопка/математика/эмодзи/картинка/кастом/собственная-Telegram) и решает стратегию (**3 варианта:** `auto` простые / `operator` сложные / `skip`); solveMath + matchButton; юнит-тесты (7). **Граница ToS:** собственные проверки Telegram (SMS/reCAPTCHA) → всегда `operator`, не авто. **Live-хук готов (15.07):** `server/lib/captchaRunner.js` — после каждого нового вступления (`joinTargetOrSkip`, status==='joined') читает последние сообщения группы, детектит капчу (`looksLikeCaptcha` + бот из `CAPTCHA_BOTS`), строит план через классификатор и действует: `auto` — клик инлайн-кнопки (`message.click`) или ответ текстом; `operator` — лог warning (аккаунт ждёт ручного действия); `skip` — выход из группы (LeaveChannel). Чистые detect/plan — юнит-тесты (captchaRunner.test.js, 6); GramJS-вызовы в try/catch (не ломают вступление). Бизнес-логика — Help Center «Капча и антиспам». Реальный прогон против живого бота-капчи — при наличии такой группы)_
- ☑ Web-Telegram по аккаунтам. §8.7 _(модуль **«Обзор аккаунта»** `/panel/inbox` (есть в сайдбаре): вкладки «Диалоги» (смотреть/отвечать) + «Группы и каналы» (в каких состоит аккаунт) — реюз inbox-бэкенда `neuro-dialogs` + `listAccountChannels`. **Сверка 21.07 нашла расхождение и оно закрыто:** страница держала СВОЙ UI диалогов и отстала от `NeuroDialogsModule` — рендерила только `m.text` (превью картинок/видео бэкенд уже отдавал), поле ответа было однострочным `<input>` без панели форматирования. Разметка вынесена в общие `ConversationBubble` + `ReplyBox` (`src/features/conversation/`) и подключена на обоих экранах; копия удалена. Теперь правка едет сразу в оба)_
- ☑ Единый видимый дашборд «Цели→Задачи→Модули» + фильтры-воронка + преследование цели. §8.8 _(`/panel/tasks` → «Дашборд задач»: воронка-сводка (цели в работе / задачи / активные+модули / средний прогресс) + фильтры по цели/модулю/статусу + режим **«По целям»** (группировка задач по цели с агрегатным прогрессом к цели и чипами модулей). Проверено)_
- ◐ Ресёрч: тест DarkTelegram + YouTube (DarkTelegram, TG Master). §8.10 _(звірка паритету — [docs/RESEARCH-parity.md](./docs/RESEARCH-parity.md): мы сильнее по ИИ-слою; главная безопасная прогалина — **автопостинг** — **закрыта** (модуль `autoposting`: постинг в свои каналы, worker+страница `/panel/autoposting`+Help Center; проверено вживую — корректно требует прав админа: CHAT_ADMIN_REQUIRED). Прогалины с бан-риском (инвайтер/тегер) — по явному «да»; авторегер/клоннер — низкий приоритет. Точный список DarkSoft из роликов ждёт текстом)_

**❓ Не расшифровано / ждёт участников:** семантика 2 чекбоксов · 3 варианта капчи · лимиты Мейлинга · два ролика (нет у Claude).

## Новый скоуп 27.07.2026 (созвон) — ПРОДАКШЕН/КОММЕРЦИАЛИЗАЦИЯ `(§10 ТЗ)`

> Полный план — [docs/TASKS-2026-07-27.md](./docs/TASKS-2026-07-27.md). Требования — [TZ §10](./docs/TZ-2026-07-10.md#10-дополнения-от-27072026).
> Дедлайн деплоя — **сегодня**. Недели 2–3 — фокус модуль-за-модулем + мониторинг. Чек-ин — **среда**.

**🎯 Неделя 1 — обязательно 100% (§10.10):**
- ◐ **Supabase**: ☑ схема (14 таблиц) + миграция `server/data/`→Supabase + адаптер сторов
  (DATA_BACKEND=supabase, всё из БД); ☑ регистрация + подписанные сессии (SESSION_SECRET);
  🔴 OTP+капча (нужны SMTP-провайдер + ключи капчи), Storage сессий (нужен прогон на боевых). §10.2
- ☑ **Биллинг/токены**: «Потрачено» = действия+токены (было занижено) · estimate (стоимость+время) ·
  **себестоимость токена считается сама из модели** (modelPricing) + правится в админке · баланс
  (кнопки не блокируются→модалка · несъёмная лента · модалка на логине + чекбокс админам · при 0 «закончился»). §10.1
- ☑ **Админка**: карточка юзера (модули/токены/деньги в $/подписки/роль) · вкл/выкл модуль на юзера ·
  блок-внутри-модуля через роли · **2 цены + скидка −X% из админки** · **роли только из sudo-админки**
  (вкладка «Роли», назначение роли юзеру) · **вложенные юзеры** · **редактор наборов** · **мониторинг**
  (нагрузка + аккаунты активные/падают + почему · **RPS + CPU/память сервера, опрос 3с**, §11 30.07). §10.4/§10.9
- ◐ **Управление аккаунтами** из админки — мониторинг здоровья + **корзина (мягкое удаление/восстановление/очистка, 30.07)**;
  импорт ведёт в Менеджер профилей; полностью свести менеджер сюда — TODO. §10.10
- 🔴 **Деплой** + авто-деплой по пушу: CI (tsc+тесты+build) готов, deploy.yml есть; нужен хостинг + SESSION_SECRET.

**🔌 API/MCP для «мозгов» (§10.3):**
- ☑ API по закрытому ключу (Bearer): создать цель/кампанию/задачу — /api/v1.
- ☑ API/MCP «что умеет модуль» (capabilities + mcp) + POST /modules/:key/run + /estimate.

**Lane B — UX/лендинг/обучение:**
- ☑ **Анализ картинок**: vision-описание фото в нейродиалогах и нейрокомментинге + чекбокс в модуле +
  наценка «картинка ×N» из админки (tokenLedger.coinMultiplier). §10.5
- ☑ **Лендинг**: герой-карусель (выбор статичная/карусель `?hero=`) · 2 версии `?v=` (SEO) · воронка
  «на выгоду клиента» / «автоматический конвейер» · «Подробнее»→страница модуля (обогащена HELP_DOCS) ·
  калькулятор наборов · «комбайн» убран · отзывы-карусель + кейсы-переработка. Позиц. «одно слово» — 🔴 бренд. §10.6
- ◐ **Learning**: «Обучение» — вкладки (Обзор/Как работает/Пример/Риски/Советы), собирается из HELP_DOCS;
  витрина модуля с нумерованными выносками добавлена под заголовком (30.07); реальные скрины — TODO. §10.7

**Прочее:** ☑ имя **Murmex** во всём UI (домены — за бизнесом) · 🔴 SIM-безопасность (бизнес). §10.8/§10.9

**🔒 Ждёт бизнеса:** модель под капотом (цена авто-адаптируется под OPENAI_MODEL) · OTP-SMTP+капча-ключи ·
платёжка (Андрей) · хостинг+SESSION_SECRET для деплоя · «одно слово» позиционирования · SIM.

## Новый скоуп 29.07.2026 (созвон) — НАБЛЮДАЕМОСТЬ + ПРАВИЛЬНАЯ БД `(§11 ТЗ)`

> Полный план — [docs/TASKS-2026-07-29.md](./docs/TASKS-2026-07-29.md). Разбор — [meeting-2026-07-29.md](./docs/meeting-2026-07-29.md).
> Требования — [TZ §11](./docs/TZ-2026-07-10.md#11-дополнения-от-29072026).
> ⚠️ **Критический путь:** схема БД (§11.3) блокирует логи по юзеру (§11.1) — нельзя показать
> действия юзера, пока данные к юзеру не привязаны. Поэтому БД делается первой.

**Приоритет 1 — БД (§11.3):**
- ◐ `foreign key` на user ID (goals/campaigns/leads/parsed_channels/accounts_meta) — миграция применена 30.07,
  код пишет владельца из подписанной сессии — проверено на боевой базе
- ◐ Разделить `auth.users` (встроенная) и **`profiles`** — таблица `profiles` + триггер `on_auth_user_created` готовы;
  **вход двухрежимный (30.07):** сперва Supabase Auth (`verifyAuthPassword`), затем legacy scrypt — маппинг по `profiles.legacy_id`/e-mail;
  осталось завести людей в Auth и задать пароли (действие человека), затем выключить legacy-ветку. свою `users` пока держим (FK 13 таблиц)
- ◐ Типы юзеров отдельной таблицей (`user_types`, UNIQUE + CHECK длины/паттерна) + `users.user_type_id` — наполнены из ролей (7 типов)
- ◐ Права — join-таблицей `user_type_modules` (`user_type_id` ↔ `module_id`) — 44 связи, пересобираются автоматически при правке роли;
  гейт `can()` осознанно оставлен на roles.permissions (наша модель прав богаче: блоки/разделы/ресурсы); сущности остаются «чистыми»
- ☐ Зарегистрировать реальных юзеров в Supabase Auth (раздел пуст) + миграция текущих данных

**Приоритет 2 — наблюдаемость и безопасность (§11.1, §11.9):**
- ◐ **Логи любого юзера в админке**: ☑ журнал активности в карточке (входы, задачи, баланс, подписки, роли, ключи;
  фильтр по типу события, IP в строке; `/api/admin/user-activity`) · ☑ «с кем переписывается» + САМА переписка (решение 30.07: храним целиком; таблица messages) ·
  ☐ пробел атрибуции: 259 из 468 записей аудита пишутся обезличенно (`initiator=operator`)
- ☑ **Блокировка юзера** — «Отключить» закрывает вход и сессию (active=false) · ☑ **логирование IP** входа + блок «Последний вход» в карточке

**Приоритет 3 — деньги и UX (§11.4, §11.5, §11.2):**
- ☑ Баланс: **два независимых остатка** — деньги (`usd`) и токены (`coins`); покупка токенов за $ по курсу пакетов
- ☑ Низкий баланс — **красным**: чип в шапке красный + слово «Пополнить», кнопки «Пополнить» в модалках красные
- ☑ Периоды подписки **генерируемым списком + селект** (недели 1–4 / месяцы 1–6 / годы 1–5); скидка за **6 мес** —
  добавляется из админки без правки кода, витрина и калькулятор строятся из них. миграция применена 30.07,
  проверено на боевой базе: «6 месяцев −10%» → $649
- ☑ Цена подписки за модуль — правится **и в админке, и в кабинете** (админу — инлайн в строке модуля)

**Приоритет 4 — лендинг (§11.6, §11.7):**
- ◐ **Картинка/скриншот у каждого модуля** — блок «Как это выглядит внутри» с нумерованными выносками готов
  (на лендинге, странице модуля и на «Обучении», 30.07); ждём реальные скриншоты (кладутся в `MODULE_SHOTS`, вёрстка не меняется)
- ☑ Залогиненному — **аватар + имя + «Кабинет»** вместо «Войти» (общий `landing/AuthCta.tsx`)
- ☑ Клик по логотипу → лендинг (принято на созвоне)

**Приоритет 5 — API «мозгов» (§11.8, Илья):**
- ☐ Убрать генерацию ключа под пользователя; подключение **server→server** по TLS-сертификату/ключу
- ☐ Самостоятельный тест API Python-скриптами (проверять структуру ответа, а не факт ответа)
- ⏸ Чтение всех логов через API — пока не нужно

**⏸ Отложено:** агенты (характер/название), шаблоны и блок скидок-наборов на лендинге.

**🔒 Ждёт внешнего:** платёжка **VIVA vs Stripe** (Андрей) · ключи мерчанта · глубина хранения
логов переписки (приватность/объём) · живой прогон прогрева на 50 закупленных аккаунтах (Илья).

## Новый скоуп 06.08.2026 (созвон) — СТАБИЛИЗАЦИЯ ПОД ПЕРВОГО ПОЛЬЗОВАТЕЛЯ `(ТЗ 06.08)`

> Полное ТЗ, roadmap и backlog — [docs/TZ-2026-08-06.md](./docs/TZ-2026-08-06.md).
> ⚠️ **Новое ТЗ имеет приоритет над старыми задачами MR-1…MR-79.** Пересечения помечены
> комментариями «⬆️ см. MR-XX» в старых тикетах; работаем по новым.
> Jira: **10 эпиков MR-80…MR-89**, **38 задач MR-90…MR-127** (label `tz0608`).
> Слой-лейблы — `UI`/`UX`/`Backend`; приоритет — системное поле **Priority** (не лейбл).
> Workflow: `To Do → In Progress → Human Testing → Done` (AI Testing остаётся внутренним шагом перепроверки).

**Этап 0 — Jira и QA-процесс** (EPIC-JR / MR-80):
- ☐ JR-001 workflow Human Testing (возврат — с комментарием причины; финальный скриншот у UI)
- ☑ JR-002 очистка labels — prio-* сняты, область кириллицей + слой UI/UX/Backend
- ☐ JR-003 правило финального скриншота в Done

**Этап 1 — Блокирующие исправления** (EPIC-AUTH/MR-81, EPIC-AM/MR-82, EPIC-PROMPT/MR-84):
- ☐ AUTH-001 полная валидация регистрации · ☐ AUTH-002 защита Supabase API (IP/flood)
- ◐ AM-001 статусы/даты проверок — даты (MR-63) и персист Spam Block (MR-78) сделаны;
  остаётся дата Proxy-проверки + действие «Обновить статус»
- ☐ AM-002 исключить нерабочий Proxy из запусков · ☐ AM-003 панель «Показать нерабочие (N)» + поиск · ☐ NOTIFY-001 колокольчик
- ☐ PROMPT-001 ложная ошибка сохранения + полный активный промпт

**Этап 2 — Общие UI-компоненты** (EPIC-CORE/MR-83, EPIC-TPL/MR-85):
- ☐ UI-001 нижняя панель «Параметры и лимиты» (см. MR-56) · UI-002 структура модулей (см. MR-54) ·
  UI-003 выбор аккаунтов (см. MR-43) · UI-004 группы/Blacklist (см. MR-57/58) · UI-005 тайминги+Advanced (см. MR-54) ·
  UI-006 терминология · HELP-001 подсказки/Help Center (см. MR-46)
- ◐ TPL-001 сохранение/применение шаблонов _(18.08: выбор шаблона — полосой ВВЕРХУ модуля, до всех настроек (появляется, когда сохранён хотя бы один), плюс меню на кнопке «Шаблон» в нижнем баре — там же пустое состояние «У вас нет шаблонов» → «Создать шаблон». Раньше выбора не было нигде: кнопка умела только сохранять. Сохранение во всех шести модулях — через одну модалку: имя + цвет-метка + владелец из числа подключённых пользователей (раньше только у нейрокомментинга, у остальных — ввод имени). Список шаблонов у каждого модуля свой. `src/features/modules/shared/PresetBar.tsx`)_ · ☐ TPL-002 менеджер шаблонов

**Этап 3 — Dashboard задач** (EPIC-TASKS/MR-86): ☐ TASK-001 (автор, время, ETA, статус, Play/Pause/Stop)

**Этап 4 — Исправления модулей** (EPIC-MODULES/MR-87), после общих компонентов:
- ☐ NC-001 нейрокомментинг (см. MR-60/61) · REACT-001 реакции · WARM-001 прогрев · ND-001 нейродиалоги ·
  MAIL-001 mailing · POST-001 autoposting · LOOK-001 mass looking · RATING-001 скрыть+баланс (см. MR-49).
  Каждый модуль — отдельно Human Testing.

**Этап 5 — Лендинг и обучение** (EPIC-LEARN/MR-88): ☐ LAND-001 галерея блоков (см. MR-48) · LEARN-001 доки после UI (см. MR-47/48)

**Этап 6 — Полное логирование** (EPIC-LOG/MR-89): ☑ LOG-001 модель данных (спроектирована, `docs/CONTRACT-action-log.md`) · ☐ LOG-002 запись действий ·
LOG-003 история в Account Manager · LOG-004 сохранённые посты для реакций

**⏸ Отложено (Low):** REACT-002 emoji · LOG-005 аналитика контента · PARSE-001 дальнейший парсинг (см. MR-39) · RATING-002 пакетное ценообразование.

**🔒 Ждёт внешнего:** платёжка VIVA/Stripe · нагрузочный тест + мощный сервер · приёмка фокус-группой (3–4 чел.).

**Требует уточнения (§24 ТЗ):** список символов полного имени · алгоритм перераспределения % · механика «Целевое действие/Фиксировано» · нужен ли Blacklist во всех модулях · финальные слой-labels.

## Новый скоуп 10.08.2026 (созвон-ревью) — МЕНЕДЖЕР АККАУНТОВ + ДАШБОРД ЗАДАЧ `(ТЗ 10.08)`

> Полное ТЗ — [docs/TZ-2026-08-10.md](./docs/TZ-2026-08-10.md). Jira: **MR-129…MR-138** (label `tz1008`).
> По модулям — «более-менее нормально». **Приоритет −1 (важнее всего):** Менеджер аккаунтов и Дашборд задач.

- 🔴 **MR-129 (P−1)** — Менеджер аккаунтов: починка (прокси/переходы/сохранение позиции/проверка/реавторизация) + переработка карточки (табы по значимости, статус-таб убрать, даты/действия в профиль, здоровье+работа в шапку, папки убрать, full-width).
- 🔴 **MR-130 (P−1)** — Дашборд задач: синхронное управление (привязка к секвенсу, единый список, статусы очередь/ожидание/работа/ошибка, loader на Play/Pause + блокировка кнопок).
- 🟠 **MR-131** — Статус аккаунта = «зона риска» с конкретикой (срок+цена=блокировка); разделить «нерабочий прокси» vs «нерабочий статус»; авто-проверка здоровья+прокси.
- 🟠 **MR-132** — Пикер: только аккаунты с прокси + шестерёнка-переход в Менеджер→Прокси (расширение MR-96).
- 🟠 **MR-133** — Страница «Прокси»: объединить блок прокси+гео-рекомендации, full-width, починить загрузку, пометка dead.
- 🟡 **MR-134** — Уведомления: расширить (статус задачи/аккаунтов, пропущенные ЛС, галочка в модуле, подписка).
- 🟡 **MR-135** — Диалоги: привязка к задачам + плашка «Бот/Человек» + видимость групп.
- 🟢 **MR-136** — Правки модулей: тайминги-Custom (→ MR-103), режим/какие посты (→ MR-110), «Обязательно» у аккаунтов, цена инлайн, лимит-иконка, нижняя панель, мейлинг-порядок.
- 🟢 **MR-137** — Навигация: Поддержка вниз в «Аккаунт», скрыть Автоматизацию, обзор без пред-выбора, порядок меню.
- 🔵 **MR-138** — Процесс: fetch перед push + тесты; meeting-review в Backlog; проверить сортировку/логи.

**Возврат на доработку (10.08):** MR-103 (тайминги-Custom) и MR-110 (режим/какие посты) — вернули из Done/Human Testing в In Progress.

## Новый скоуп 12.08.2026 (созвон-ревью) — ДАШБОРД/ЭКОНОМИКА/АВТОРИЗАЦИЯ + ЦЕЛИ/MCP `(ТЗ 12.08)`

> Полное ТЗ — [docs/TZ-2026-08-12.md](./docs/TZ-2026-08-12.md). Jira: **MR-140…MR-160** (label `tz1208`).
> Meeting-плейсхолдер — MR-140. Следующий созвон — пятница 15:00. 94 пункта → 19 задач + эпик Цели/MCP.

**Авторизация/безопасность:** 🟠 MR-141 тайм-аут сессии + локальная проверка cookies · 🟠 MR-142 баги (старые данные после логина, шаринг авторизаций, автозаполнение реги) · MR-143 валидация имени + баг баланса при реге.

**Дашборд задач:** 🟠 MR-144 лимит параллельных задач в панели владельца · 🔴 **MR-145 (Highest)** задача с ошибкой = статус + карточка двойной высоты · 🟠 MR-146 warning-система (валидация аккаунтов задачи) · 🟠 MR-147 карточка = вкладки Описание/Настройки + чистка + сортировка · MR-148 перезапуск/редактирование + баги (результаты=0, прогрев за 3ч).

**Экономика:** 🟠 MR-149 оплата действия по максимуму + калькулятор в шапке · MR-150 округление CEIL, токены подписки + подарок, цены в админку.

**Админка:** ☑ MR-151 оптимизация запросов — закрыта 24.08: вход в админку 4 обращения к серверу без дублей, по вкладке строго её эндпоинты, «Обновить» есть на всех вкладках (на Ценах/Ролях её не было вовсе). Кэш вкладок делать НЕ стали — решение заказчика 24.08: заход на вкладку = свежие данные.

**Поддержка:** MR-152 realtime (websocket) + просмотрено/печатает/ожидает + кнопка Закрыть.

**Роли/RLS:** ☑ MR-153 — блок отключённого работает; 24.08 добавлено: панель закрывается САМА в пределах 10 с после снятия доступа (раньше узнавала только на следующем запросе), вход отключённого отвечает «доступ отключён», а не «неверный пароль», и окна про деньги на экране блока молчат.

**Аккаунты/группы/прокси (Николай):** MR-154 менеджер (тултип/сортировка/скрыть Компании + баги) · MR-155 группы (XML/Google, Добавить, удаление) · MR-156 прокси (копировать, убрать «занят N», ротация, автоопределение).

**Модули/профиль:** MR-157 доступ к модулям (панель покупки, скрытие после покупки, «Мои модули»→«Подписки») · MR-158 профиль + UI-косметика + light-палитра.

**Цели/MCP:** 🟣 **MR-160 (эпик)** Цели→Кампании→Задачи + «мозги» по MCP (уточняющие вопросы до запуска) — заказчик делает параллельно.

**Процесс:** MR-159 human testing обязателен (Илья→Николай) + справки «О модуле» + тест через браузер Claude.

## Новый скоуп 14.08.2026 (созвон-ревью) — MCP + ПРОКСИ В БД + АВТОТЕСТЫ `(ТЗ 14.08)`

> Полное ТЗ — [docs/TZ-2026-08-14.md](./docs/TZ-2026-08-14.md). Jira: **MR-161…MR-177** (label `tz1408`),
> эпик **MR-160 (MCP)** переведён в активную разработку.
> **MCP — единственная блокирующая задача созвона:** без схемы «мозги» не пишутся.
> Дедлайн заказчика — **до 17.08**: MCP, полностью заполненный хотя бы для одного модуля.

**🟣 MCP — целиком наш (Илья / Lane A). Отдельный раздел: [docs/mcp/](./docs/mcp/README.md).**
Николай MCP не разрабатывает, но **обязан регистрировать каждое новое поле и модуль**
в дескрипторе тем же коммитом ([MCP-CONTRIBUTING.md](./docs/mcp/MCP-CONTRIBUTING.md)) —
contract-тест не даст смержить иначе.

- ☑ **Этап 0 (14.08)** — формат дескриптора + полная карта нейрокомментинга (32 параметра,
  9 блоков, 2 набора пресетов, со ссылками на код): [VISION](./docs/mcp/MCP-VISION.md) ·
  [SPEC](./docs/mcp/MCP-SPEC.md) · [MAP](./docs/mcp/MCP-MAP.md) · [ROADMAP](./docs/mcp/MCP-ROADMAP.md)
- ☑ **Этап 1 (14.08)** — дескриптор нейрокомментинга в коде (`server/mcp/descriptors/`, 9 блоков,
  29 параметров / 32 поля, пресеты, примеры) + **contract-тест** (`mcpDescriptors.test.js`, 10 проверок:
  поле в воркере без схемы → красный, поле в схеме без кода → красный, числа пресетов сверяются
  с поведением сервера) + `GET /api/v1/modules/:key/describe` и `GET /api/v1/modules`;
  `GET /api/v1/mcp` отдаёт JSON Schema для описанных и честный `schema: 'partial'` для остальных.
  Тесты 612/612.
- ☑ **Этап 2 (14.08)** — настоящий MCP-сервер: JSON-RPC 2.0 + Streamable HTTP на
  **`POST /api/v1/mcp`** (Bearer), `initialize`/`tools/list`/`tools/call`/`resources/*`;
  6 инструментов (`list_modules`, `describe_module`, `describe_block`, `validate_task`,
  `estimate_task`, `create_task`) + 10 ресурсов `murmex://…`; валидатор черновика задачи
  без запуска (ошибки с указанием поля + предупреждения о полях, которые не сработают).
  Проверено живым HTTP, тесты 630/630
- ☑ **Этап 3 (14.08)** — **покрытие 15/15**: описаны все модули платформы (нейрокомментинг 39 полей,
  парсер пользователей 32, парсеры сообщений и комментариев по 30, нейродиалоги 30, нейрочаттинг 28,
  массовые реакции 25, мейлинг 22, масслукинг 21, прогрев 15, парсеры каналов и групп по 15,
  автопостинг 12, снятие спамблока 3, AI Rating 1) + help-слой по блокам ресурсами `murmex://help/…`.
  *(26.08: цифры пересчитаны из дескрипторов — прежние вели руками и разошлись; теперь их
  сверяет тест. Мейлинг вырос с 19 «полей» до 22 реальных: у него была сломана защита от вранья.)*
- ◐ **Этап 4 (17.08)** — сквозной прогон через протокол на боевом стенде выполнен:
  `describe_module` → `validate_task` → `estimate_task` → `create_task` → `get_task`,
  задача `done` 5/5, инициатор `mcp`. Добавлены `get_task`/`stop_task`. Живой прогон
  вскрыл три дефекта: задачи из API и MCP **никогда не выполнялись** (не сохранялись
  перед `startWorker`), дескриптор AI Rating обещал недостижимое поведение, знаменатель
  прогресса выдумывался у модулей без лимита. Осталось: запрет ручной правки задач
  «под MCP» (MR-148) и уточняющие вопросы «мозгов»
- ☑ **Этап 6 (26.08)** — **соответствие текущей ревизии протокола `2026-07-28`** (мы стояли
  на `2025-06-18`, отстав на три ревизии). Сервер стал **двухэрным**: новые клиенты работают
  без рукопожатия (`server/discover`, per-request `_meta`, `resultType`, коды −32020/−32021/−32022,
  `ttlMs`/`cacheScope`, пагинация, `resources/templates/list`), старые продолжают ходить через
  `initialize` — их не сломали. Добавлены `outputSchema` у всех восьми инструментов, честные
  аннотации (`create_task` → `destructiveHint: true`), четыре рабочих промпта вместо пустого
  списка, обнаружение авторизации по RFC 9728 (401 с `WWW-Authenticate` + `.well-known`).
  Попутно найдено и исправлено: **мейлинг обещал 5 полей при реальных 16** (источник контракта
  был затянут в комментарий, и contract-тест сканировал не тот файл), манифест `GET /api/v1/mcp`
  объявлял несуществующие инструменты `run_*`, в схеме осело 94 склейки строк без пробела и
  33 непереведённые русские строки. Тесты 747/747 — [MCP-ROADMAP этап 6](./docs/mcp/MCP-ROADMAP.md)
- ☑ **Этап 7 (26.08)** — **возможности отдельными срезами**. Появились три вида
  возможностей (`module` / `service` / `user`) и четыре запроса вместо одного: всё сразу,
  только модули, только пользователь и одна конкретная возможность любого вида
  (`GET /api/v1/capabilities[/modules|/services|/user|/users|/<kind>/<id>]`, плюс
  MCP-инструменты `list_capabilities` и `describe_capability`). Подсистемы платформы —
  прокси, менеджер аккаунтов, дашборд задач, статистика, цели, кампании, CRM, каналы,
  логи, автоматизация, баланс — описаны так же полно, как модули, и у каждой посчитано
  `access.allowed` для владельца ключа с причиной отказа. Расчёт прав вынесен из
  приватной функции `usersRoutes.js` в `lib/effectivePermissions.js` и переиспользован:
  панель и API считают доступ одним кодом. Тесты 766/766
- ☑ **Этап 8 (26.08)** — **живая документация `/docs/mcp`**: веб-страница, которая весь
  MCP показывает глазами и умеет выполнять запросы. Инструменты со схемами входа и выхода,
  аннотации, ресурсы, промпты, коды ошибок, две эры протокола, возможности и дескрипторы
  всех модулей — всё читается живыми запросами к этому же серверу, поэтому расходиться с
  кодом нечему. У каждого блока «Run»: видно и запрос с заголовками транспорта, и ответ;
  ключ в панели замаскирован. Рядом — политика использования данных `/docs/mcp/policy`
  (`resource_policy_uri` в метаданных RFC 9728), доступная всегда.
  `npm run dev` → `/docs/mcp` на рабочих данных,
  `npm run mcp:docs` → отдельный порт и изолированные данные (второй экземпляр на общей
  папке глушит задачи первого). На проде выключена без `MCP_DOCS=1`
- ☑ **Этап 9 (27.08)** — **язык MCP закреплён правилом**: всё, что уезжает «мозгам», пишется
  по-английски ([CLAUDE.md](./CLAUDE.md) → «ЯЗЫК MCP»). Понадобилось потому, что схему правят
  несколько агентов параллельно и мейлинг за сутки оказался наполовину переведён обратно на
  русский. Приведено к английскому: мейлинг целиком, остатки в mass-react/warming, 25 полей
  `unit: 'With'` (машинный перевод «с» = секунды) и 4 склейки в фабрике парсеров. Проверка
  расширена с `params` на весь клиентский срез. Правила запуска больше не текут русским:
  логика одна, рендера два — `message` для панели, `messageEn` для MCP. Попутно: `usesAi`
  добавили в дескрипторы, но наружу он не выходил и у пяти парсеров терялся — проброшен;
  `coinsPer1kTokens` убран из ответов как удалённая из модели цен величина
- ☐ **Этап 5** — вынос в отдельный сервис (общие аккаунты с Instagram)

**Дашборд/экономика (Илья):** ☑ MR-161 карточки `grid`→`flex` + `items-start` (пустая
карточка больше не «поднимает» соседнюю) · ☐ MR-162 только кастомные тултипы вместо
нативных · ☑ MR-163 цена сверху = чистая цена за действие из админки (наценку за текст
из витрины убрали, полная стоимость остаётся внизу).
**Решение без тикета:** состав редактируемых настроек задачи **не расширяем** — тайминги
править нельзя (завязаны на длительность/стоимость/риск).

**Профиль (Илья):** MR-164 тариф наверх + убрать дубли лимитов/баланса + валидация пароля
«сперва текущий» · MR-165 удаление аккаунта отдельным красным пунктом меню + юр. текст +
подтверждение вводом e-mail (как GitHub) · ☑ MR-166 цвет баланса по **токенам** (>500 → зелёный; нулевые деньги при живых токенах больше не красят чип в красный).

**Менеджер аккаунтов и прокси (Николай):** MR-167 панель целиком в иконках (play вместо
галочки, «0 из 25», убрать «Выберите аккаунт») · MR-168 «Свободные»/«Удалённые» внутрь
фильтров, убрать кнопку «Колонки» · MR-169 дропдаун прокси поверх «Сохранить» + название
прокси вместо адреса · 🟠 **MR-170 прокси в БД раздельными полями + UID + join-таблица
account↔proxy + уникальность host+port+login+password и названия** ·
🔴 **MR-171 (Highest) баг: массовое удаление снесло все прокси** · MR-172 прокси из менеджера
не попадает в общий список · MR-176 прогрев: тайминги и уровень.

**Автотесты (Илья):** 🟠 MR-173 `id` у ключевых элементов (навигация по Tailwind-классам
не работает) · 🟠 MR-174 сценарии в Claude in Chrome — отдельная запись на модуль, прогон
ежедневно, сотни маленьких проверок.

**Прочее:** MR-175 автообновление грузит запросы чужих вкладок (продолжение MR-151) ·
🔵 MR-177 *(отложено)* Instagram отдельным сервисом с общими аккаунтами — влияет на этап 5 MCP.

## Новый скоуп 17.08.2026 (созвон-ревью) — ТЕСТИРОВАНИЕ + RLS/ХАРДЕНИНГ БД `(ТЗ 17.08)`

> Полное ТЗ — [docs/TZ-2026-08-17.md](./docs/TZ-2026-08-17.md). Метка `tz1708`.
> **Не новые фичи, а ревью + тестирование + харденинг БД.** Дедлайн — **среда 20.08**:
> вычистить To Do / Highest / High и запустить первое полноценное тестирование.
> MCP заказчик принял (живой прогон). В Supabase вскрыт критичный пробел: **RLS-политик
> нет ни на одной таблице**, бэкенд не проверяет доступ (только фронт).

**MCP (Илья):** ☑ Этап 4 закрыт живым сквозным прогоном; три дефекта прогона исправлены
(задачи API/MCP не выполнялись `a67e1dc`, AI Rating дескриптор `8106f9e`, знаменатель
прогресса `103381e`). ☐ Осталось **MR-148** — запрет ручной правки задач «под MCP» + уточняющие вопросы.

**Тестирование (Илья) `(Highest)`:** пошаговая проверка каждого блока (защита/промпты/тайминги),
первый полноценный прогон; эталонный тест таймингов+усталости (распорядок 0–1 % кроме одного
часа, задержка разведена с отдыхом, 2 действия, счётчик усталости растёт только на
СОВЕРШЁННОМ действии); отключение защиты = движок её игнорирует полностью.

**Харденинг БД (Николай):**
- 🔴 **RLS-политики (Highest)** — их нет ни на одной таблице; миграция: доступ `to authenticated`
  только при `exists(profiles p where p.id=auth.uid() and p.active=true)` на каждую таблицу.
- 🟠 **Выход при `active=false`** — выкидывать на логин + плашка по центру (сейчас сломанный экран).
- 🟠 **Схема:** убрать `user_id` из `bundles`/тарифов (он только в `subscription`); `modules.key`
  → `unique`, формат `a`+цифра; добавить `modules.description`; удалить таблицу `api_keys` (drop cascade).
- 🟠 **Баг таймингов** — правка сбрасывает в Custom; Custom не должен быть предустановлен и должен
  наследовать значения пресета, с которого перешли (`TimingSection.tsx`).

**Оптимизация (Илья частично + знать обоим):** `AccessGate` middleware делает 2 запроса на
каждый API-вызов; фронт спамит `my`/`profile`/`user`. Сервер нагружен вхолостую.

**Отложено/доработка:** тулбар менеджера — 4 иконки + остальные в дропдаун (Николай,
«пусть пока весь») · лимит символов в профиле — когда будет нормальная БД.

**Процесс (совместно, до среды):** пройти по всем To Do → Human Testing (highest/high, ~29):
не помним → Backlog, проверено → Done, не проверено → Human Testing. Николай — все To Do в
AI Testing + отчёт по In Progress/AI Testing.

## Новый скоуп 19.08.2026 (созвон-ревью) — КАЧЕСТВО: ЦЕНЫ ИЗ БД + ТЕСТИРОВАНИЕ `(ТЗ 19.08)`

> Полное ТЗ — [docs/TZ-2026-08-19.md](./docs/TZ-2026-08-19.md). Метка `tz1908` (номера MR — при заведении).
> **Не новые фичи, а разбор качества + рефакторинг + ручное тестирование.** Заказчик читает код,
> нашёл: цены считаются неправильно и захардкожены (а не из БД), ручное тестирование не делается.
> Две задачи вернулись из Done. **Дедлайн 1 сентября — 90 % РЕАЛЬНО работает, всё протестировано.**
> Порядок: **1) Цены → 2) Аккаунты → 3) Тайминги → 4) Усталость.** Все задачи созвона — Highest.

**Теги (постоянно):** ручной тест #1 (запуск→логи→реальная TG-группа) · знать БД-таблицу и смотреть
её (JSON в БД = ошибка) · мелкие конкретные задачи · аудит констант · конец дня — список в Telegram.

**☑ 1. Цены (Highest, Николай) — СДЕЛАНО 20.08** (ветка `feat/a-user-subscriptions`, ждёт деплоя).
Цена = `база_из_БД × N`; расчёт за символы/токены (`MAX_TEXT_*`, `fullActionPrice`, `estimateCost`,
`coinsPer1kTokens`) удалён. **Строго БД-only:** в режиме БД `effectivePrices` больше НЕ читает
код-константы — база из `module_prices` (+ `month_tokens`), экономика пространства из
`price_overrides`; нет значения → warn + 0, а не цена из кода. Сетапы (скидочные наборы) вынесены
в таблицы `setups` + `setup_modules` (реляционно) и правятся из админки. В админке «Цены» —
себестоимость и маржа рядом с ценой (MR-149/MR-22/MR-150/MR-151).

**☑ 2. Аккаунты — подписки (Highest) — СДЕЛАНО 20.08** (там же). Подписки JSON→таблица
`user_subscriptions` (строка на user×модуль + `expires_at`), данные перенесены на проде.
Докупка модуля больше не затирает набор (INSERT, а не перезапись); повторная оплата гасится
`unique(user_id, module_key)`. Плюс месячная выдача токенов: начисление в день оплаты
(`billing_day` + `last_credit_month`, идемпотентно) + ежедневный крон (MR-173). Осталось из
пункта: роль = дропдаун, владелец управляет доступами субов.

**☑ 5. Безопасность — аудит 20.08 (через инспектор, вне созвона — по требованию «юзер не должен
видеть, сколько МЫ берём»).** Найдено и закрыто: `/api/pricing` отдавал клиенту `tokenUsd` +
`avgTokens` (из них выводится себестоимость); `GET /api/admin/prices` был БЕЗ гварда — единственный
из 26 админских, а туда добавлены себест/маржа (могли прочитать все 70 юзеров). Отдельный класс
утечек — роуты `async (_req, res)`, не смотревшие, кто спрашивает: цели, кампании, расписания,
группы аккаунтов, агенты и **прокси с логинами и паролями** отдавались всему пространству.
Введён общий паттерн `ownerScopeForRequest` / `ownedForRequest` (`lib/accessGuard.js`): считаем по
владельцу пространства (суб = как владелец), админ и дев видят всё, легаси без владельца — админу.
База каналов остаётся ОБЩЕЙ (дедуп, одна статистика на канал), но клиент видит только каналы,
найденные ЕГО прогонами (`channelsForRequest` по `parse:<taskId>` → владелец задачи).
Покрыто тестами `server/__tests__/ownerScope.test.js`.

**☑ 5б. Безопасность — аудит 21.08 (по вопросу владельца «почему у пользователя видны вообще
все данные с админ-панели? так же платформу нашу взломают»).** Началось с каталога ролей
(показывал аккаунты, папки и каналы ВСЕЙ платформы), но дыра оказалась шире: аудит 20.08 закрыл
СПИСКИ, а точечные роуты `/:id` и всё, что идёт «по аккаунту», остались открытыми.

Закрыто:
- **Действия чужими руками** (тяжелее утечек): нейро-диалоги брали `accountId` из адреса без
  проверки — `POST` ОТПРАВЛЯЛ сообщение от имени чужого аккаунта; `/api/leads/conversation`
  открывал переписку любым аккаунтом платформы по id; `PATCH`/`DELETE` меняли и удаляли чужие
  профили; `empty-trash` чистила корзину всей платформы; `/accounts/activity` отправляла чужие
  аккаунты на отдых (работа клиента вставала без ошибки в его логах); `/accounts/unblock` заводил
  задачу к @SpamBot на чужие аккаунты; `/channels/:id/refresh` собирал статистику чужим аккаунтом.
- **Сам гейт `canSeeAccount`**: проверял только роль, а роль по умолчанию открывает «все
  аккаунты» — имелось в виду «все МОИ». Теперь две оси: чьё пространство (`meta.ownerId`) и что
  разрешает роль внутри него; владельца роль не ограничивает (иначе клиент без ролей не попал бы
  к собственным аккаунтам).
- **Владелец заведён там, где его не было вовсе**: лиды (без него CRM платформы отдавалась любому
  вошедшему), отслеживаемые ссылки, папки целей, правила автоматизации, пресеты модулей,
  импорты TGStat, персоны (`createAgent` не писал владельца — из-за этого свои же персоны не
  показывались в списке).
- **Точечные `/:id`** — цели и их база знаний (включая `/kb-file/:ref` по прямой ссылке),
  кампании, расписания, персоны, группы аккаунтов, роли, прокси с логином и паролем — закрыты
  общей проверкой `ownsRecord` (`lib/accessGuard.js`).
- **Глобальные настройки** (`/ai-settings`, `/ai-safety`, `/target-blacklist`, правка общей базы
  каналов) — только админ: один клиент менял поведение ИИ у всех остальных.
- **Внешний API v1**: `/goals` и `/campaigns` отдавали всю платформу, хотя ключ обещает «от имени
  пользователя»; `POST` создавал «ничьи» записи.

Заслоны стоят на префиксе пути (`app.use('/api/tg/accounts/:accountId')`) и на параметре маршрута
(`router.param('accountId')`), а не в каждом обработчике: дыра завелась ровно потому, что полтора
десятка роутов писались в разное время и в части проверку забыли — заслон на префиксе забыть нельзя.

Покрыто тестами `tenantLeaks.test.js` + `tenantLeaksAccounts.test.js` (23 теста, писались красными
до починки). Прогон: 807/807.

**☐ Хвост аудита 21.08 — разовая атрибуция старых записей.** Запись без владельца по правилам
`ownedForRequest` видит только админ, поэтому на боевом сервере клиент перестанет видеть свои
СТАРЫЕ лиды, персоны и папки. Опаснее прочего — легаси-правило автоматизации: планировщик
продолжает его запускать (тратит аккаунты и деньги), а выключателя у владельца нет.
Готов скрипт `server/scripts/backfillOwners.mjs` — выводит владельца по связям
(задача → цель/кампания → лид, кампания → персона, журнал → инициатор), по умолчанию только
показывает, пишет с `--apply`. **Запускать на проде ДО или сразу после выката**, остаток разбирать
руками по списку, который он печатает.

**☐ Не закрыто осознанно (решает владелец):** cookies TGStat лежат одним файлом на всю платформу —
любой клиент их заменяет или сносит (`tgstat/router.js`: `/session/upload`, `/session/verify`,
`DELETE /session`). Закрытие сломает сценарий «клиент грузит свои cookies», поэтому нужна модель:
свои cookies на клиента или общие только для админа.

**🔴 3. Тайминги (Highest):** баги памяти (шаблон/настройки слетают); тест исполнения по логам +
живая группа; «Параметры» не сверху после групп; нейродиалоги — AI-ответы оставить; пересмотр
useless-параметров («минимум слов в посте» и т. п.).

**☑ 4. Усталость (Highest, Илья) — СДЕЛАНО 20.08 по ТЗ: ТРИ раздельных параметра.**
(20.08 модель ненадолго сводилась к двум полям — владелец в тот же день подтвердил ТЗ,
раздельность возвращена.) Файлы: `server/lib/accountFatigue.js`, `server/accountActivity.js`,
`src/pages/AccountsPage.tsx`, `src/pages/TasksPage.tsx`.
- **Порог действий** — сколько подряд до перерыва; **Отдых** — обязательный перерыв после
  порога (задаётся **часами и минутами**), после него счётчик обнулён; **Восстановление** —
  единиц за час простоя, тает в обычных паузах, пока порог не достигнут (0 = выключено).
  Роли разведены, чтобы поля не «спорили числами» (грабли 18.08): отбытый отдых обнуляет
  счётчик сразу, восстановление после него ничего не довосстанавливает.
- счётчик не превышает порог — «2 из 1» больше не появляется; при пороге 1 отдых после 1-го действия;
- задача **не завершается**, пока аккаунты отдыхают: потолок ожидания 30 мин → 12 ч, в логе
  «Ждём N мин — аккаунт вернётся в HH:MM. Задача продолжает работу»;
- ETA в дашборде считает отдыхи: `⌊остаток / порог⌋` перерывов + тот, что идёт сейчас;
- порог/отдых/восстановление настраиваются **у каждого аккаунта** (массово — по выделению).
- «Рандом инвертирован» **не подтвердилось**: в `scheduleGate` бросок `rnd() > p` — при 89 %
  промах в 11 % случаев, это верно. Проблема была в ТЕКСТЕ лога: «не попал в вероятность 76 %»
  читалось как «система подменила мои 100 %». Теперь пишем «распорядок дня: в 14:00 аккаунт
  активен на 76 %, сейчас не выпало» и повторяем бросок через минуту, а не ждём конца часа.

Модель личности/распорядка — позже.

### Деньги 21.08: скидка при докупке и понятная история `(по вопросам владельца)`

**☑ Скидка набора теперь достаётся и докупке.** Цена докупки считалась по добавленным
модулям ОТДЕЛЬНО, и скидка набора при этом пропадала: клиент, купивший четыре модуля
«Аутрича» и добравший пятый, платил за него полную цену — хотя именно этой покупкой
набор и закрывал. Штраф за то, что покупал не всё сразу. Теперь цена докупки — РАЗНИЦА
(«сколько подписка стала стоить в месяц» минус «сколько стоила»). Проверено живым
прогоном: 4 модуля — $61, пятый (полная цена $20) — **$4**, итого $65 = цена набора со
скидкой 20%. Витрина считает по той же формуле, иначе показанная цена разошлась бы со
списанием.

**☑ История операций отвечает «за что списали».** Было «докупка 1 модул.», стало
«Подписка: докупка до конца подписки — Парсинг», «Токены подписки (первый месяц):
Мейлинг, Нейрокомментинг». Больше трёх имён сворачиваем в «и ещё N».

**☑ История читает тот же кошелёк, что и баланс.** Записи идут под владельцем кошелька
(`resolveWalletOwner`), а история фильтровалась по своему id: сотрудник с общим балансом
видел деньги владельца в шапке и ПУСТУЮ историю.

**☑ Лента вверху различает три беды** (нет подписки / кончилась / нет токенов) и ведёт
в разные места. Раньше человеку без подписки предлагали пополнить баланс — пополнение
ему ничего не давало.

**🔒 Скидка за год на докупку — решение за владельцем.** Годовая −20% на докупку не
распространяется, и формально это правилу не противоречит: скидка даётся «от 12 месяцев
оплаты вперёд», а докупка покрывает остаток срока. Чтобы дать её и здесь, нужно помнить,
что подписка куплена на год, — сейчас в `user_subscriptions` хранятся только модуль и
дата окончания, периода там нет. Это чужая дорожка (таблица Николая), поэтому колонку
согласовывать.

### Задержки 21.08: числа из замеров, видимые в логе `(по вопросу владельца)`

Претензия была одна: «сейчас трешняк и непонятно, как считает». Разобрано на четыре части.

**☑ Бросок по распорядку (MR-175).** Логика не была инвертирована (это сторожит тест
«шанс распорядка не инвертирован»), но убедиться в этом по логу было нельзя — писали
только «не выпало». Теперь в строке стоят оба числа и правило: `в 11:00 аккаунт активен
на 89%, выпало 94 — мимо (проходит 89 и меньше)`. Удачный бросок тоже пишем — молчим лишь
в часы со 100%, где броска фактически нет.

**☑ Скорость набора — из замеров, а не на глаз.** Было «40 слов в минуту» константой.
Стало 26–44 слова в минуту со случайным выбором на КАЖДОЕ сообщение
(`server/lib/humanDelays.js`). Источник: исследование Aalto University / Cambridge /
ETH Zürich (2019), 37 000 участников: среднее 36.2 сл/мин, 75% ниже 44, одним пальцем 29,
двумя большими 38. Одинаковый темп у полусотни аккаунтов — сам по себе признак фермы.
В логе: `Пауза 42 с — читает 12 с, набирает 30 с: 16 сл. со скоростью 33 сл/мин + 0.9 с на подумать`.

**☑ Пауза при переключении модулей — тоже из замеров.** Было 1–5 секунд «на глаз».
Стало 10–45 секунд (resumption lag, в замерах порядка 25 с). Важно не спутать с широко
известными «23 минутами» Gloria Mark — это про возвращение к прерванной сложной работе,
а не про переход между однотипными занятиями. Видно в карточке аккаунта: «Перерыв после
«Нейрокомментинг»: 24 с, осталось 9 с» (`switchPause` в `/api/tg/accounts/:id/stats`).

**☑ Ждём ровно до окна, а не опрашиваем.** Повтор броска считался фиксированной минутой —
одинаково для 89% и для 5%: в первом случае аккаунт зря стоял минуту, во втором воркер
молотил вхолостую 12 раз в час. Теперь срок считается от шанса часа (среднее ожидание до
удачного броска): 89% → 7 с, 50% → 1 мин, 10% → 9 мин, но не дальше конца часа — там уже
другой шанс. Ожидание пачки логируется с точностью до секунд и идёт в общий счёт пауз
задачи (раньше округлялось вверх до минут и в сумму не попадало).

Тесты: `server/__tests__/humanDelays.test.js` (сторожит происхождение чисел, а не сами
задержки) + правки в `accountFatigue.test.js`, `accountBusy.test.js`. Прогон 817/817.

### Аудит 20.08: слои задержек и поведение пачки аккаунтов `(по вопросу владельца)`

Три слоя задержек. Слой 1 — **задержки задачи** (диапазоны в настройках, случайные внутри
диапазона × пресет) — работает во всех модулях. Слой 2 — **задержки аккаунта** (усталость,
распорядок, отдых — сквозные через все модули, `canWorkNow`). Слой 3 — **пауза при переключении
аккаунта между модулями** — **☑ СДЕЛАНО 20.08** вместе с многомодульностью (решение владельца):
- лок смягчён: «один аккаунт = одна задача» → «один аккаунт = один модуль КАЖДОГО типа»
  (`accountLocks.js`, держатели списком; два мейлинга на одном аккаунте — по-прежнему конфликт);
- физическая невозможность: аккаунт не делает два действия одновременно — окно занятости =
  подключение→отключение (`accountBusy.js` + `connectAccount`/`disconnectAccount`); карусельные
  модули и прогрев пропускают занятый аккаунт с логом «Пропуск: занят действием в модуле …»,
  потоковые (мейлинг/диалоги) ждут освобождения;
- пауза при переключении модулей: случайная 1–5 с, в логе «Пропуск: пауза при переключении
  модулей: X → Y»;
- карантин исключён на всех уровнях: `isAccountRunnable` не пускает его ни в один модуль,
  карусели пишут «Пропуск: статус quarantine», диалоги выводят из набора;
- форма запуска блокирует выбор аккаунта только при совпадении МОДУЛЯ (раньше — при любой занятости).
- ☐ хвост: карточка аккаунта показывает лишь первый модуль занятости (`alsoLabels` не выведены).

**Аудит многомодульности 20.08 (10 дыр найдено тестами, все закрыты).** Смягчение лока
обнажило то, что раньше держалось на нём одном:
- ☑ **прогрев остался без защиты** — боевой рантайм-гейт `isAccountRunnable` не знает про
  `warming`, и «Мейлинг + Прогрев на тех же 20 аккаунтах» проходил без возражений.
  Теперь гейт модуль-зависимый (`canModuleUseAccount`) во всех 9 точках воркеров, а
  прогрев — эксклюзивный держатель лока в обе стороны (+ зеркало в форме запуска);
- ☑ **пауза теряла локи**: страховочный `finally` в `launchWorker` звал `finalizeAccounts`
  без флага паузы и отменял решение воркера — «Возобновить» падало с «аккаунты заняты»;
- ☑ **залипшие слоты занятости**: `releaseTaskBusy` в `finalizeAccounts` + протухание
  слота мёртвой задачи (`reconcileBusy`), иначе аккаунт выпадал из всех модулей до рестарта;
- ☑ **слот отбирался по таймауту** — прежний владелец продолжал работать живым клиентом,
  и два модуля действовали одним аккаунтом одновременно; теперь отказ, а не отъём;
- ☑ **«Стоп» ждал до 2 минут** на аккаунт — проброшен `shouldStop` в ожидание слота;
- ☑ **`forceReleaseAccount` сносил всех держателей** — теперь снимает конкретного;
- ☑ **гейт §12** («Освободить» не-админом) смотрел только первого держателя;
- ☑ **мейлинг терял лидов**: статус аккаунта проверялся один раз на старте, ушедший в
  карантин по ходу съедал цели; теперь проверка на каждом круге с выводом из ротации;
- ☑ **спам-разблок и легаси-воркер комментинга** ходили в сессию мимо реестра занятости.

Проверено живым API: прогрев к занятому аккаунту отклонён («Прогрев не делит аккаунт…»),
мейлинг к греющемуся отклонён, комментинг рядом с мейлингом — разрешён.

**Пачка: «недоступен — переходим к следующему, ждём только когда недоступны все»** — работает
в 4 «карусельных» модулях (нейрокомментинг, нейрочатинг, масс-реакции, масслукинг): round-robin,
«Пропуск: <причина>» в лог, полный круг пропусков → ждём до ближайшего возврата (потолок 12 ч).
Хвосты по остальным:

- ☑ **`canWorkNow` доведён (20.08):** мейлинг и нейродиалоги теперь и копят, и спрашивают
  усталость — мейлинг выводит уставших из ротации на круг и ЖДЁТ, если отдыхают все;
  диалоги пропускают уставший аккаунт (собеседник не теряется). Прогрев вне правила
  сознательно. Регресс-тест сверяет списки «копит»/«спрашивает» по исходнику воркеров.
- ☑ **Мейлинг (20.08):** статус перечитывается на каждом круге, недоступный выводится
  из ротации с логом (раньше карантинный по ходу съедал цели без повтора).
- ☑ **Автопостинг (20.08):** канал закрепляется за первым ПРИГОДНЫМ аккаунтом; пропуски
  пишутся в лог поимённо, «некем публиковать» говорится честно.
- ☐ **Прогрев:** полный круг недоступных = немедленное завершение (без «Ждём N мин»). Решить,
  надо ли ему ожидание как у карусельных.
- ☑ **Парсер (20.08):** цели потока, остановленного FloodWait, возвращаются в общую
  очередь — их добирают живые потоки, закончив своё.
- ☐ **Перераспределение целей не динамическое:** цель аккаунта = max(жребий, доля от общего) —
  доля считается от ВСЕХ аккаунтов задачи, включая выпавшие; при постоянной недоступности
  одного аккаунта задача может не добрать общую цель (`server/lib/targets.js:42-66`).

**Логи задержек** — видимы только три класса: «Задержка перед вступлением Nс», «Пропуск:
<причина>» + «Ждём N мин — аккаунт вернётся в HH:MM», «Аккаунт N: старт через Nс» (парсер).
- ☑ **Молчавшие паузы озвучены (20.08):** каждая пишет «Пауза 47 с — задержка перед
  комментарием» с именем аккаунта; подключено в комментинге, чатинге (+ человеческий
  темп), реакциях, масслукинге, диалогах (+ темп), мейлинге, автопостинге.
- ☑ **Сумма ожидания считается (20.08):** `progress.waitMs`, хвост «· в паузах 8 мин» в
  итоговой строке задачи и отдельная цифра в дашборде рядом с прогнозным ETA.

### Аудит 20.08: статус пунктов ТЗ 19.08 по коду (`HEAD 845efb9`)

- §1 Цены: хардкоды ещё в ветке (MODULE_MONTH_PRICE, COINS_PER_1K_TOKENS, estimateCost,
  subscriptionCost); рефактор Николая идёт в `feat/a-pricing-from-db`, не влит. ◐
- §2 срок подписки: ☑ 20.08 — `expires_at` теперь РЕАЛЬНО закрывает доступ (`modulesAllow`
  + оба гейта: `accessGuard` и `notInPlanPayload`), отказы различают «истекла» и «не
  куплено», в интерфейсе видно «Оплачено до …» и метки в меню.
  ◐ Таблица `user_subscriptions` — **делает Николай (решение владельца 21.08): взять наши
  правки и построить таблицу на них.** Была сделана (MR-173) и удалена (`bb14aa8`), когда
  владелец выбрал модель «одна подписка, модули докупаются внутрь»; сейчас набор снова
  JSON-массивом в `subscriptions.modules`. Баги, ради которых заводилась таблица, уже
  закрыты и без неё — переносим ХРАНЕНИЕ, поведение менять не нужно.

  **Контракт: что таблица обязана сохранить.** Эти три вещи терялись при каждом из двух
  слияний 21.08 — сверяться при переносе:
  1. **Одна дата на всю подписку.** Строка на модуль — это способ хранения, а не
     помодульные сроки. Докупка ставит новому модулю дату ДЕЙСТВУЮЩЕЙ подписки и не
     двигает срок остальным. Иначе возвращается дыра: докупка парсера за $8 продлевала
     весь набор за $121.
  2. **Объединение набора на сервере.** Клиентский список — это «что выбрано», а не
     «что оставить». Запись, принимающая список как ТОЧНЫЙ (с удалением отсутствующих),
     вернёт баг 19.08: «Готовый набор» выкидывает оплаченное, деньги списаны, доступ исчез.
  3. **Полный состав при истёкшей подписке.** Чтение, отдающее только действующие строки,
     превращает просрочку в «ничего не куплено»: человек слышит «модуль не входит в
     подписку» вместо «истекла, продлите», и продлевать в кабинете нечего.

  Проверка после переноса — три сценария живьём (мы гоняли их так же): покупка на месяц;
  докупка в тот же день (полная цена, дата на месте); докупка при остатке 10 дней
  (треть цены, дата на месте). Плюс продление — цена за ВЕСЬ набор, дата от конца текущей
  подписки, а не от «сегодня».
- §2 покупка затирает подписку: ☑ 20.08 — сервер сам объединяет набор (режим докупки) и
  не доверяет полному списку от клиента; «Готовый набор» больше не выкидывает оплаченное.
- §2 повторная оплата купленного: ☑ 20.08 — оплаченная плитка залочена (замок, бейдж
  «оплачен», дата), в футере считается цена только добавленного.
- §2 роль одним дропдауном: ☑ сделано 20.08. Чипы-мультивыбор заменены на `select` с
  одной ролью (хранение осталось массивом — сервер суммирует права); подписи приведены
  в соответствие, у записей с несколькими старыми ролями выводится предупреждение.
- §2 единый экран «доступы суба»: ◐ **упирается в архитектуру, нужно решение владельца.**
  Разбор 20.08: набор модулей суба ВСЕГДА читается у владельца — `resolveSubscriptionOwner`
  (`server/users.js:240`, MR-28 §4.1: «модули покупает пространство, а не сотрудник»).
  Поэтому «выдать субу модули» через подписку физически ни на что не влияет: его личная
  запись в `balance.json` не читается ни гейтом, ни меню. Я довёл такую правку до рабочего
  вида (владелец мог менять набор своего суба, с запретом выдавать неоплаченное) и
  **откатил**: это была бы кнопка, которая делает вид, что работает.
  Реально суб ограничивается РОЛЬЮ — `accessGuard` требует обе оси: `can(role,'module',key)`
  И подписку владельца. То есть механизм уже есть, но ТЗ просит «настройки по доступам,
  не ролевые игры».
  **Развилка (решать заказчику):** (а) оставить роли и просто свести их редактирование
  в одно место — дёшево, но это и есть «ролевые игры»; (б) завести ТРЕТЬЮ ось —
  персональные гранты модулей на пользователе (как уже сделано для аккаунтов:
  `accountIds`/`accountGroupIds`), читать её в `accessGuard` наравне с ролью. Вариант (б)
  ближе к ТЗ, но это новая ось прав: нужна миграция, аудит и решение, что сильнее при
  конфликте с ролью.
- §2 добавление/удаление модулей подписки: работает (и в кабинете, и в админке). ☑
- §3 «сохранил шаблон → настройки слетают»: ☑ починено 20.08. `applyPreset` не возвращал
  семь полей, которые `buildSettings` исправно СОХРАНЯЛ (postWindow, stopWords,
  analyzeImages, typeWeights, warmLevel, notifyOnStatus, postUrls) — применённый шаблон
  молча оставлял значения текущей формы. Проверено через UI: сохранил шаблон, сломал все
  значения, применил — вернулись ровно сохранённые.
- §3 нейродиалоги, AI-ответы оставить: соблюдено. ☑
- §3 «минимум слов в посте» (useless-параметры): ещё в UI, пересмотр не сделан. ☐
- §5 регрессия ширины: ☑ 20.08. Причина найдена — `MR-129` (коммит 07d1f2e, 11.08) снял
  глобальный кап `max-w-[1400px]` для роута `/panel/accounts/*`, шапка кап сохранила,
  отсюда «страница шире шапки». Кап вернулся для всех роутов через обёртку-родителя, так
  что и плавающая панель запуска модулей поехала по блоку. Высота не пострадала.
- §5 поп-ап блокировки: ☑ 20.08. Фраза про подписку убрана (это админ-блок), текст
  переписан: «Доступ к платформе отключён администратором. Напишите в поддержку…».
- §5 ссылка «инкубатор тест» в истории: ☑ 20.08 — вместо ссылки наружу кнопка
  «Скопировать ссылку на объект» (свой вьювер — позже, как в ТЗ).
- §5 «проверка спамблока» в истории аккаунта: ☑ 20.08 — событие пишется тем же
  `recordAction`, что и действия воркеров, и видно в ленте с результатом.
- §5 MR-151 дубль-запросы: ☑ 20.08. Потребителей было ТРИ (чип шапки, LowBalanceBar,
  usePlan) — заведён общий стор с дедупликацией по in-flight промису и одним таймером на
  всех. Проверено в Network: один `/api/balance` на загрузку даже под React StrictMode.

**☑ Прочие баги — закрыты 24.08:** UI-регрессия ширины (MR-56) · поп-ап блока: «продлите подписку»
убрано (MR-177) · «проверка спамблока» в истории аккаунта — пишется и видна, подтверждено живым
запуском @SpamBot 24.08 в 17:05 (MR-78) · MR-151 дубли: `/api/admin/overview` уходил 4 раза с
одними параметрами — отметка «загружено» ставилась после ответа, теперь занятым считается и то,
что уже в пути. Осталась ссылка «инкубатор тест» в истории.

**☑ Подтверждено рабочим:** MR-141/142 (раздельные сессии), MR-153 (блок аккаунта), MR-95 (история/даты).

## Новый скоуп 24.08.2026 (созвон-ревью) — ДОТОШНОЕ ТЕСТИРОВАНИЕ + ХРАНЕНИЕ ТОЛЬКО В БД `(ТЗ 24.08)`

> Полное ТЗ — [docs/TZ-2026-08-24.md](./docs/TZ-2026-08-24.md). Метка `tz2408`.
> Заказчик отдельно похвалил цены/калькулятор (Николай) и управление пользователями (Илья).
> Но каждый созвон открывается тем, что «закрытое» не работает: **из Done вернулись MR-176,
> MR-37, MR-157**, и отвалилась поддержка у заблокированного (в пятницу работала).
> **Дедлайн прежний: 1 сентября, 90 % функций реально работают.**

**Теги — к пяти прежним добавились новые (постоянно):** дотошные сценарные тесты (не «кнопка
есть», а «зайти под юзером 1 → изменить → выйти → зайти под юзером 2 → проверить дефолт») ·
проверять БАЗУ, а не экран · **никаких локальных хранилищ** (ни in-memory, ни файлов, ни JSON, ни
локального SQLite, ни localStorage — только общая БД) · перекрёстная проверка задач друг друга ·
читать текст задачи перед тем, как отдать её ИИ · присылать логи протестированных задач.

**🔴 MR-176 (Highest, Николай) — ВЕРНУЛАСЬ В BACKLOG.** Шаблон не восстановил текст промпта
(«123» не появилось, открылся дефолтный), «минимум слов» и мин/макс. Корень найден в коде и
общий с MR-185: тексты промптов лежат в `localStorage` браузера
(`shared/promptDefaults.ts`, ключ `ai-incubator:prompts:<модуль>`) **без привязки к пользователю**,
а `PromptCards` держит свою копию оттуда же и перекрывает ею то, что выставил `applyPreset`.
Лечится переносом промптов в БД (тег «никаких локальных хранилищ»), а не правкой UI.
Плюс: кнопка шаблона не подсвечивается в момент нажатия.

**🔴 Тайминги (Highest, Николай).** На двух аккаунтах вверху «Полное время ≈ 25 мин», внизу
«≈ 13 мин»: нижняя панель делит время на число аккаунтов. Аккаунты работают **параллельно**, лимит
задаётся как «сколько сделает 1 аккаунт», поэтому ни умножения, ни деления быть не должно — оба
места показывают одно число.

**🟠 Цены — приняты с правками (Николай).** Убрать метку «изм.» · «200 в месяц + 200» — второе
«+200» убрать · подарочные начисляются единоразово · в пакетах токенов показать ту же математику
(за сколько покупаем / за сколько продаём) · скидки и периоды — в деньгах, не только в процентах ·
годовая подписка не выдаёт токены за год вперёд. Позже — калькулятор по аккаунтам (аккаунты
продаются, прокси арендуются).

**🔴 MR-157 (Highest, Илья) — ВЕРНУЛАСЬ.** Неоплаченные модули не скрывать: показывать всегда,
read-only, всем включая суб-юзеров; вместо панели запуска — яркая оранжевая панель
«предпросмотр → оплатить»; запуск закрыт и прямым запросом к API; кнопка скрыть некупленные —
на странице подписок и в меню. История операций перекрыта плашкой → вынести кнопкой справа сверху
в модалку, добавить иконку $/⚡ и баланс «до/после» крупно, зелёным на плюс и красным на минус.

**🔴 MR-37 (Highest, Илья) — ВЕРНУЛАСЬ.** В мейлинге блоки доступа не соответствуют реальности
(есть промпты, которых в модуле нет; нет «Сообщений»); блоки «Сообщение» и «Защита» слиплись.
Перепроверить названия и фактическое действие переключателей во всех модулях. Терминология:
«роли» или «шаблоны доступа» — выбрать одно слово и держать везде.

**Экономика (Николай):** MR-187 в пакетах токенов показать НАШУ себестоимость ⚡ и маржу —
сейчас видна только цена продажи, и непонятно, можно ли двигать цену · MR-188 скидки и периоды
показывать в деньгах, а не только процентом · MR-189 убрать метку «изм.», убрать дубль «+200»,
подарочные начислять единоразово.

**Новые задачи созвона:** MR-180 протестировать «Новый пользователь» (индивидуальный лимит; нельзя
выдать больше, чем есть у админа; показывать «было / останется») · MR-181 трекинг подписок по
каждому модулю отдельно (свой срок истечения) · MR-182 настройки усталости и шаблоны распорядка
дня — в БД + редактирование из админки · MR-183 распорядок дня берёт время сервера вместо времени
пользователя · MR-184 заблокированный должен мочь написать в поддержку (на созвоне отвалилось) ·
MR-185 промпты применяются ко всем пользователям вместо одного.

**☑ Подтверждено рабочим на созвоне:** MR-178 автодеплой (разобран сам workflow) · MR-173 аккаунты
подписки · MR-174/175 усталость и шанс по часам (инверсии нет) · MR-36 доступ переключателями ·
MR-22 калькулятор · запросы в админке «только те, что нужны на странице» (MR-151).

**Организационное:** перед платёжкой — бета с посторонним человеком; при 3–5 % возвратов платёжку
отключат. Созвоны по 3 часа сократятся, когда появятся дотошные автотесты и перекрёстная проверка.

## Новый скоуп 27.08.2026 (созвон-ревью) — ПРОЦЕСС РАБОТЫ + ЭКОНОМИКА ТОКЕНОВ `(ТЗ 27.08)`

> Полное ТЗ — [docs/TZ-2026-08-27.md](./docs/TZ-2026-08-27.md). Метка `tz2708`.
> Показ задач Николая прошёл хорошо: цены, скидки, периоды, пакеты токенов, тайминги,
> шаблоны и промпты **приняты** — «всё стало понятно, тут даже дурак разберётся». Но главный
> разбор был не про фичи, а **про то, как мы работаем**: прямые пуши в `main`, параллельные
> задачи в одной ветке и проверка сразу на бою названы причиной того, что «клауд делает криво
> и приходится всё перепроверять». **Дедлайн прежний: 1 сентября.**

**🔴 Теги 12–15 — процесс (оба, с этого дня, сильнее любого пункта по фичам):**
**каждая задача — в своей ветке** (ветка = отдельная копия проекта; «ломается ветка, а не
проект») · **пуш только в свою ветку, мерж в `main` — только после явного разрешения человека**
(«пока вы не сказали, что всё проверили, ни хрена не должно вливаться») · **проверять локально,
а не в лайве** · **прописать это в `CLAUDE.md`**. Механика, которую разобрал заказчик: две
задачи стартуют от одного состояния кода, первая пушится через пять минут, вторая всё ещё
«верит» в прежний код — и на пуше разбирает чужие правки в тех же строках, «и там начинается
каша». Отдельно показаны: **выбор элемента «мышкой» во внутреннем браузере** (в задачу уезжает
ссылка на элемент в файлах кода + скриншот — «сужает воронку») и флаг **`USE_LIVE_API`** в
api-конфиге (`true` — данные с сервера, `false` — с локального).

**☑ MR-187 пакеты токенов — цены УЖЕ на боевой** (50 — $6.99, 200 — $21.99, 500 — $59.99; на созвоне называли 59, уточнить). Как считали: Пересчитали на созвоне: 50 ⚡ = $6.99, значит
200 ⚡ — вчетверо больше, ≈$28, а продавали за $17.99 («мы дарим дохуища»). **200 ⚡ → $21.99**,
**500 ⚡ → $59**. Цены живут в БД (`price_overrides.coin_packs`), меняются из админки.
Отдельно **MR-194: скрыть цену одного токена под пакетами в кошельке** (AppHeader, `p.price / p.coins` — для 6.99/50 это и есть 0.140) — «а то мы тогда будем
проёбываться»; не удалять, а закомментировать, в админке оставить. Что именно считаем —
зафиксировано: **чистая математика по токенам**; аккаунты, прокси, трафик и риск банов не
входят, их покрывает наценка сверху.

**🔴 MR-193 месячное начисление — НЕ ПРОТЕСТИРОВАНО.** Опасение подтвердилось в тот же день: Илья нашёл, что таблица user_gifts на боевой ПУСТАЯ — подарочные не получил никто, выдача жила в одной точке (в момент оплаты), добавлена досверка шестичасовым тиком (65291c7). Отображение принято,
но начисление никто не проверял. Сценарий заказчика: поменять период **с месяца на минуту**,
убедиться, что каждую минуту приходит 1400 ⚡, что деньги списываются (нужен баланс), что при
годовой подписке деньги не снимаются, а токены идут, и что **разовый подарок начислился ровно
один раз** — на втором «месяце» не должно быть ещё +311. Отдельная задача на тестирование.
Причина жёсткости: «придёт клиент, пройдёт месяц, и он скажет — какого хера я не получил свои
токены».

**🟠 MR-176 + MR-185 шаблоны и промпты — приняты с правками (Николай).** Подтверждено
правильным: шаблон админа виден его субам, шаблон суба виден админу, посторонний не видит
чужого, у каждого свои промпты и свой глобальный промпт, **применение чужого шаблона
подтягивает промпты целиком** («это абсолютно разумно»), вся плашка кликабельна. Правки:
галочка **«с аккаунтами / без аккаунтов»** при создании, **по умолчанию без** · шаблон
администратора **не редактируется** субом · **нативные `window.confirm` заменить на нашу
модалку** (правило общее — все всплывашки наши) · **убрать белое выделение** активной карточки
промпта, оно ничего не даёт (тип решает «Распределение типов»). Редактирование содержимого
шаблона решили не делать.

**🟡 AI-промпты: UI/UX — ВОПРОС, НЕ ЗАДАЧА (оба).** Заказчик остановил обсуждение и записал
отдельным пунктом: «мне кажется, все объебутся об это распределение, оно у нас очень сложно
сделано». На экране сразу три сущности: карточки промптов, предпросмотр итогового текста и
распределение типов в процентах. Прозвучало предложение сделать распределение **необязательным**,
а по умолчанию дать человеку выбирать тип самому. **Решения нет** — заказчик ждёт предложений
от обоих; до ответа поведение не меняем.

**🟠 Мелкие правки не из задач (Николай).** История операций на странице подписок показывает
**общую** историю с токенами, а должна — только подписки и купленные модули; разбор заказчика:
«надо было перенести в модалку блок, который уже был внизу страницы, а сделали новый с нуля» ·
историю своих трат негде найти (нашли через статистику → кошелёк, «очень сложно») · на дашборде
задач текст «Выполненные задачи» убегает на вторую строку · раздел «Статистика» переработать.

**🟠 Зона Ильи (для перекрёстной проверки, тег 9).** Лимит токенов суба теперь отталкивается от
баланса владельца, есть «без ограничений»; суб не видит баланс в долларах; вместо «Пополнить
счёт» у суба нужен блок **«Запросить токены»** · **баг: после выхода на фронте остаются данные
предыдущего пользователя** (показалось 80 ⚡, через две секунды 200 ⚡) — выход не чистит кэш,
память запросов и куки, нужна **отдельная задача** · аккаунт в двух модулях больше не уходит в
бесконечные таймауты · группы перестали отображаться после миграции БД · «сколько сделает
аккаунт для одной цели» → переименовать в **«для действия»** · прогрев переведён на «действий за
N часов, N дней» · таймер показывает «отдых после действия» там, где должно быть восстановление
(12 мин) · **вернуть синхронный/асинхронный режим** — сейчас аккаунты в одной задаче идут
поочерёдно, а нейродиалоги должны параллельно.

**☑ Подтверждено рабочим на созвоне:** MR-188 скидки и периоды в деньгах (принято без
замечаний — видно цену без скидки, размер скидки в деньгах, итог; у наборов видно, сколько
стоили бы модули по отдельности: было $185 → стало $120) · MR-187 пакеты токенов (с правками
выше) · MR-56 тайминги, потолки полей и запрет отрицательных значений · MR-176/MR-185 шаблоны и
промпты (с правками) · у Ильи — логи по пользователям и субпользователям, статусы и сортировка
на дашборде задач.

**Задачи с созвона заведены: MR-193…MR-208** (метка `tz2708`, не ниже High, четыре — Highest). Перед заведением каждый пункт сверен с кодом и боевой базой уже с учётом десяти коммитов Ильи от 27.08: цены пакетов оказались сделаны, «Пополнить счёт» у сотрудника уже убран (осталось поставить «Запросить токены»), выход уже делает жёсткую перезагрузку (искать надо то, что её переживает). Разбор — [ТЗ §9](./docs/TZ-2026-08-27.md).

**Организационное:** формат показа — не «сделано, поехали дальше», а **показать и рассказать по
всем пунктам задачи**. Созвон закончился раньше: прогрев, синхронный режим и параллельность
нейродиалогов остались недосмотренными и перейдут на следующий.

## Новый скоуп 30.08.2026 (созвон-ревью) — ДЕНЬГИ ВЫШЕ ВСЕГО + БЕЗОПАСНОСТЬ ХРАНИЛИЩА `(ТЗ 30.08)`

> Полное ТЗ — [docs/TZ-2026-08-30.md](./docs/TZ-2026-08-30.md). Метка `tz3008`.
> Показ прошёл хорошо: подписки, начисления, списания, уведомления, регистрация и чистка базы
> **приняты** — «с подписками прямо супер», «со всех сторон работает, даже не докопаться». Но
> заказчик впервые открыл хранилище браузера и увидел там **токены обеих зон и флаги прав**
> (`isAdmin`, `isOwner`) — это перевесило всё остальное. Плюс сменились приоритеты: всё, что
> приносит деньги, идёт выше того, что не приносит.
> **Дедлайн: 1 сентября (вторник) до 13:00 — выход в лайв.**

**🔴 Безопасность хранилища (Highest, оба).** В браузере лежат `token`, `admin-token`, а в
записи сессии — `isAdmin`, `isOwner`, `isSub`, роли и e-mail. Дословно: «токены — это данные,
которые должны быть скрыты за четырьмя замками»; «если я поменяю значение этих флагов, я что,
стану админом?»; «максимум, что может храниться, — это id». Убрать флаги и роли (права решает
сервер), убрать e-mail, пересмотреть хранение токена. Сама MR-203 при этом **принята** —
найденное идёт отдельной задачей.

**Правило приоритета, дословно:** «всё, что связано с деньгами, идёт приоритетом над всем,
что не связано». Парсер каналов — ниже продажи аккаунтов.

**Приоритеты Ильи:** 1) **аллокация токенов** — владелец ВЫДАЁТ токены субу, они списываются
с его баланса; сейчас «индивидуальный лимит» ничего не выделяет, а показывается субу как
баланс, и сумма лимитов пяти сотрудников может втрое превышать остаток владельца. Рядом с
токенами писать, сколько это в деньгах. 2) **продажа аккаунтов и прокси за токены** — пул
аккаунтов в админке, градация по прогреву (без прогрева → лёгкий → средний → глубокий, дороже
за глубину), суб запрашивает аккаунт у своего владельца; **всё внутри системы продаётся
только за токены**, за деньги — подписка и токены. Открытый вопрос: где хранить tdata (~250 МБ
на аккаунт). 3) **усталость** — простой дефолтный выбор образами («работает днём», «учится
утром»), цифры и вероятности убрать в расширенные настройки; восстановление действий — 1 мин
вместо 10.

**Приоритеты Николая — UI-долг:** отмена подписки с подтверждением и составом; плашка
«текущая подписка» на витрине с затемнением остальных; **история кошелька** — не обрезать
текст, две строки (`до → после` сверху, дата снизу), фильтр «только токены / только доллары»;
панель периодов и «Экспорт» опустить вниз к «Дашборд / Запуски / Кошелёк»; ID задачи перед
названием и с копированием; **экспорт качает не тот раздел** (всегда дашборд); счётчик
аккаунтов — «8 из 10 работает» вместо лимита `0/50`, по клику список упавших с причиной;
**баг: 98 аккаунтов из 50**; один инпут телефона с дропдауном кода страны; прокси —
одинаковый вид в обоих местах, клик = копирование, смена отдельной кнопкой; переписка
суб-юзер ↔ админ внутри системы и плашка «Связаться с администратором» вместо «Обратиться в
поддержку»; убрать кнопку `Dev`.

**Упрощение модулей (High, общая).** Панель запуска: заголовок → четыре кнопки настроек →
«Начать». Блоки «Параметры», «Лимиты», «Защита и тайминги», «Промпты» по умолчанию **свёрнуты**
и показывают одну строку сводки через `·`. Обоснование: «90 % пользователей не будут менять
настройки; им нужно три кнопки — выбрать пользователей, выбрать группу, начать». **Прогрев
упростить обязательно до релиза** — «мы не можем запустить в продакшн модуль прогрева, в
котором будет куча настроек».

**Движок задач.** Задача зависла: после «отдыхает 2 минуты» запустилась ещё и «пауза между
действиями», дальше несколько пауз подряд и остановка — «когда он отдыхает 2 минуты, ты 2
минуты его не трогаешь». Галочку «асинхронный режим» убрать: аккаунты **всегда** работают
параллельно, «это независимые пользователи». Разброс старта задавать как N случайных значений
из общего окна, а не попарными паузами. Нейрочаттинг: аккаунт не отвечает сам себе, диалог
между своими не уходит вглубь, заинтересованность важнее распорядка.

**Промпты (High, Илья).** Отдельная таблица промптов, привязка к `user_id`, а не запись внутрь
данных пользователя; открепил — вернулся дефолт; шаблоны перенести из кода в БД. Это остаток
MR-190.

**Подтверждено правило начисления:** списание и начисление — **через 30 дней с момента
оплаты**, а не по числу месяца. «Не доебаться, тогда правильно».

**Задачи с созвона заведены: MR-224…MR-251** (28 штук, метка `tz3008`; четыре Highest —
MR-224 хранилище, MR-225 аллокация, MR-226 продажа аккаунтов, MR-235 счётчик 98 из 50).
Не заводили то, на что задача уже есть: MR-214 (прогрев мин-макс), MR-223 (ответ самому
себе), MR-182/MR-183 (усталость в БД и локальное время), MR-202 (статистика), MR-199
(история операций), MR-204 («Запросить токены»). Отдельно: MR-206 и MR-215 просили вернуть
переключатель синхронного режима — решение изменилось на противоположное, см. MR-241.

**Организационное:** регламент задач ([MR-210](https://provotorov200.atlassian.net/browse/MR-210))
действует и прочитан обоими: Claude работает только из задачи, тестирование — по команде,
`Done` ставит человек, после `Done` — два отдельных действия `мердж` и `пуш`. Миграции БД
теперь накатываются автоматически после каждого пуша. Капчу на регистрации оставляем как есть
(тестовый виджет Cloudflare, настоящие ключи позже). Промежуточное тестирование — на людях со
стороны.

## Граф зависимостей

```
Фаза 0 ─┬─> A: статусы/локи ─> A: задачи ─> A: статистика каналов
        ├─> A: безопасный выбор ─> B: трекер задач (UI)
        ├─> B: Goal/KB/Lead ─> B: CRM/нейрочатинг ─> B: аналитика
        └─> B: логи/термины ─> B: Help Center / детальная вью
§6 (открытые вопросы) ─(блокируют)─> trust score, прокси/SIM, семантика, период статистики
```

Открытые вопросы (§6 ТЗ), гейтящие пункты `🔒`: trust score · прокси/SIM/регистрация · метрики/формула
рейтинга канала · период статистики · роли · safety-лимиты чата · семантический поиск · Instagram/ComfyUI.

## Инструменты команды

- **codebase-memory-mcp** — граф кода для навигации (ставит каждый у себя, см. `CLAUDE.md`).
- **Файловая память Claude** (`.claude/.../memory/`) — решения и контракты между дорожками.
- Верификация: `npx tsc --noEmit` + превью; бэкенд после правок воркеров/схем — перезапуск.

---

# Часть II. Текущий статус (что уже есть) — срез 2026-07-10, `main`

**Стек:** React 18 + TS + Vite 5 + Tailwind 3 · Node Express (ESM) + GramJS + cheerio · zustand, react-router v6.
Прогресс — polling. Данные модулей — JSON в `server/data/`. Секреты в `.gitignore`.

## ✅ Что работает (по модулям)

Все 12 модулей имеют реальный бэкенд-воркер (`workers.js`, карта `WORKERS`) — живые вызовы GramJS.

### Парсеры (протестированы на 5 живых аккаунтах)

| Модуль | Ключ | Статус | Что делает |
|---|---|---|---|
| Парсинг каналов | `parsing` | ✅ live | Поиск по ключам/окончаниям, фильтры, дедуп, ★-рейтинг, экспорт |
| Парсер групп | `parsing-groups` | ✅ live | То же для групп, шаблоны, ИИ-подсказки |
| Парсер пользователей | `parsing-users` | ✅ live | Участники + «кто писал» (роли), пересечение аудиторий |
| Парсер по сообщениям | `parsing-messages` | ✅ live | Юзеры по ключам в сообщениях, фильтр дат, активность |
| Парсер комментариев | `parsing-comments` | ✅ live | Юзеры из комментов, фильтры, лимиты |
| TGStat-парсер | `tgstat` | ✅ live | Каталог + расширенный поиск с фильтрами (cookies) |

### Модули инкубатора (бэкенд реальный; системный live-прогон — в бэклоге, B3)

| Модуль | Ключ | Бэкенд | Действия в TG |
|---|---|---|---|
| Нейрокомментинг | `neuro-commenting` | ✅ | join + `sendChannelComment` + OpenAI |
| Нейрочаттинг | `neuro-chatting` | ✅ | `sendMessage` в группах, контекст |
| НейроДиалоги | `neuro-dialogs` | ✅ | мониторинг ЛС + авто-ответы (ответы всем + пресеты) |
| Массовые реакции | `mass-react` | ✅ | `sendReaction` |
| Масслукинг | `mass-looking` | ✅ | `markStoriesRead` / просмотр постов |
| Прогрев | `warming` | ✅ | имитация активности |
| GGR-рейтинг | `ggr` | 🟡 БЕТА | оценка качества аккаунта |

### Инфраструктура

Менеджер аккаунтов (фильтры/прокси) · локи (1 аккаунт = 1 задача) · ИИ-защита (3 уровня) ·
задержки + FloodWait/карантин · папки целей с валидацией · экспорт CSV/Excel · история парсинга ·
Help Center (OpenAI `gpt-4o-mini` + fallback) · чёрный список · пресеты во всех модулях.

## ✅ Закрыто полностью

Богатый парсер каналов (бэк) · `ModuleTaskSettings` · `ChannelParserModule` · роутинг + прогон ·
TGStat (парсер/store/router/клиент/UI) · переключатель режимов · `ParticipantsParserModule` ·
парсинг участников (фильтры/мульти-цели) · TGStat как источник целей · полировка групп ·
пересечение аудиторий · экспорт `.xls` · ★-бейдж · code-split + автотесты · режим «кто писал» ·
папки + валидация · расширенный поиск TGStat · OpenAI-ключ (health/Help Center) ·
НейроДиалоги (ответы всем + пресеты) + фикс зависания воркеров · пресеты во всех модулях ·
единый разбор «защиты аккаунтов» + разделы «Риски» в Help Center · правки GGR.

## 📘 Документация проекта

[docs/PROJECT-2026-07-22.md](./docs/PROJECT-2026-07-22.md) — что это за система, иерархия
Цель→Кампания→Задача→Агент→Аккаунт, готовность по каждому модулю и разделу, биллинг,
безопасность аккаунтов и открытые вопросы по прайсам. Это же ядро отчёта к пятнице (§5.5).

## 🧪 Ручной прогон 21–22.07 по ТЗ и роадмапу — 39 находок из 41 закрыто

Полный отчёт с описаниями и причинами: [docs/TEST-PLAN-checklist.html](./docs/TEST-PLAN-checklist.html)
(118 тестов, результат у 105; остальные 13 сняты как разрушающие или недоступные без стенда).
Каждый коммит-правка назван номером теста, поэтому связь «находка → фикс» прослеживается по `git log`.

**Главный вывод прогона.** Восемь защит были написаны и покрыты юнит-тестами, но в бою
молчали — до них не доходили данные. Это оказалось важнее любой отдельной находки:

| Защита | Почему не работала |
|---|---|
| Права на папки целей (§8.1) | `GET /target-folders` отдавал все папки всем — фильтр жил только во фронте |
| Аудит опасных действий (§3.1) | папки, лиды и расписания не вызывали `appendAudit` вовсе |
| Точечный запрет доступа (§8.1) | `mergePermissions` копировала только ALLOW, запрет терялся |
| Дедуп целей (§3.7) | без `toLowerCase` — `@Crypto` и `@crypto` два канала, двойная обработка |
| Разные тексты у аккаунтов (§9) | вариант шаблона брался по `promptIndex` — настройке задачи, общей для всех |
| «Непрогретый в бой не идёт» (§3.3) | статус `warming` не выставлял никто, он был недостижим |
| Гейт trust (§6) | fail-open: аккаунт без оценки — самый уязвимый — проходил |
| Честный статус постинга (§8.4) | задача с 0 из N показывалась как «Готово» |

**Также закрыто:** потеря входящих в переписке и сортировка инбокса по алфавиту (§9.5);
пауза парсера теряла прогресс и результаты (§3.9); «Стоп» не работал на задаче с паузы;
учёт рабочего времени считал календарь (одна «сессия» в 98 часов); подпись задачи не
различала парсеры по ключевым словам; запуск правила боевого модуля без подтверждения;
импорт прокси игнорировал строку-схему и ссылки ротации IP; живость прокси мерялась
TCP-пингом вместо протокольного рукопожатия; `geoSource` не сохранялся; модалка смены
прокси читала пустой мок-стор вместо базы; у кампании не было своих целевых каналов,
а рядом жил неотличимый «разовый запуск»; «?» блока открывал статью всего модуля.

**Осталось 2 — кода в них нет:** `9.10` (проверить через `GetAuthorizations()`, что аккаунт
реально ходит через прокси) и `6.11` (не воспроизводится: форма такое состояние не собирает,
бэкенд его отклоняет).

Автотесты: **288 зелёных**, из них 33 регресс-теста на эти находки
(`server/__tests__/qaFixes2607.test.js`, каждый назван номером теста).

## ⛔ Бэклог / известные моменты

| # | Пункт | Приоритет | Детали | Куда в плане |
|---|---|---|---|---|
| B1 | Формула рейтинга ★ | ☑ | ★ по §6: приоритет **ER** (вовлечённость = (реакции+комменты)/просмотры, ~4%+ = полный бонус) + нормализ. подписчики (занижены) + открытые комменты (`channelRating`). ER считается 2-м проходом (`engagementFromPosts` в `channelStats.js`, средние views/reactions/comments), fallback на свежесть если ER нет. Юнит-тесты | §3.8 |
| B2 | `parsing-users` на закрытых группах | средний | `CHAT_ADMIN_REQUIRED` → подсказка режим «Активные» | — |
| B3 | Live-прогон инкубатор-модулей | высокий | `neuro-*`/`mass-*`/`warming`/`ggr` системно на 5 акках не гоняли | Фаза 1–2 (тест-прогон) |
| B4 | Прокси-фильтр | низкий | Дефолт «Рабочие прокси» = OFF | Фаза 3 §3.4 |
| B5 | Прогресс/остановка | низкий | UI-отклик остановки с задержкой | Фаза 2 (Task) |
| B6 | Реалтайм | низкий | polling 2.5s без websockets | — |
| B7 | GGR | низкий | БЕТА, формула требует калибровки | §6 (метрики) |

## Тесты и качество

`npm test` (`parser.test.js` + `aiHelp.test.js`) · `npm run lint` (`tsc --noEmit`, 0 ошибок) · `npm run build`.

## Что подключает каждый у себя (нет в репо)

`.env` (`OPENAI_API_KEY`, `TELEGRAM_API_ID/HASH`) · cookies TGStat · аккаунты/сессии через Менеджер.

---

# Часть III. Правки по созвону 17.07.2026

> Полный разбор демо/кросс-теста (Илья + Николай + заказчик) с архитектурой оркестрации
> **Цель → Кампания → Задача → Модуль** и автоматизацией под кампанию/задачу:
> **[docs/meeting-2026-07-17.md](./docs/meeting-2026-07-17.md)** — 14 разделов + 5 фаз работ.
>
> Ключевое (🧠 требует утверждения схемы до старта): резерв→кампания, кампания настраивает
> модуль (3 режима выбора аккаунтов), автоматизация только под кампанию/задачу, вьюшки вместо
> модалок, «выбор кампании» в модуле вместо «цели», проверка уникальности задачи.

**Статус на 22.07.2026: 70 ☑ / 6 ◐ / 3 ☐** из 71 пункта разбора. Полностью закрыты
§1 резерв→кампания · §2 менеджер · §5 кампании · §6 автоматизация · §7 модули ·
§13 Help Center · §14 деплой. Детали и что осталось — в самом файле разбора.

**Разделение 21.07:** §10 Прокси и остаток §11 Mailing взял Илья («не трогай, я вже роблю там») —
Николай в `AddAccountWizard.tsx` / `ProxyPoolModal.tsx` / `proxies.js` / `mailing.js` / `MailingPage.tsx`
не заходит.

Требования созвона внесены в ТЗ как **[§9](./docs/TZ-2026-07-10.md#9-дополнения-от-17072026-демо--кросс-тест-с-заказчиком)**
(раньше жили только в файле разбора — из-за этого роадмап их не отслеживал).

### Что осталось по §9 ТЗ — поимённо

**Lane A (Илья):**
- ◐ §9.10 Прокси: выбор из базы в мастере добавления · новый прокси → имя аккаунта + попадание в базу · цепочка SIM/device/IMEI.
- ◐ §9.11 Mailing: конвертация телефон↔username · папки для номеров.
- ☑ §9.4 Дедлайн цели останавливает и УЖЕ ИДУЩИЕ задачи: проверка в цикле всех шести воркеров (раньше дедлайн смотрели только при создании задачи, и запущенная накануне рассылка работала после срока). Оркестрация/воркеры).
- ☑ §9.12 Барьер прогрева в менеджере аккаунтов: массовый «Стоп / освободить» снимал локи напрямую через `/release` и обходил защиту — остановить недели работы можно было в два клика. Теперь тот же 403, что и в модуле.
- ◐ §9.14 LIVE-прогон деплой-пайплайна на боевом сервере (нужны доступы).

**Lane B (Николай):**
- ☑ §9.3 / §8.7 Общий компонент переписки: `ConversationBubble` + `ReplyBox` в `features/conversation`,
  подключены и в «Обзор аккаунта», и в НейроДиалоги. Копия разметки в НейроДиалогах удалена, вместе
  с ней ушёл мёртвый `fmtReply`/`replyRef`. Обзор аккаунта получил превью медиа (бэкенд отдавал их
  и раньше — страница просто не показывала), многострочное поле и панель форматирования.
- ☑ §9.8 Редактирование задачи — только на паузе. `PATCH /:moduleKey/tasks/:id/settings` (409, если
  статус не `paused`) + чистые `canEditTask`/`pickEditableSettings` (4 теста) + панель правки во
  вьюшке. Состав аккаунтов править нельзя: за задачей держатся локи, подмена оставила бы их висеть
  на чужих профилях — это отдельно закреплено тестом.

**Ждёт заказчика (не код):**
- ☐ §9.4 «Время выполнения» цели — отдельное поле или дубль дедлайна?
- ☐ §9.10 Какие именно «доп. настройки прокси под аккаунт» — нужен список.
- ☐ §8.10 Два ролика DarkSoft — до сих пор нет текстом.

**Починено по итогам боевого прогона 21–22.07 (см. `docs/QA-run-2026-07-21-results.md`,
`docs/BUGS-2026-07-21-proxy-import.md`):**
- ☑ Мёртвый ключ OpenAI больше не деградирует в шаблон: фатальные ошибки (401/403/404/квота)
  останавливают задачу, временные (сеть/5xx/rate limit) по-прежнему дают шаблон на один коммент.
- ☑ Шаблон выбирался по `promptIndex` — все аккаунты писали дословно одинаковый текст.
  Теперь случайно и с оглядкой на уже отправленное в задаче.
- ☑ Массовый импорт прокси: заголовки схем `http:`/`socks5:`, живость по реальному выходу
  (статус `bad` вместо ложного «ok»), `geoSource` сохраняется, «проверить все» обновляет гео,
  ссылки ротации не считаются ошибкой.
- ☑ §3.6 Переписку с лидом видно из CRM — читается из Telegram аккаунтом-владельцем.
- ☑ §3.3 В менеджере виден срок спамблока; 20 зависших без срока аккаунтов долечены.
- ☑ §9 Второй вариант первого сообщения терялся (`\w` не покрывает кириллицу) — вся рассылка
  уходила одним текстом. Варианты чередуются, в мейлинге видно их число до запуска.
- ☑ §3.9 Пять причин FloodWait в парсере участников: двойной `GetParticipants`, чат канала
  тремя способами, цели без чата пропускаются, FloodWait останавливает поток аккаунта,
  нижняя граница паузы вступления 60с.
- ☑ Старт API не ждёт опроса прокси; `unhandledRejection`/`uncaughtException` больше не молчат.

**Ждёт живых сценариев (главный риск, см. Фазу 1):**
- ☐ ≥5 тест-профилей в разных состояниях (FloodWait/карантин/invalid/горячий лид). Вся state machine,
  локи и гейты покрыты юнит-тестами, но на реальных сбоях не проверялись ни разу.

---

## MR-290 (01–02.09.2026) — БД продакшен-уровня: связи, типы, секреты

> Задача была поставлена одной фразой: «привести всю БД в порядок, особенно убрать json».
> Ниже — что за этим стояло на самом деле, пункт за пунктом. Всё проверено на ЖИВОЙ базе в
> откатываемых транзакциях: каждая миграция применялась к проду внутри `begin; … rollback;`,
> так что цифры ниже — не оценка, а факт.

**Связи (было: поиск по id вместо внешнего ключа):**
- ☑ 33 внешних ключа + 13 индексов там, где связь была «по договорённости»
- ☑ Найдено и описано в боевых данных: 370 записей журнала кошелька из 1187 и 95 платежей
  из 151 ссылались на несуществующих владельцев; профилю `usr_3947b0add427` был выдан
  аккаунт `acc_9` и группа `grp_1`, которых нет
- ☑ `accounts_meta.proxy` (текст) → `proxy_id` со ссылкой на `proxies`; 55 аккаунтов
  ходили мимо прокси напрямую с адреса сервера и об этом никто не знал
- ☑ Состав групп, выдачи субам, роли, модули кампаний, состав подписки в журнале —
  строками вместо массивов; массивов `text[]` в схеме после сноса не остаётся ни одного

**Json (было: 23 колонки, из них 16 — обычные записи в одной ячейке):**
- ☑ Карточка канала (13 полей у всех 85 каналов), цель, агент, режим отдыха аккаунта,
  распорядок дня, каталоги цен, права роли (555 правил) — колонками и строками
- ☑ Граница проведена явно и закреплена тестом: настройки модуля, тело отложенной
  кампании и ответы внешних систем остаются json — их форма принадлежит не базе

**Типы:**
- ☑ 30 колонок времени `bigint` → `timestamptz`; ноль как «никогда» стал NULL
- ☑ Найдена и исправлена одна и та же величина, записанная двумя способами
  (`recoveryPerHour` и `recoveryEveryMs`)

**Секреты и доступы:**
- ☑ Пароли 2FA (47 из 63 лежали открытым текстом), пароли прокси (101), файлы сессий —
  шифруются ключом из окружения; в базу ключ не попадает
- ☑ RLS и права на все новые таблицы; функции-триггеры лишены прав `public`

**Задачи и счётчики — в базе:**
- ☑ Задачи модулей, журнал, история, результаты, статистика по аккаунтам
- ☑ Суточные потолки и кэш доверия: таблицы существовали и были ПУСТЫ — код работал мимо

**Осталось следующим выпуском (подготовлено, `supabase/next-release/`):**
- ☐ Снос 16 старых колонок — после того, как выпуск отработает сутки
- ☐ Снос открытых паролей — после прогона скриптов переноса на проде
