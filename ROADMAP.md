# ROADMAP — AI-платформа управления Telegram-профилями

> ⚠️ **ПИВОТ В ПРОДАКШЕН (созвон 27.07).** От «инструмента» к **продаваемому SaaS**: биллинг
> (токены→доллары + estimate), перенос всего в **Supabase**, достройка **админки** (юзеры/цены/
> доступы из БД), **API/MCP для «мозгов»**, переделка **лендинга** + вкладка **Learning**. Дедлайн
> деплоя — сегодня; план — 3 недели; чек-ин — среда. План: **[docs/TASKS-2026-07-27.md](./docs/TASKS-2026-07-27.md)**;
> требования: **[TZ §10](./docs/TZ-2026-07-10.md#10-дополнения-от-27072026)**; разбор:
> **[docs/meeting-2026-07-27.md](./docs/meeting-2026-07-27.md)**. Обязательный минимум недели 1 —
> админка · цены · управление аккаунтами · всё в БД.

> ⚠️ **РАЗВОРОТ АРХИТЕКТУРЫ (созвон 22.07).** Иерархия Цель→Кампания→Задача переделана,
> добавлены Агенты, поведение аккаунтов (усталость/распорядок), балансы, лендинг, админка.
> Актуальный план работ: **[docs/TASKS-2026-07-22.md](./docs/TASKS-2026-07-22.md)** (пошагово,
> для второго агента). Архитектура: **[docs/SPEC-2026-07-22-иерархия-балансы.md](./docs/SPEC-2026-07-22-иерархия-балансы.md)**.
> Разделы ниже (по ТЗ 10.07) остаются в силе по модулям, но иерархия целей/кампаний —
> по новой спеке.

> **Единый роадмап.** Часть I — план работ по ТЗ от 10.07.2026 (куда идём, разбито на
> 2 исполнителей и фазы). Часть II — текущий статус (что уже работает и закрыто).
> Источник истины по ТЗ: [docs/TZ-2026-07-10.md](./docs/TZ-2026-07-10.md).
> Файл живой — статусы обновляем прямо здесь. Ссылки `§3.2` — на разделы ТЗ.

## Как пользоваться

- **Один роадмап, две дорожки (lane A / lane B)** — разделение по владению кодом, чтобы два
  человека не редактировали одни файлы одновременно.
- **Claude** читает `docs/TZ-2026-07-10.md` + этот файл перед задачей и обновляет статус пункта
  после выполнения (правило в `CLAUDE.md`).
- **Статусы:** `☐` не начато · `◐` в работе · `☑` готово · `🔒` заблокировано открытым вопросом (§6 ТЗ).
- Каждая задача — ветка `feat/<lane>-<имя>`, PR в `main`.

## Дорожки и владение

| Lane | Кто | Зона | Основные файлы |
|---|---|---|---|
| **A — Ядро** | Разработчик A | Профили, статусы, локи/lease, прогрев, задачи/оркестрация, прокси-модель | `accountLocks.js`, `accountsMeta.js`, `protection.js`, `accountRunner.js`, `registry.js`, `taskStore.js`, `automation/scheduler.js`, `AccountsPage.tsx`, `AccountPicker`, `AccountManagementModal` |
| **B — Кампании/UX** | Разработчик B | Цели/KB/CRM/нейрочатинг, нейрокомментинг, выбор каналов, парсер, логи, Help Center | `neuroCommenting/*`, `workers.js` (comment/chat/parser), `LogsPanel.tsx`, `HelpCenterDrawer.tsx`, `helpDocs.ts`, `ChannelParserModule.tsx`, `ParticipantsParserModule.tsx`, `NeuroCommentingModule`, `NeuroDialogsModule` |

**Правило:** `server/modules/workers.js` — общий. Lane A трогает воркеры оркестрации/прогрева/статистики,
Lane B — comment/chat/parser. Чужие файлы — только через контракт Фазы 0.

---

# Часть I. План по ТЗ (что делаем)

## 🎯 MVP — сдаём ПЕРВЫМ (можно тестить и продавать) `(дедлайн: неделя)`

> Самое первое, что сдаём. Смысл: **поставил цель → поставил задачу → всё в дашборде работает как задачи →
> один модуль реально отрабатывает** на прогретых ботах. Готовый продаваемый срез для демо/теста/продажи.
> Остальной скоуп (RBAC, Мейлинг, резолв, DarkTelegram-паритет) — ПОСЛЕ MVP.

**Что входит в MVP:**
- ☑ **A** Боты запускаются (подключение + вступление). §3.3 _(**проверено вживую 15.07**: реальные аккаунты подключаются по GramJS и вступают в группу/канал по инвайту; статусы/локи/конфликты работают. Прогрев live — по мере надобности)_
- ☑ **A** Движок задач: запуск = задача (ID/статус/прогресс), стоп/рестарт, инициатор, аудит. §3.9 _(запуск=задача с ID/статусом/прогрессом, стоп/рестарт, инициатор, аудит; **пауза/продолжение готовы** — `pauseWorker`+control-флаги в `taskStore`, гонка сохранения покрыта тестом)_
- ☑ **B** Экран **«Цели»**: создать цель (название/действие/этапы/критерий). §3.6 _(бэкенд `/api/goals` + тесты + страница `GoalsPage` в меню; live-прогон CRUD — за тобой)_
- ☑ **B** Дашборд **«Задачи»**: все задачи по всем модулям со статусом/прогрессом/целью + стоп/рестарт (`/panel/tasks`, агрегат `/api/modules/tasks`, авто-обновление 5с). §3.9
- ☑ **B** **1-й модуль сквозь поток** (нейрокомментинг/нейрочаттинг). §3.5/§4 _(выбор цели → `task.goalId` → **генерация к цели с базой знаний** → видно в дашборде. **Live 15.07 — ОБА модуля реально сработали:** нейрочаттинг отправил ИИ-ответ к цели в группу; **нейрокомментинг** вступил в канал + группу обсуждения и **опубликовал ИИ-комментарий под постом** («Да, мы тут! …будем рады обсудить!», status sent). Полный цикл цель→задача→модуль→реальное действие в Telegram)_
- ☑ **B** **2-й модуль = Нейрочаттинг-к-цели** (AI Sales Agent). §3.6 _(тот же поток: селект цели в запуске + `runNeuroChatting` генерит к цели с базой знаний; слои промпта: глоб.ограничения + стиль + цель+KB)_
- ☑ **A/B** Дашборд **видимый** (главный экран = задачи + прогресс). §8.8 _(«Дашборд задач» с воронкой Цели→Задачи→Модули + фильтры + режим «По целям»)_

**DoD MVP:** оператор ставит цель → создаёт задачу → выбирает модуль + прогретые аккаунты → запускает →
видит в дашборде статус/прогресс/результат/логи. Это можно показать клиенту и продать.

**Решено (14.07):** делаем оба, **приоритет — Нейрокомментинг** (быстрее к демо), Нейрочаттинг-к-цели — сразу после.

---

## Фаза 0 — Фундамент и контракты `(совместно)`

> «Архитектура меняется — сделать правильно». Фиксируем интерфейсы до параллельной работы.

- ☐ **A+B** Аудит текущего кода/схемы данных (см. Часть II): что есть, что переписываем → оценки в часах.
- ☑ **A** State machine профиля: `Active/Warming/Pause/FloodWait/Quarantine/SpamBlock/Reauth/Invalid` + переходы и разрешённые действия. §3.3, §4 _(код `server/lib/accountStatus.js` + юнит-тесты + контракт [docs/CONTRACT-account-state-machine.md](./docs/CONTRACT-account-state-machine.md): таблица переходов, временные статусы, авто-возврат)_
- ☑ **A** Контракт локов/lease: `lock(accountId,taskId)`, `lease(channelId,botId,ttl)`, приоритет прогрева. §3.9, §4 _(код `accountLocks.js`/`channelLease.js` + контракт [docs/CONTRACT-locks-lease.md](./docs/CONTRACT-locks-lease.md): API, самолечение, инварианты)_
- ☑ **A+B** Схема сущностей: `Profile/Account/Proxy/Task/Channel/Goal/KnowledgeBase/Lead` + граница «общий слой ↔ Telegram-специфика» (адаптер, §3.10). _(Goal/KB/Lead — [ARCH-goals-crm](./docs/ARCH-goals-crm.md); Account — [state-machine](./docs/CONTRACT-account-state-machine.md); Proxy/Task/Channel + карта связей + **граница адаптера** — [ARCH-entities.md](./docs/ARCH-entities.md))_
- ☑ **A+B** Формат единого лога/аудита: `action, module, profile, initiator, time, reason/code, affectedScope`. §3.1, §5. _(код `server/lib/auditLog.js` + контракт [docs/CONTRACT-audit-log.md](./docs/CONTRACT-audit-log.md): формат записи, словари action/initiator, ротация, инвариант append-only)_
- ☑ **B** Терминология: «Профиль/Аккаунт», «Логи» (не «История»), «Шаблоны поиска». §3.1/3.2/3.8. _(единый экран «Логи», «Шаблоны поиска» в парсере, глоссарий в CLAUDE.md)_
- ☑ **A+B** Документ Goals/KB/Leads ([docs/ARCH-goals-crm.md](./docs/ARCH-goals-crm.md)). §3.6 (P0). _(схемы Goal/KB/Lead, воронка, связка `Task.goalId`+`goalContext`, открытые вопросы §6)_

**DoD:** согласованы схемы и API-контракты — дальше дорожки идут параллельно без пересечения файлов.

## Фаза 1 — P0: Безопасность профилей + контракт целей

**Lane A — профили и статусы**
- ◐ State machine статусов; развести `Pause/FloodWait/Quarantine/SpamBlock` (причина/длительность/действия). §3.3 _(модуль `accountStatus.js` + аудит + `setAccountStatus`; переходы в `accountRunner` (quarantine/spamblock/invalid/reauth) идут через state machine с аудитом; reconciler авто-выхода на старте. `floodwait`-статус с длительностью в хот-пути (пауза→возврат/карантин) + skip floodwait/pause; ручной `pause`/возврат оператором из UI (кнопка + аудируемый роут). Остаётся: ≥5 тест-профилей (нужны реальные аккаунты в разных статусах))_
- ☑ Настраиваемое число попыток до карантина (деф. 3) + длительность от Telegram. §3.3 _(поле «FloodWait до карантина» (деф.3, min 1) в модалке ИИ-безопасности + длительность из `FLOOD_WAIT_N` Telegram в `handleFlood`)_
- ☑ Прогрев полностью блокирует профиль для ручного/модульного назначения. §3.3 _(guard на границе запуска: `canModuleUseAccount`/`assertAccountsAssignable` (409 при назначении); **UI-подсказка есть** — в AccountPicker warming-аккаунт disabled + метка «в прогреве» + toast «Профиль недоступен — дождитесь возврата в активные»)_
- ☑ Авто-стоп при падении trust score → прогрев → авто-возврат. §3.3 _(решение 15.07: полную формулу пока не строим — **жёсткий авто-стоп** по твёрдым сигналам (spamblock/quarantine/3×FloodWait/invalid/reauth — почти есть в `accountRunner`) + safety-дефолты (`server/lib/safetyLimits.js`). **Trust score (15.07):** `server/lib/trustScore.js` — взвешенный балл 0–100 строго по решению §6 (FloodWait 24ч 40% · история spamblock/quarantine 25% · чистые действия 15% · возраст/GGR 20%), `computeTrustScore`/`extractTrustSignals`/`trustRecommendation`, пороги <40/40–70/>70; сигналы берутся из реальной активности+статуса в `buildAccountStats` (`stats.trust`); карточка «Здоровье» → «Trust score (§6)» (гейдж + чип полосы + разбивка по компонентам + рекомендация); юнит-тесты (trustScore.test.js, 7); проверено вживую (Ілья score 73 → «В пул»). **Авто-стоп по trust (15.07):** `server/lib/trustCache.js` — балл кэшируется при расчёте stats; `assertAccountsAssignable` не пускает аккаунты с trust<40 в боевые модули (neuro-commenting/chatting/dialogs/mass-react/mailing) → 409 «отправьте на прогрев», прогрев/парсинг/просмотр не гейтятся (recovery-путь открыт); проверено вживую (seed trust 30 → 409, warming тем же аккаунтом → 200). В списке аккаунтов low/mid-trust помечены чипом (`trustScore`/`trustBand` в `tgListAccounts`). Кэш обновляется при просмотре карточки И **фоновым планировщиком каждые 10 мин** (`refreshAllTrustCache` в `index.js` — без сети, по мете+активности из логов; проверено: при старте «обновлён кэш trust: N акк.», гейт актуален без ручного открытия карточек). **Enforcement суточных лимитов (15.07):** `server/lib/dailyActions.js` — счётчик действий на аккаунт по дням (сброс в полночь), воркеры инкрементят по факту (`incAction`) и **пропускают аккаунт при достижении дневного потолка** (`limitReached` guard — skip+лог). Покрыты все 4 типа §6: comments (нейрокомментинг), dm (нейродиалоги — guard+инкремент, лог 1×/аккаунт т.к. модуль-ответчик простаивает, а не завершается), reactions (масс-реакции), joins (нейрокомментинг/чаттинг/масс-реакции/масс-просмотр — инкремент при `membership.status==='joined'`; парсинг исключён); юнит-тесты (dailyActions.test.js, 4) + **проверено вживую 15.07**: аккаунт с counts.comments=40 → нейрокомментинг пропускает его (commentsSent=0, лог «Суточный лимит комментариев достигнут (§6)»); mass-react с reactions=200 → пропуск. **UI-видимость:** (1) карточка аккаунта → вкладка «Здоровье» → блок «Суточные лимиты (§6)» показывает used/cap по каждому действию с цветным прогрессом и меткой «· лимит» (роут `GET /api/tg/accounts/:id/daily` → `dailySummary`); (2) **в списке аккаунтов** — чип «§6 лимит: …» на строке throttled-аккаунта с тултипом (роут `GET /api/tg/accounts/daily-all` → `dailySummaryAll`, опрос раз в 30с); проверено в браузере (Ілья: чип «комментарии» при comments=40/40). Решения зафиксированы: [docs/DECISIONS-06](./docs/DECISIONS-06-open-questions.md), бизнес-логика в Help Center «Лимиты безопасности»)_
- ☑ Исключение «горячий лид»: статус не меняем, диалог продолжается. §3.3/§4 _(guard `assertNoHotLeadConflict` в `server/leads.js`: аккаунт с лидом в статусе `hot` нельзя забрать в НЕ-диалоговый модуль (mass-*/parsing/warming…) — 409; диалоговые (neuro-chatting/neuro-dialogs) пропускаются. Подключён в `/api/modules/:key/tasks` + кампании; юнит-тесты; проверено API (mass-react→409, neuro-chatting→200))_
- ☑ Безопасный выбор профилей: только свободные/разрешённые; запрет незаметного захвата. §3.2/§5.1 _(бэкенд-guard `assertAccountsAssignable` + **UI**: в `AccountPicker` непрогретые/в карантине/на паузе/занятые не выбираются (disabled + понятная причина), «Добавить все» их пропускает)_
- ☑ Подтверждение переноса профиля + аудит инициатора/действия. §3.2/§4 _(перенос роли/проекта: подтверждение с последствиями (confirm) + запись `account.transfer` в аудит с инициатором)_
- ☑ Двухэтапное подтверждение опасных массовых действий. §3.2 _(bulk «в корзину» и «Отключить» ≥2 — подтверждение с последствиями)_
- ☑ Безопасность API управления: слушать только `127.0.0.1` по умолчанию (`API_HOST`) + аутентификация/роли. §8.1 _(бинд на localhost по умолчанию + **логин по e-mail/паролю** (scrypt) + **RBAC-роли** + **серверный enforcement** `can()` на роутах модулей (`accessGuard.js`, 403). Продакшн-шаг — подписанный токен сессии вместо `X-User-Id` (см. CONTRACT-rbac.md §8))_
- ☐ ≥5 тестовых профилей (блокировки/FloodWait/карантин/лид). §3.3

**Lane B — логи (низкий риск) + контракт целей**
- ☑ «История» → «Логи»; единый экран без дублей. §3.1 _(единый журнал «Логи» `/panel/logs` (аудит-трейл, инициатор+причина, фильтры, «Потоки») + переименованы per-модульные секции: «История»→«Логи» (LiveModule), «История комментариев»→«Логи комментариев», «История просмотров»→«Логи просмотров», «История реакций»→«Логи реакций», страница «История парсинга»→«Логи парсинга»)_
- ☑ Сущности `Goal/KnowledgeBase/Lead` (бэкенд-скелет) + страница «Цели». §3.6 _(Goal + KnowledgeBase + Lead CRUD + страницы «Цели»/«CRM·Лиды»; файлы/OCR KB — §6)_
- ☑ Расширить лог-запись полями из Фазы 0. §3.1 _(`taskStore.appendLog` авто-добавляет `module`+`initiator` (из контекста задачи) в КАЖДУЮ запись лога + опц. `code`/`reason`; тип `LogEntry` расширен. Единый аудит-лог уже несёт полный формат ([CONTRACT-audit-log.md](./docs/CONTRACT-audit-log.md)))_

**DoD:** §5.1–5.2 (нельзя назначить непрогретый/забрать чужой; всё в логах с инициатором); есть контракт целей.

## Фаза 2 — P1: UX, каналы и задачи

**Lane A — движок задач**
- ☑ Сущность `Task` на каждый запуск (ID/модуль/цель/профили/каналы/время/статус/прогресс). §3.9 _(есть id/модуль/статус/прогресс/настройки, `initiator` + аудит `task.start`/`task.stop`. **Сверка 21.07:** `goalId` больше не «когда появятся» — проставляется на запуске (`modules/routes.js`) и сохраняется в DTO (`taskStore.js`), рядом добавлен `campaignId` (§0 Цель→Кампания→Задача). Каналы задачи живут в `settings.targets`/`channels` — отдельного `channelIds` не заводили осознанно, чтобы не плодить второй источник правды)_
- ☑ Трекер задач: просмотр/пауза/продолжение/отмена/перезапуск/ошибки/результат. §3.9 _(отмена=stop, перезапуск=`/restart`, **пауза `/pause` + продолжение `/resume`** (статус `paused`, прогресс сохранён, resume перезахватывает локи и продолжает с места), просмотр/ошибки/результат — в дашборде+логах; кнопки в «Дашборде задач». Попутно устранена гонка: `saveTask` больше не затирает выставленные извне флаги stop/pause (надёжнее и стоп). **Проверено вживую 15.07:** прогрев → пауза (done 2, status paused) → resume (продолжил, done 3))_
- ☑ Запрет конфликтующих задач на профиль; приоритет прогрева. §3.9/§4 _(локи + статус-гейт + распределение пула в кампании — не дают двум модулям взять один аккаунт)_
- ☑ Планировщик/оркестрация: перераспределение профилей между модулями под одну цель. §3.9 _(**проверено вживую 15.07:** 1 цель → нейрокомментинг (канал) + нейрочаттинг (группа) на 4 аккаунтах round-robin (2/2, без конфликтов) → ОБА модуля опубликовали реальные ИИ-действия к цели. `buildCampaignPlan` прокидывает лимиты/задержки в модули (+override). «Кампания»: `/api/campaigns/launch` — одна цель → N модулей на общем пуле (round-robin, непересекающиеся подмножества) + **расписание вкл/выкл**: `campaignSchedules.js` (store + тик каждую минуту + isDue one-time/daily) + роуты `/api/campaigns/schedules` + UI на странице «Кампания» (дата/время + повтор + список с тумблером вкл/выкл и удалением); инициатор запусков по расписанию — `scheduler`; юнит-тесты)_
- ☑ Схема каналов + lease «один канал — один бот». _(примитив `server/lib/channelLease.js` + юнит-тесты, по контракту locks-lease §4. **Сверка 21.07:** lease не «примитив в вакууме» — реально применяется в `channelStats.js` (`acquireChannelLease(channel.id, acc, 'stats', 60_000)` перед проходом и `releaseChannelLease` после), схема Channel и оркестратор статистики закрыты в Фазе 3)_

**Lane B — экраны и каналы**
- ☑ Help Center: правая панель + якоря по «?». §3.1 _(«?» открывают нужную статью (`setHelpTopic`+`findHelpDoc`) + панель **не блокирующая** — основной интерфейс доступен)_
- ☑ Логи: список + фильтр (действие/инициатор) + **детальная вью записи** (клик → модалка) + **режим «Потоки»** — multi-select до 3 действий, параллельные колонки для сравнения. §3.1
- ☑ Нейрокомментинг UI: явные режимы, min/avg/max время, связка лимитов, окно постов, распределение типов = 100%. §3.5 _(расчётное **min–max время** + **окно постов** (3/5/10/20) + **распределение типов %** (веса на тип, нормируются при запуске, выбор типа по весу в воркере); **связка лимитов** (предупреждение о противоречиях макс/аккаунты/мин-на-акк); **явные режимы (15.07):** 3 подписанных режима «Режим комментирования» — Случайный (один пост из окна) / По ключевым словам (фильтр по keywords) / Все посты (комментируем всех кандидатов), каждый обрабатывается в `pickCommentCandidates`+воркере; описаны в Help Center «Нейрокомментинг»)_
- ☑ Выбор каналов: объединённый список, карточка, фильтры, multi-select, общий компонент. §3.7 _(база каналов `/panel/channels` + в «Кампании» цели из **3 источников**: ручной ввод + база каналов (multi-select) + **папки** через общий `FolderPicker` (загрузка/сохранение/управление/валидация) — используется во всех секциях с целями)_
- ☑ Парсер: «Шаблоны поиска», AND-пересечение, дедуп в общей базе, «Открыть в Telegram», прозрачная формула рейтинга. §3.8 _(дедуп в базу + «Открыть в Telegram» + прозрачная формула рейтинга + **AND-пересечение** (тумблер «Пересечение (AND)» при 2+ ключах: канал должен совпасть со ВСЕМИ ключевыми словами; трекинг совпадений в воркере + фильтр по пересечению). **Проверено вживую 15.07:** «crypto»+«news» → union 15 → пересечение 1 (@cryptowallet_news_cis))_

**DoD:** §5.4–5.6 (каналы из папок выбираются списком; парсер без дублей и с AND; запуск = задача со статусом).

## Фаза 3 — P2: Статистика каналов + инфраструктура

**Lane A**
- ☑ Модуль статистики каналов: выбор аккаунтов/каналов/задержек, пары «канал–бот», 2 прохода, lease, «обновить сейчас», дата/источник. §3.9 _(база каналов `/api/channels` с дедупом + страница «Каналы» + «Обновить сейчас» по lease + дата/источник; период (день / час-если-бот-в-группе) + авто-расписание; интеграция парсер→база (`upsertMany`); **2 прохода** (`refreshOneChannel`: проход 1 — подписчики, проход 2 — активность/свежесть: `activityFromDates` → метка `high/medium/low/stale` + `lastPostAt`, показываются на карточке канала; 2-й проход некритичен). Юнит-тесты)_
- ◐ Прокси/SIM-модель `аккаунт→страна→SIM→proxy→device/IMEI→статус`; прокси в Менеджере профилей. §3.2/3.4 _(разблокировано 14.07: своя ферма + докупаемые. **Готово:** сущность `Proxy` (`server/proxies.js`: тип static/mobile/farm, scheme, host/port/auth, страна, статус) + CRUD `/api/proxies` + аудит + страница `/panel/proxies` (CRUD + **назначение на аккаунты** через `account.proxy`=URL) + юнит-тесты. **Авто-проверка живости (15.07, §6):** `checkProxyLiveness`/`checkAllProxies` (TCP-пинг host:port) + статус ok/dead + lastCheckAt; планировщик в `index.js` (старт + каждые 30 мин) + роуты `POST /api/proxies/check-all`, `POST /:id/check`; правило «1 прокси = 1 аккаунт» — `sharedProxies`/`GET /api/proxies/shared` (нарушения 1:1); юнит-тесты (usage-map + tcpPing); Help Center «Прокси: правила». **Далее:** цепочка SIM/device/IMEI (глубже инфра))_
- ☐ 🔒 Отчёт static/mobile/своя ферма + пилот (§6).

**Lane B**
- ☑ CRM: лид/статус/ответственный/история/результат; приоритет ответившему; лимиты по активным диалогам. §3.6 _(`/api/leads` + воронка cold→answered→hot→target→closed + страница CRM; **приоритет ответившему** (`sortLeadsByPriority`: hot>answered>target>cold>closed — лиды сортируются в CRM); **лимит активных диалогов** (`assertActiveDialogLimit` + `activeLeadCount`: при `maxActiveDialogs`>0 нельзя грузить профиль в диалоговый модуль сверх лимита активных лидов — 409); юнит-тесты. Проверено API: лимит 2 при 2 диалогах → 409, лимит 5 → 200)_
- ☑ Нейрочатинг к цели из списка; разделить ограничения/KB/цель/стиль. §3.6 _(цель из списка + цель+KB в генерации + слои промпта (глоб.ограничения+стиль+цель+KB); **лимиты активных диалогов** (`assertActiveDialogLimit`/`activeLeadCount`); **приоритет ответившему (15.07)** — воркер сортирует диалоги захода по CRM-приоритету лида (`sortDialogsByLeadPriority`: hot>answered>target>cold, матч по username/id), лимит ответов тратится на важных первыми; юнит-тесты)_
- ☑ Аналитика: отправлено/ответы/активные диалоги/горячие лиды/конверсия. §3.6 _(страница `/panel/analytics`: метрики + воронка лидов + разбивка по целям; из данных задач/лидов)_
- ☑ Тональность/семантика в нейрокомментинге (фильтр/vector/оба). §3.5 _(фильтр стоп-слов (нежелательные темы/тон) + **семантический фильтр к цели (15.07)**: `server/lib/semantic.js` — embedding цели+поста (OpenAI text-embedding-3-small) + косинусная близость, коммент только если ≥ порога (деф.0.2); опция `semanticFilter` (тумблер в UI нейрокомментинга); чистые cosine/rank — юнит-тесты; **проверено вживую**: релевантный пост sim 0.52, офтоп 0.09 → порог 0.2 режет офтоп. Help Center «Нейрокомментинг»)_

**DoD:** §5.7–5.8 (кампания к цели и KB, лид не теряется; статистика — единственный исполнитель на канал + дата актуальности).

## Фаза 4 — Later: Мультиплатформа

- ☐ **A+B** Переключатель Telegram/Instagram в архитектуре; Telegram-специфика через адаптер. §3.10
- ☐ **B** 🔒 Исследование ComfyUI (место в пайплайне). §3.10

## Новый скоуп 14.07.2026 (созвон) — требует приоритизации `(§8 ТЗ)`

> Из [docs/TZ §8](./docs/TZ-2026-07-10.md). Пока НЕ распределено по фазам — ждёт ответов участников
> по ❓. Дедлайн от заказчика: **следующая неделя** — базовый цикл (см. приоритет ниже).

**🎯 Приоритет (дедлайн неделя):**
- ☑ **A** Сквозной цикл: **запустить ботов + 1 «задача для модуля»** (постановка цели→задача→выполнение). §8.9 _(**проверено вживую 15.07** на реальных аккаунтах: цель «ТЕСТ» → задача нейрочаттинга → аккаунты подключились (GramJS) + вступили в группу по инвайту + сгенерировали ответ **к цели с базой знаний** и **реально отправили** его в группу (Дмитрій Руб, статус `sent`, 0 floodwait) → видно в дашборде/логах. По ходу найден и починен баг «тихого нуля» (`resolveTotalTarget`: при min=0 seed мог дать цель 0 → задача молча простаивала))_

**Lane A — ядро/инфра:**
- ☑ RBAC: 1 админ→N юзеров, роль «Модератор», доступ по модулям/парсерам/блокам, права на папку/таймер/шаблон/канал (2 чекбокса), выделение снятого доступа. §8.1 _(матрица прав определена заказчиком 14.07 — снят 🔒. Страница ролей `/panel/roles` (матрица модули×блоки + ресурсы, 2 чекбокса allow/deny + подсветка снятого доступа, шаблон «Модератор»); сущность **«Пользователь»** (`server/users.js`, scrypt-пароль, логин, назначение роли на `/panel/users`); **enforcement** `can()` в UI — сайдбар/модули под роль + гейт прямого URL; сид тестового юзера; аудит `role.*`/`user.*`; юнит-тесты; [docs/CONTRACT-rbac.md](./docs/CONTRACT-rbac.md). Проверено в браузере: модератор видит 4 модуля, парсеры/админ-страницы скрыты, прямой заход → «Нет доступа»)_
- ☑ Карточка сотрудника + учёт рабочего времени. §8.1 _(карточка на `/panel/users`: имя/e-mail/роль/активность + **учёт рабочего времени** (`server/workLog.js`: clock-in при входе / clock-out при выходе, «в сети» + сегодня/7 дней, `GET /api/users/worktime`); юнит-тесты. Серверный **enforcement** `can()` на роутах модулей (`accessGuard.js`, 403 при отказе) — проверено)_
- ☑ Прогрев: 3 уровня — **Быстрый** (2 дня) / **Нормальный** (3–7) / **Стандартный** (7–14). §8.2 _(названия + пропорции по заказчику; `warmingPace(level)`: темп (mul) + действий/день (40/20/10) + **веса действий** (просмотр 40% · реакции 20% · чтение/ЛС 20% · вступления 10% · подписки/ping 10%), выбор действия — `pickWeightedKey` (взвешенно), `inActiveWindow` (дневное окно 9–23); юнит-тесты; бизнес-логика в Help Center «Политика прогрева». **Реальные реакции/вступления (15.07):** `runWarming` при kind==='react'/'join' выполняет настоящие действия (`sendReaction` по свежему посту публичного канала / `joinTargetOrSkip` в публичный канал) под суточными лимитами §6 (при достижении потолка — деградация в просмотр); расход виден в карточке «Здоровье». Multi-day ramp-up (activity растёт по дням) + окна активности 9–23 — есть в `warmingPace`/`inActiveWindow`; полноценный multi-day scheduler (пауза на ночь как отдельные запуски) — при live)_
- ☐ Резолв номеров → Telegram (модуль + сервер парсинга). §8.5 🔒(инфра/ToS) ❓
- ☑ Регионы Европа+Украина в GEO-модели. §8.3 _(единая GEO-модель `src/shared/config/geo.ts`: 14 стран с регионами Европа/СНГ + фильтр по региону (`reg:europe`/`reg:cis`) и стране в менеджере и пикере аккаунтов; `countryFromPhone` расширен европейскими префиксами (gb/fr/es/it/nl/cz/ro/lt/lv); юнит-тест мэппинга. Проверено в браузере)_

**Lane B — модули/UX (паритет DarkTelegram):**
- ☑ **Мейлинг / AI Mailing** (рассылка в Telegram по номерам). §8.4 _(стек решён по видео заказчика — **Node-модуль в этом проекте** (GramJS), Yii2/PHP не трогаем. **Каркас готов:** модуль `mailing` в движке (MODULE_DEFS+WORKERS+MODULE_LABELS), страница `/panel/mailing` (аккаунты + номера + текст + цель + лимиты/задержки, предупреждение о риске), задача создаётся и видна в дашборде. **Реальная отправка готова (15.07, по явному «да» заказчика):** `runMailing` резолвит каждый номер через `contacts.ImportContacts` → `sendMessage` ЛС (шаблон или ИИ к цели), НЕ «слепая» — предохранители §6: только аккаунты с trust>70 (gate через `buildAccountStats`), суточный лимит ЛС (`limitReached('dm')`/`incAction('dm')`, дефолт 25/акк), паузы 90–300с, round-robin, номер не в Telegram → skip, FloodWait→карантин (`handleFlood`). Страница: badge «live», паузы 90–300с по умолчанию, чекбокс ИИ-текста, предупреждение о реальной отправке+риске. Опция отписки в тексте — на операторе)_
- ☐ Story Mentions (упоминания в сторис) — доработка масслукинга. §8.4
- ☑ Прохождение капчи групп + 3 варианта антиспама + авто-заход. §8.6 _(типы капч уточнены заказчиком. **Готово:** классификатор `server/lib/captcha.js` — распознаёт капчи групп-ботов (кнопка/математика/эмодзи/картинка/кастом/собственная-Telegram) и решает стратегию (**3 варианта:** `auto` простые / `operator` сложные / `skip`); solveMath + matchButton; юнит-тесты (7). **Граница ToS:** собственные проверки Telegram (SMS/reCAPTCHA) → всегда `operator`, не авто. **Live-хук готов (15.07):** `server/lib/captchaRunner.js` — после каждого нового вступления (`joinTargetOrSkip`, status==='joined') читает последние сообщения группы, детектит капчу (`looksLikeCaptcha` + бот из `CAPTCHA_BOTS`), строит план через классификатор и действует: `auto` — клик инлайн-кнопки (`message.click`) или ответ текстом; `operator` — лог warning (аккаунт ждёт ручного действия); `skip` — выход из группы (LeaveChannel). Чистые detect/plan — юнит-тесты (captchaRunner.test.js, 6); GramJS-вызовы в try/catch (не ломают вступление). Бизнес-логика — Help Center «Капча и антиспам». Реальный прогон против живого бота-капчи — при наличии такой группы)_
- ☑ Web-Telegram по аккаунтам. §8.7 _(модуль **«Обзор аккаунта»** `/panel/inbox` (есть в сайдбаре): вкладки «Диалоги» (смотреть/отвечать) + «Группы и каналы» (в каких состоит аккаунт) — реюз inbox-бэкенда `neuro-dialogs` + `listAccountChannels`. **Сверка 21.07 нашла расхождение и оно закрыто:** страница держала СВОЙ UI диалогов и отстала от `NeuroDialogsModule` — рендерила только `m.text` (превью картинок/видео бэкенд уже отдавал), поле ответа было однострочным `<input>` без панели форматирования. Разметка вынесена в общие `ConversationBubble` + `ReplyBox` (`src/features/conversation/`) и подключена на обоих экранах; копия удалена. Теперь правка едет сразу в оба)_
- ☑ Единый видимый дашборд «Цели→Задачи→Модули» + фильтры-воронка + преследование цели. §8.8 _(`/panel/tasks` → «Дашборд задач»: воронка-сводка (цели в работе / задачи / активные+модули / средний прогресс) + фильтры по цели/модулю/статусу + режим **«По целям»** (группировка задач по цели с агрегатным прогрессом к цели и чипами модулей). Проверено)_
- ◐ Ресёрч: тест DarkTelegram + YouTube (DarkTelegram, TG Master). §8.10 _(звірка паритету — [docs/RESEARCH-parity.md](./docs/RESEARCH-parity.md): мы сильнее по ИИ-слою; главная безопасная прогалина — **автопостинг** — **закрыта** (модуль `autoposting`: постинг в свои каналы, worker+страница `/panel/autoposting`+Help Center; проверено вживую — корректно требует прав админа: CHAT_ADMIN_REQUIRED). Прогалины с бан-риском (инвайтер/тегер) — по явному «да»; авторегер/клоннер — низкий приоритет. Точный список DarkSoft из роликов ждёт текстом)_

**❓ Не расшифровано / ждёт участников:** семантика 2 чекбоксов · 3 варианта капчи · лимиты Мейлинга · два ролика (нет у Claude).

## Новый скоуп 27.07.2026 (созвон) — ПРОДАКШЕН/КОММЕРЦИАЛИЗАЦИЯ `(§10 ТЗ)`

> Полный план — [docs/TASKS-2026-07-27.md](./docs/TASKS-2026-07-27.md). Требования — [TZ §10](./docs/TZ-2026-07-10.md#10-дополнения-от-27072026).
> Дедлайн деплоя — **сегодня**. Недели 2–3 — фокус модуль-за-модулем + мониторинг. Чек-ин — **среда**.

**🎯 Неделя 1 — обязательно 100% (§10.10):**
- ◐ **Supabase**: ☑ схема (14 таблиц) + миграция `server/data/`→Supabase + адаптер сторов
  (DATA_BACKEND=supabase, всё из БД); ☑ регистрация + подписанные сессии (SESSION_SECRET);
  🔴 OTP+капча (нужны SMTP-провайдер + ключи капчи), Storage сессий (нужен прогон на боевых). §10.2
- ☑ **Биллинг/токены**: «Потрачено» = действия+токены (было занижено) · estimate (стоимость+время) ·
  **себестоимость токена считается сама из модели** (modelPricing) + правится в админке · баланс
  (кнопки не блокируются→модалка · несъёмная лента · модалка на логине + чекбокс админам · при 0 «закончился»). §10.1
- ☑ **Админка**: карточка юзера (модули/токены/деньги в $/подписки/роль) · вкл/выкл модуль на юзера ·
  блок-внутри-модуля через роли · **2 цены + скидка −X% из админки** · **роли только из sudo-админки**
  (вкладка «Роли», назначение роли юзеру) · **вложенные юзеры** · **редактор наборов** · **мониторинг**
  (нагрузка + аккаунты активные/падают + почему). §10.4/§10.9
- ◐ **Управление аккаунтами** из админки — частично (мониторинг здоровья готов; свести менеджер — TODO). §10.10
- 🔴 **Деплой** + авто-деплой по пушу: CI (tsc+тесты+build) готов, deploy.yml есть; нужен хостинг + SESSION_SECRET.

**🔌 API/MCP для «мозгов» (§10.3):**
- ☑ API по закрытому ключу (Bearer): создать цель/кампанию/задачу — /api/v1.
- ☑ API/MCP «что умеет модуль» (capabilities + mcp) + POST /modules/:key/run + /estimate.

**Lane B — UX/лендинг/обучение:**
- ☑ **Анализ картинок**: vision-описание фото в нейродиалогах и нейрокомментинге + чекбокс в модуле +
  наценка «картинка ×N» из админки (tokenLedger.coinMultiplier). §10.5
- ☑ **Лендинг**: герой-карусель (выбор статичная/карусель `?hero=`) · 2 версии `?v=` (SEO) · воронка
  «на выгоду клиента» / «автоматический конвейер» · «Подробнее»→страница модуля (обогащена HELP_DOCS) ·
  калькулятор наборов · «комбайн» убран · отзывы-карусель + кейсы-переработка. Позиц. «одно слово» — 🔴 бренд. §10.6
- ◐ **Learning**: «Обучение» — вкладки (Обзор/Как работает/Пример/Риски/Советы), собирается из HELP_DOCS; скрины — TODO. §10.7

**Прочее:** ☑ имя **Murmex** во всём UI (домены — за бизнесом) · 🔴 SIM-безопасность (бизнес). §10.8/§10.9

**🔒 Ждёт бизнеса:** модель под капотом (цена авто-адаптируется под OPENAI_MODEL) · OTP-SMTP+капча-ключи ·
платёжка (Андрей) · хостинг+SESSION_SECRET для деплоя · «одно слово» позиционирования · SIM.

## Новый скоуп 29.07.2026 (созвон) — НАБЛЮДАЕМОСТЬ + ПРАВИЛЬНАЯ БД `(§11 ТЗ)`

> Полный план — [docs/TASKS-2026-07-29.md](./docs/TASKS-2026-07-29.md). Разбор — [meeting-2026-07-29.md](./docs/meeting-2026-07-29.md).
> Требования — [TZ §11](./docs/TZ-2026-07-10.md#11-дополнения-от-29072026).
> ⚠️ **Критический путь:** схема БД (§11.3) блокирует логи по юзеру (§11.1) — нельзя показать
> действия юзера, пока данные к юзеру не привязаны. Поэтому БД делается первой.

**Приоритет 1 — БД (§11.3):**
- ☐ `foreign key` на user ID во всех таблицах данных (кто/когда создал) — сейчас записи «висят в пустоте»
- ☐ Разделить `auth.users` (встроенная) и **`profiles`**; свою таблицу `users` не держать
- ☐ Типы юзеров отдельной таблицей: `name` UNIQUE + CHECK длины/паттерна на уровне БД; в профиле — `user_type_id`
- ☐ Права — join-таблицей `user_type_id` ↔ `module_id`; сущности остаются «чистыми»
- ☐ Зарегистрировать реальных юзеров в Supabase Auth (раздел пуст) + миграция текущих данных

**Приоритет 2 — наблюдаемость и безопасность (§11.1, §11.9):**
- ☐ **Логи любого юзера в админке**: все действия, вся активность, **с кем переписывается**; фильтры по дате/модулю/аккаунту
- ☐ **Блокировка юзера** (отдельно от «выключить») + **логирование IP** входа

**Приоритет 3 — деньги и UX (§11.4, §11.5, §11.2):**
- ☐ Баланс: **$ отдельно, токены отдельно** (не «токены в долларах»)
- ☐ Низкий баланс — **красным** («пополнить» красное, не зелёное)
- ☐ Периоды подписки **генерируемым списком + селект** (недели 1–4 / месяцы 1–6 / годы 1–5); скидка за **6 мес**
- ☐ Цена подписки за модуль — правится **и в админке, и в кабинете**

**Приоритет 4 — лендинг (§11.6, §11.7):**
- ☐ **Картинка/скриншот у каждого модуля** с подписями-сносками по блокам; продающий тон («за что $192»)
- ☐ Залогиненному — **аватар + имя + «Кабинет»** вместо «Войти»
- ☑ Клик по логотипу → лендинг (принято на созвоне)

**Приоритет 5 — API «мозгов» (§11.8, Илья):**
- ☐ Убрать генерацию ключа под пользователя; подключение **server→server** по TLS-сертификату/ключу
- ☐ Самостоятельный тест API Python-скриптами (проверять структуру ответа, а не факт ответа)
- ⏸ Чтение всех логов через API — пока не нужно

**⏸ Отложено:** агенты (характер/название), шаблоны и блок скидок-наборов на лендинге.

**🔒 Ждёт внешнего:** платёжка **VIVA vs Stripe** (Андрей) · ключи мерчанта · глубина хранения
логов переписки (приватность/объём) · живой прогон прогрева на 50 закупленных аккаунтах (Илья).

## Граф зависимостей

```
Фаза 0 ─┬─> A: статусы/локи ─> A: задачи ─> A: статистика каналов
        ├─> A: безопасный выбор ─> B: трекер задач (UI)
        ├─> B: Goal/KB/Lead ─> B: CRM/нейрочатинг ─> B: аналитика
        └─> B: логи/термины ─> B: Help Center / детальная вью
§6 (открытые вопросы) ─(блокируют)─> trust score, прокси/SIM, семантика, период статистики
```

Открытые вопросы (§6 ТЗ), гейтящие пункты `🔒`: trust score · прокси/SIM/регистрация · метрики/формула
рейтинга канала · период статистики · роли · safety-лимиты чата · семантический поиск · Instagram/ComfyUI.

## Инструменты команды

- **codebase-memory-mcp** — граф кода для навигации (ставит каждый у себя, см. `CLAUDE.md`).
- **Файловая память Claude** (`.claude/.../memory/`) — решения и контракты между дорожками.
- Верификация: `npx tsc --noEmit` + превью; бэкенд после правок воркеров/схем — перезапуск.

---

# Часть II. Текущий статус (что уже есть) — срез 2026-07-10, `main`

**Стек:** React 18 + TS + Vite 5 + Tailwind 3 · Node Express (ESM) + GramJS + cheerio · zustand, react-router v6.
Прогресс — polling. Данные модулей — JSON в `server/data/`. Секреты в `.gitignore`.

## ✅ Что работает (по модулям)

Все 12 модулей имеют реальный бэкенд-воркер (`workers.js`, карта `WORKERS`) — живые вызовы GramJS.

### Парсеры (протестированы на 5 живых аккаунтах)

| Модуль | Ключ | Статус | Что делает |
|---|---|---|---|
| Парсинг каналов | `parsing` | ✅ live | Поиск по ключам/окончаниям, фильтры, дедуп, ★-рейтинг, экспорт |
| Парсер групп | `parsing-groups` | ✅ live | То же для групп, шаблоны, ИИ-подсказки |
| Парсер пользователей | `parsing-users` | ✅ live | Участники + «кто писал» (роли), пересечение аудиторий |
| Парсер по сообщениям | `parsing-messages` | ✅ live | Юзеры по ключам в сообщениях, фильтр дат, активность |
| Парсер комментариев | `parsing-comments` | ✅ live | Юзеры из комментов, фильтры, лимиты |
| TGStat-парсер | `tgstat` | ✅ live | Каталог + расширенный поиск с фильтрами (cookies) |

### Модули инкубатора (бэкенд реальный; системный live-прогон — в бэклоге, B3)

| Модуль | Ключ | Бэкенд | Действия в TG |
|---|---|---|---|
| Нейрокомментинг | `neuro-commenting` | ✅ | join + `sendChannelComment` + OpenAI |
| Нейрочаттинг | `neuro-chatting` | ✅ | `sendMessage` в группах, контекст |
| НейроДиалоги | `neuro-dialogs` | ✅ | мониторинг ЛС + авто-ответы (ответы всем + пресеты) |
| Массовые реакции | `mass-react` | ✅ | `sendReaction` |
| Масслукинг | `mass-looking` | ✅ | `markStoriesRead` / просмотр постов |
| Прогрев | `warming` | ✅ | имитация активности |
| GGR-рейтинг | `ggr` | 🟡 БЕТА | оценка качества аккаунта |

### Инфраструктура

Менеджер аккаунтов (фильтры/прокси) · локи (1 аккаунт = 1 задача) · ИИ-защита (3 уровня) ·
задержки + FloodWait/карантин · папки целей с валидацией · экспорт CSV/Excel · история парсинга ·
Help Center (OpenAI `gpt-4o-mini` + fallback) · чёрный список · пресеты во всех модулях.

## ✅ Закрыто полностью

Богатый парсер каналов (бэк) · `ModuleTaskSettings` · `ChannelParserModule` · роутинг + прогон ·
TGStat (парсер/store/router/клиент/UI) · переключатель режимов · `ParticipantsParserModule` ·
парсинг участников (фильтры/мульти-цели) · TGStat как источник целей · полировка групп ·
пересечение аудиторий · экспорт `.xls` · ★-бейдж · code-split + автотесты · режим «кто писал» ·
папки + валидация · расширенный поиск TGStat · OpenAI-ключ (health/Help Center) ·
НейроДиалоги (ответы всем + пресеты) + фикс зависания воркеров · пресеты во всех модулях ·
единый разбор «защиты аккаунтов» + разделы «Риски» в Help Center · правки GGR.

## 📘 Документация проекта

[docs/PROJECT-2026-07-22.md](./docs/PROJECT-2026-07-22.md) — что это за система, иерархия
Цель→Кампания→Задача→Агент→Аккаунт, готовность по каждому модулю и разделу, биллинг,
безопасность аккаунтов и открытые вопросы по прайсам. Это же ядро отчёта к пятнице (§5.5).

## 🧪 Ручной прогон 21–22.07 по ТЗ и роадмапу — 39 находок из 41 закрыто

Полный отчёт с описаниями и причинами: [docs/TEST-PLAN-checklist.html](./docs/TEST-PLAN-checklist.html)
(118 тестов, результат у 105; остальные 13 сняты как разрушающие или недоступные без стенда).
Каждый коммит-правка назван номером теста, поэтому связь «находка → фикс» прослеживается по `git log`.

**Главный вывод прогона.** Восемь защит были написаны и покрыты юнит-тестами, но в бою
молчали — до них не доходили данные. Это оказалось важнее любой отдельной находки:

| Защита | Почему не работала |
|---|---|
| Права на папки целей (§8.1) | `GET /target-folders` отдавал все папки всем — фильтр жил только во фронте |
| Аудит опасных действий (§3.1) | папки, лиды и расписания не вызывали `appendAudit` вовсе |
| Точечный запрет доступа (§8.1) | `mergePermissions` копировала только ALLOW, запрет терялся |
| Дедуп целей (§3.7) | без `toLowerCase` — `@Crypto` и `@crypto` два канала, двойная обработка |
| Разные тексты у аккаунтов (§9) | вариант шаблона брался по `promptIndex` — настройке задачи, общей для всех |
| «Непрогретый в бой не идёт» (§3.3) | статус `warming` не выставлял никто, он был недостижим |
| Гейт trust (§6) | fail-open: аккаунт без оценки — самый уязвимый — проходил |
| Честный статус постинга (§8.4) | задача с 0 из N показывалась как «Готово» |

**Также закрыто:** потеря входящих в переписке и сортировка инбокса по алфавиту (§9.5);
пауза парсера теряла прогресс и результаты (§3.9); «Стоп» не работал на задаче с паузы;
учёт рабочего времени считал календарь (одна «сессия» в 98 часов); подпись задачи не
различала парсеры по ключевым словам; запуск правила боевого модуля без подтверждения;
импорт прокси игнорировал строку-схему и ссылки ротации IP; живость прокси мерялась
TCP-пингом вместо протокольного рукопожатия; `geoSource` не сохранялся; модалка смены
прокси читала пустой мок-стор вместо базы; у кампании не было своих целевых каналов,
а рядом жил неотличимый «разовый запуск»; «?» блока открывал статью всего модуля.

**Осталось 2 — кода в них нет:** `9.10` (проверить через `GetAuthorizations()`, что аккаунт
реально ходит через прокси) и `6.11` (не воспроизводится: форма такое состояние не собирает,
бэкенд его отклоняет).

Автотесты: **288 зелёных**, из них 33 регресс-теста на эти находки
(`server/__tests__/qaFixes2607.test.js`, каждый назван номером теста).

## ⛔ Бэклог / известные моменты

| # | Пункт | Приоритет | Детали | Куда в плане |
|---|---|---|---|---|
| B1 | Формула рейтинга ★ | ☑ | ★ по §6: приоритет **ER** (вовлечённость = (реакции+комменты)/просмотры, ~4%+ = полный бонус) + нормализ. подписчики (занижены) + открытые комменты (`channelRating`). ER считается 2-м проходом (`engagementFromPosts` в `channelStats.js`, средние views/reactions/comments), fallback на свежесть если ER нет. Юнит-тесты | §3.8 |
| B2 | `parsing-users` на закрытых группах | средний | `CHAT_ADMIN_REQUIRED` → подсказка режим «Активные» | — |
| B3 | Live-прогон инкубатор-модулей | высокий | `neuro-*`/`mass-*`/`warming`/`ggr` системно на 5 акках не гоняли | Фаза 1–2 (тест-прогон) |
| B4 | Прокси-фильтр | низкий | Дефолт «Рабочие прокси» = OFF | Фаза 3 §3.4 |
| B5 | Прогресс/остановка | низкий | UI-отклик остановки с задержкой | Фаза 2 (Task) |
| B6 | Реалтайм | низкий | polling 2.5s без websockets | — |
| B7 | GGR | низкий | БЕТА, формула требует калибровки | §6 (метрики) |

## Тесты и качество

`npm test` (`parser.test.js` + `aiHelp.test.js`) · `npm run lint` (`tsc --noEmit`, 0 ошибок) · `npm run build`.

## Что подключает каждый у себя (нет в репо)

`.env` (`OPENAI_API_KEY`, `TELEGRAM_API_ID/HASH`) · cookies TGStat · аккаунты/сессии через Менеджер.

---

# Часть III. Правки по созвону 17.07.2026

> Полный разбор демо/кросс-теста (Илья + Николай + заказчик) с архитектурой оркестрации
> **Цель → Кампания → Задача → Модуль** и автоматизацией под кампанию/задачу:
> **[docs/meeting-2026-07-17.md](./docs/meeting-2026-07-17.md)** — 14 разделов + 5 фаз работ.
>
> Ключевое (🧠 требует утверждения схемы до старта): резерв→кампания, кампания настраивает
> модуль (3 режима выбора аккаунтов), автоматизация только под кампанию/задачу, вьюшки вместо
> модалок, «выбор кампании» в модуле вместо «цели», проверка уникальности задачи.

**Статус на 22.07.2026: 70 ☑ / 6 ◐ / 3 ☐** из 71 пункта разбора. Полностью закрыты
§1 резерв→кампания · §2 менеджер · §5 кампании · §6 автоматизация · §7 модули ·
§13 Help Center · §14 деплой. Детали и что осталось — в самом файле разбора.

**Разделение 21.07:** §10 Прокси и остаток §11 Mailing взял Илья («не трогай, я вже роблю там») —
Николай в `AddAccountWizard.tsx` / `ProxyPoolModal.tsx` / `proxies.js` / `mailing.js` / `MailingPage.tsx`
не заходит.

Требования созвона внесены в ТЗ как **[§9](./docs/TZ-2026-07-10.md#9-дополнения-от-17072026-демо--кросс-тест-с-заказчиком)**
(раньше жили только в файле разбора — из-за этого роадмап их не отслеживал).

### Что осталось по §9 ТЗ — поимённо

**Lane A (Илья):**
- ◐ §9.10 Прокси: выбор из базы в мастере добавления · новый прокси → имя аккаунта + попадание в базу · цепочка SIM/device/IMEI.
- ◐ §9.11 Mailing: конвертация телефон↔username · папки для номеров.
- ☑ §9.4 Дедлайн цели останавливает и УЖЕ ИДУЩИЕ задачи: проверка в цикле всех шести воркеров (раньше дедлайн смотрели только при создании задачи, и запущенная накануне рассылка работала после срока). Оркестрация/воркеры).
- ☑ §9.12 Барьер прогрева в менеджере аккаунтов: массовый «Стоп / освободить» снимал локи напрямую через `/release` и обходил защиту — остановить недели работы можно было в два клика. Теперь тот же 403, что и в модуле.
- ◐ §9.14 LIVE-прогон деплой-пайплайна на боевом сервере (нужны доступы).

**Lane B (Николай):**
- ☑ §9.3 / §8.7 Общий компонент переписки: `ConversationBubble` + `ReplyBox` в `features/conversation`,
  подключены и в «Обзор аккаунта», и в НейроДиалоги. Копия разметки в НейроДиалогах удалена, вместе
  с ней ушёл мёртвый `fmtReply`/`replyRef`. Обзор аккаунта получил превью медиа (бэкенд отдавал их
  и раньше — страница просто не показывала), многострочное поле и панель форматирования.
- ☑ §9.8 Редактирование задачи — только на паузе. `PATCH /:moduleKey/tasks/:id/settings` (409, если
  статус не `paused`) + чистые `canEditTask`/`pickEditableSettings` (4 теста) + панель правки во
  вьюшке. Состав аккаунтов править нельзя: за задачей держатся локи, подмена оставила бы их висеть
  на чужих профилях — это отдельно закреплено тестом.

**Ждёт заказчика (не код):**
- ☐ §9.4 «Время выполнения» цели — отдельное поле или дубль дедлайна?
- ☐ §9.10 Какие именно «доп. настройки прокси под аккаунт» — нужен список.
- ☐ §8.10 Два ролика DarkSoft — до сих пор нет текстом.

**Починено по итогам боевого прогона 21–22.07 (см. `docs/QA-run-2026-07-21-results.md`,
`docs/BUGS-2026-07-21-proxy-import.md`):**
- ☑ Мёртвый ключ OpenAI больше не деградирует в шаблон: фатальные ошибки (401/403/404/квота)
  останавливают задачу, временные (сеть/5xx/rate limit) по-прежнему дают шаблон на один коммент.
- ☑ Шаблон выбирался по `promptIndex` — все аккаунты писали дословно одинаковый текст.
  Теперь случайно и с оглядкой на уже отправленное в задаче.
- ☑ Массовый импорт прокси: заголовки схем `http:`/`socks5:`, живость по реальному выходу
  (статус `bad` вместо ложного «ok»), `geoSource` сохраняется, «проверить все» обновляет гео,
  ссылки ротации не считаются ошибкой.
- ☑ §3.6 Переписку с лидом видно из CRM — читается из Telegram аккаунтом-владельцем.
- ☑ §3.3 В менеджере виден срок спамблока; 20 зависших без срока аккаунтов долечены.
- ☑ §9 Второй вариант первого сообщения терялся (`\w` не покрывает кириллицу) — вся рассылка
  уходила одним текстом. Варианты чередуются, в мейлинге видно их число до запуска.
- ☑ §3.9 Пять причин FloodWait в парсере участников: двойной `GetParticipants`, чат канала
  тремя способами, цели без чата пропускаются, FloodWait останавливает поток аккаунта,
  нижняя граница паузы вступления 60с.
- ☑ Старт API не ждёт опроса прокси; `unhandledRejection`/`uncaughtException` больше не молчат.

**Ждёт живых сценариев (главный риск, см. Фазу 1):**
- ☐ ≥5 тест-профилей в разных состояниях (FloodWait/карантин/invalid/горячий лид). Вся state machine,
  локи и гейты покрыты юнит-тестами, но на реальных сбоях не проверялись ни разу.
