node_modules
dist
dist-ssr
*.local
.DS_Store
*.tsbuildinfo
.env
# Бэкапы .env содержат те же секреты, что и сам .env, — в репозиторий им нельзя.
.env.*
!.env.example
server/data/
/AI-Incubator-report-2026-07-26.docx
backups/

# Одноразовые debug/скрин-скрипты (Claude), не коммитим
scripts/_*.mjs

# Локальные настройки Claude Code и утилиты с сервисными ключами (см. шапку файла)
.claude/settings.local.json
scripts/set-supabase-pass.mjs

# Локальный выбор API-сервера (боевой/локальный). У каждого свой — в репозиторий
# не коммитим: случайный "useLiveApi": true увёл бы чужой дев на боевой сервер.
# Образец — api.config.example.json, без файла всё работает на локальном бэкенде.
api.config.json

# Serena (навигация по коду). Конфиг и заметки коммитим — они общие для команды;
# кеш индекса у каждого свой и пересобирается на машине.
.serena/cache/
