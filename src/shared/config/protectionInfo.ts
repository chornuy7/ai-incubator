// Единый источник правды по логике «Защиты аккаунтов».
// Числа соответствуют реальной реализации на сервере:
//  • server/lib/protection.js — delayMultiplier (уровень ×1.8/1/0.75, пресет ×0.6/1/1.8),
//    effectiveProbability (кап 25% / 45%), isAccountRunnable (пропуск статусов), pickDelay (пол 5с).
//  • server/lib/accountRunner.js — handleFlood (FloodWait + запас, карантин после N),
//    applyBanPolicy (политики бана/спамблока), perAccountLimitReached (суточный кап).
//  • server/aiSafety.js — глобальные дефолты (запас 120с, порог карантина 3, кап 0 = выкл).
//  • server/lib/accountLocks.js — один аккаунт = одна задача (глобальные локи).

export interface ProtectionStep {
  title: string
  body: string
}

export const PROTECTION_STEPS: ProtectionStep[] = [
  {
    title: 'Задержки между действиями',
    body:
      'Базовая пауза умножается на уровень защиты (Консервативный ×1.8, Сбалансированный ×1, Агрессивный ×0.75) и на пресет задержек (Мин ×0.6, Рекомендуемые ×1, Макс ×1.8), плюс глобальный множитель ИИ-безопасности. Точное значение выбирается случайно из диапазона, но не меньше 5 секунд — ровных интервалов, по которым вычисляют ботов, не бывает.',
  },
  {
    title: 'Вероятность действия',
    body:
      'При включённой защите доля целей, по которым бот реально действует, ограничивается сверху: Консервативный — не выше 25%, Сбалансированный — не выше 45%, Агрессивный — без ограничения. Остальные цели пропускаются «по вероятности», чтобы поведение не выглядело сплошным перебором.',
  },
  {
    title: 'FloodWait',
    body:
      'Если Telegram просит подождать (FloodWait), бот делает паузу на всю длительность флуда плюс запас (по умолчанию 120с). После 3 FloodWait подряд аккаунт уходит в карантин и в работу больше не берётся, пока вы не снимете статус.',
  },
  {
    title: 'Пропуск проблемных аккаунтов',
    body:
      'Аккаунты со статусами quarantine, spamblock, invalid, frozen и reauth задача не трогает — воркер их пропускает. Если пропущен полный круг (все аккаунты недоступны или выбрали лимиты), задача корректно завершается, а не крутится вхолостую.',
  },
  {
    title: 'Реакция на бан и спамблок',
    body:
      'Глобальные политики ИИ-безопасности: при бане — продолжить, отправить в карантин, остановить аккаунт или всю задачу (по умолчанию — продолжить с логом); при спамблоке — пропустить или карантин. Дополнительно есть мягкий суточный лимит действий на аккаунт (по умолчанию выключен).',
  },
  {
    title: 'Один аккаунт — одна задача',
    body:
      'Глобальные локи не дают одному аккаунту работать в двух модулях одновременно: занятый помечается «в работе» и пропускается другими задачами. Поэтому важно не запускать пересекающиеся наборы аккаунтов параллельно.',
  },
]

/** Короткая сводка одной строкой — для подсказок и шапок. */
export const PROTECTION_SUMMARY =
  'Уровень защиты множит задержки и ограничивает вероятность действий; FloodWait → пауза → карантин; проблемные статусы и занятые аккаунты пропускаются.'
