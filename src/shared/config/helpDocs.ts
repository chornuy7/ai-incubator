// Документация Help Center по модулям и парсерам.
// Тексты описывают реальную реализацию воркеров на сервере
// (server/modules/workers.js, server/neuroCommenting/worker.js,
// server/lib/protection.js, server/lib/accountRunner.js, server/lib/accountLocks.js).
// Где серверная логика упрощена, а поведение задаётся интерфейсом/конфигом модуля,
// это явно оговорено в тексте.

export interface HelpDoc {
  /** Человекочитаемый заголовок для отображения. */
  title: string
  /** «Что это» — что за фича и зачем. */
  what: string
  /** «Как работает внутри» — реальная механика воркера. */
  how: string
  /** «Как работает вместе с остальными» — связь с другими модулями. */
  together: string
  /** «Пример результата» — конкретный сценарий «выбрал X → получил Y». */
  example: string
  /** Короткие советы (опционально). */
  tips?: string[]
}

// Общие факты о защите/блокировках, чтобы не повторять их дословно в каждом доке:
// • Уровни защиты множат все задержки: Консервативный ×1.8, Сбалансированный ×1, Агрессивный ×0.75.
// • Пресеты задержек множат сверху: Мин ×0.6, Рекомендуемые ×1, Макс ×1.8 (плюс глобальный множитель ИИ-безопасности).
// • При включённой ИИ-защите вероятность действия ограничивается: Консервативный ≤ 25%, Сбалансированный ≤ 45%.
// • Один аккаунт = одна задача на всех модулях (глобальные локи). Занятый аккаунт помечается «в работе».
// • FloodWait: пауза = длительность флуда + запас (по умолчанию 120с); после 3 FloodWait подряд аккаунт уходит в карантин.

export const HELP_DOCS: Record<string, HelpDoc> = {
  'neuro-commenting': {
    title: 'Нейрокомментинг',
    what:
      'Модуль автоматически оставляет ИИ-комментарии под постами выбранных каналов. Он мониторит целевые каналы и генерирует осмысленные комментарии нейросетью в выбранном тоне (позитивный, вопрос автору, аналитический и т.д.), имитируя живого подписчика.',
    how:
      'Аккаунты из пула перебираются по кругу. Для каждого прохода бот берёт случайный канал, при необходимости вступает в него (задержка вступления по умолчанию 84–156с), затем читает последние 20 постов и отбирает кандидатов по «Режиму комментирования» (Случайный / По ключевым словам / Все посты) и фильтру минимума слов. По каждому кандидату срабатывает бросок вероятности (по умолчанию 30%): если проходит — бот ждёт задержку комментария 30–120с, генерирует текст через OpenAI (при отсутствии ключа OPENAI_API_KEY используется шаблон) и публикует комментарий в обсуждении поста. Режим работы «По количеству» ограничивает общий лимит и лимит на аккаунт, «По времени» — длительностью; между проходами пауза 5–15с, каждый (аккаунт+пост) комментируется только один раз.',
    together:
      'Аккаунты берутся из общего пула и на время задачи блокируются глобальным локом — тот же профиль нельзя параллельно запустить в Нейрочаттинге, Реакциях или парсерах (в таблицах он показывается как «в работе»). Целевые каналы можно добавить вручную, из прошлых задач или из папки — той же папкой пользуются другие модули. Уровень защиты и пресет задержек умножают все паузы и ограничивают вероятность, а FloodWait и баны обрабатываются общей логикой ИИ-безопасности (карантин после 3 FloodWait). Автоответчик может передавать ответы в НейроДиалоги.',
    example:
      'Выбрали 5 аккаунтов, 20 каналов, режим «Только новые», вероятность 30%, защита «Сбалансированный»: бот подписывается на каналы, ждёт новые посты и оставляет ИИ-комментарий примерно на каждый третий пост, с паузами 30–120с между комментариями. За задачу с лимитом 100 комментариев вы получите ~100 живых комментариев, равномерно размазанных по 5 аккаунтам, с логом каждого отправленного текста в истории.',
    tips: [
      'Начните с пресета «Рекомендуемые» и защиты «Сбалансированный» — вероятность автоматически ограничится 45%.',
      'Без ключа OPENAI_API_KEY комментарии будут шаблонными — это видно в логах пометкой «Шаблон».',
    ],
  },

  'neuro-chatting': {
    title: 'Нейрочаттинг',
    what:
      'Модуль ведёт ИИ-переписку в группах и чатах: мониторит сообщения в целевых группах и автоматически отвечает умными репликами нейросети в заданном стиле (дружелюбный, экспертный, юмористический и т.д.).',
    how:
      'Аккаунты перебираются по кругу. На каждом проходе бот вступает в случайную группу (задержка вступления 50–120с), читает последние 15 сообщений и выбирает одно случайное. Срабатывает бросок вероятности ответа (по умолчанию 30%): если проходит — бот ждёт задержку отправки 42–78с, генерирует ответ нейросетью (с учётом глубины контекста диалога) и отправляет его реплаем на выбранное сообщение. Режим работы «По количеству» / «По времени» и лимиты аналогичны Нейрокомментингу; между проходами пауза 5–15с.',
    together:
      'Как и остальные активные модули, Нейрочаттинг захватывает аккаунты глобальным локом — занятый профиль недоступен другим модулям. Целевые группы задаются вручную, из прошлых задач или из общей папки целей. Уровень защиты и пресет задержек масштабируют паузы и режут вероятность (≤45% на «Сбалансированном»), FloodWait/баны идут через общую ИИ-безопасность. Автоответчик умеет переключаться на текстовые шаблоны или на НейроДиалоги для продолжения переписки в ЛС.',
    example:
      'Выбрали 3 аккаунта, 15 групп, режим «На триггеры», вероятность 30%, глубина контекста 10 сообщений: бот заходит в группы, читает свежую переписку и примерно на каждое третье подходящее сообщение отвечает по контексту, с паузами 42–78с. При лимите 100 сообщений вы получите ~100 естественных реплаев от трёх аккаунтов без явных признаков автоматизации.',
    tips: [
      'Глубина контекста >0 делает ответы точнее; 0 — это legacy-режим без учёта переписки.',
    ],
  },

  'mass-react': {
    title: 'Массовые Реакции',
    what:
      'Модуль автоматически ставит эмодзи-реакции на посты в каналах и группах — либо мониторит новые посты, либо реагирует на конкретные существующие. Помогает быстро «оживить» посты набором реакций от разных аккаунтов.',
    how:
      'Аккаунты перебираются по кругу; используется палитра эмодзи (по умолчанию 👍 ❤️ 🔥). Если указаны прямые ссылки на посты — бот реагирует именно на них; иначе вступает в случайную цель и берёт последний пост. Дальше идёт бросок вероятности реакции (по умолчанию 50%) и, если проходит, отправляется случайный эмодзи из палитры. Задержка между реакциями по умолчанию 30–120с, задержка входа 21–39с, между проходами 5–15с. Есть лимиты «Максимум реакций» и «на 1 аккаунт», а также правила вроде «не реагировать, если уже больше 2 реакций».',
    together:
      'Аккаунты берутся из общего пула и блокируются локом на время задачи — параллельный запуск того же профиля в других модулях невозможен. Цели и ссылки на посты можно тянуть из общей папки и из истории заданий; чёрный список каналов исключает нежелательные цели ещё до действий (общий механизм для всех модулей). Уровни защиты и пресеты задержек масштабируют паузы, FloodWait/спамблок обрабатываются общей ИИ-безопасностью.',
    example:
      'Выбрали 10 аккаунтов, вставили 3 ссылки на посты, эмодзи 👍 ❤️ 🔥, вероятность 50%, цель 100: бот распределяет реакции по постам от лица разных аккаунтов, ставя случайный эмодзи примерно в половине проходов, с паузами 30–120с. В итоге на каждый пост набегает набор из десятков разнообразных реакций, а история реакций фиксирует, какой аккаунт что поставил.',
    tips: [
      'Режим «Существующие сообщения» с прямыми ссылками точнее, чем «Мониторинг» новых постов.',
    ],
  },

  'mass-looking': {
    title: 'Масслукинг',
    what:
      'Модуль массово просматривает истории (stories) у каналов и пользователей от лица ваших аккаунтов. Просмотры повышают видимость ваших аккаунтов у целевой аудитории и выглядят как естественная активность.',
    how:
      'Аккаунты перебираются по кругу. На каждом проходе бот подключается к цели (при необходимости вступает), затем помечает истории цели как просмотренные. Задержка действия по умолчанию 20–60с, между проходами 10–30с; в лог пишется, сколько историй просмотрено. Задача идёт по общему лимиту действий/времени, аккаунты, ушедшие в карантин или невалидные, пропускаются.',
    together:
      'Как и другие активные модули, Масслукинг захватывает выбранные аккаунты глобальным локом (тот же профиль занят и недоступен другим модулям). Цели задаются юзернеймами/ссылками; уровень защиты и пресет задержек управляют темпом, а FloodWait обрабатывается общей ИИ-безопасностью с уходом в карантин после серии флудов.',
    example:
      'Выбрали 8 аккаунтов и 95 целей: бот по очереди заходит к каждой цели и от лица всех аккаунтов просматривает их актуальные истории с паузами 20–60с. За задачу вы получаете тысячи просмотров сторис, распределённых так, чтобы каждый аккаунт вёл себя как обычный зритель.',
  },

  warming: {
    title: 'Прогрев аккаунтов',
    what:
      'Модуль имитирует естественную активность аккаунтов, чтобы свежие или «спящие» профили выглядели достоверно и реже попадали под ограничения Telegram. Прогрев не выполняет целевых действий — только фоновую «жизнь» аккаунта.',
    how:
      'Аккаунты перебираются по кругу, и на каждом проходе бот случайно выбирает одно из безопасных действий: пролистать список диалогов, выполнить публичный поиск (например, по «news») или просто пингануть аккаунт (getMe). Между проходами пауза 30–90с (масштабируется защитой). Целей задавать не нужно — модуль работает только с самими аккаунтами; невалидные/карантинные профили пропускаются.',
    together:
      'Прогрев тоже занимает аккаунты глобальным локом, поэтому прогреваемый профиль на время задачи недоступен боевым модулям — прогрев логично запускать отдельным этапом перед комментингом/чаттингом. Уровень защиты и пресет задержек задают темп «жизни», а FloodWait обрабатывается общей ИИ-безопасностью.',
    example:
      'Выбрали 20 новых аккаунтов, защита «Консервативный»: в течение задачи каждый аккаунт периодически (раз в ~30–160с с учётом множителя ×1.8) листает диалоги, ищет публичные каналы и пингует сервер. В результате аккаунты накапливают ровную фоновую историю активности и становятся готовы к боевым модулям с меньшим риском блокировок.',
    tips: [
      'Прогревайте на «Консервативном» — задержки увеличиваются, активность выглядит спокойнее.',
    ],
  },

  'neuro-dialogs': {
    title: 'НейроДиалоги',
    what:
      'Модуль автоматически отвечает на входящие личные сообщения от лица ваших аккаунтов, ведя переписку нейросетью в выбранном стиле (дружелюбный, деловой, продающий, нейтральный). Подходит для авто-обработки ЛС и продолжения диалогов из других модулей.',
    how:
      'Аккаунты перебираются по кругу. На каждом проходе бот запрашивает до 20 диалогов, отбирает непрочитанные, берёт первый и читает последние 3 сообщения для контекста. Затем генерирует ответ нейросетью (или шаблоном без ключа OpenAI), ждёт задержку 5–30с и отправляет сообщение. Если непрочитанных нет — короткая пауза и следующий аккаунт; между проходами 10–25с.',
    together:
      'Аккаунты захватываются общим локом, как и в остальных модулях. НейроДиалоги служат «продолжением» воронки: Нейрокомментинг и Нейрочаттинг через автоответчик могут переводить общение в ЛС, где переписку подхватывает этот модуль. Темп и обработка ошибок — через общий уровень защиты и ИИ-безопасность.',
    example:
      'Выбрали 4 аккаунта и стиль «Продающий»: при поступлении новых ЛС бот от имени нужного аккаунта отвечает по последним 3 сообщениям диалога, выдерживая паузу 5–30с перед отправкой. В результате входящие обрабатываются автоматически и «по-человечески», а в логах видно, в каком диалоге и что было отвечено.',
  },

  ggr: {
    title: 'GGR · GramGPT Рейтинг™',
    what:
      'Инструмент оценки качества Telegram-аккаунтов. Быстро проверяет, живая ли сессия аккаунта, и выставляет числовой рейтинг, помогая отсеять невалидные и слабые профили перед запуском боевых модулей.',
    how:
      'Модуль перебирает выбранные аккаунты (или все не удалённые, если ничего не выбрано), подключается сессией и вызывает getMe. Рейтинг складывается из наличия валидной сессии (базово 50 баллов), привязанного юзернейма (+15) и телефона (+10). Валидные аккаунты получают статус «активный» и сохранённый ggrScore; аккаунты с нерабочей сессией помечаются как невалидные и уходят на реавторизацию. Проверка идёт последовательно, без вступлений и целей.',
    together:
      'GGR читает тот же пул аккаунтов, что и остальные модули, и обновляет их статусы/метаданные, которые затем видны в таблицах выбора (валидный/заморожен/разавторизирован/невалидный). Логично запускать GGR перед активными модулями, чтобы исключить мёртвые сессии до захвата локов и старта работы.',
    example:
      'Нажали «Проверить все» по 200 аккаунтам: модуль по очереди подключает каждую сессию и выставляет балл — например, валидный аккаунт с юзернеймом и телефоном получит 75/100, а профиль с битой сессией — 0 и статус «разавторизирован». На выходе — отсортированный список, где сразу видно, какие аккаунты готовы к работе.',
    tips: [
      'Итоговый балл берётся из проверки сессии и профиля, а не из реальной «истории» аккаунта в Telegram.',
    ],
  },

  parsing: {
    title: 'Парсинг каналов',
    what:
      'Модуль ищет Telegram-каналы по ключевым словам и собирает их в список для дальнейшей работы (комментинг, реакции и т.д.). Это и есть «Парсер каналов»: на входе — тематические слова, на выходе — отфильтрованная база каналов.',
    how:
      'Из ключевых слов и окончаний строятся поисковые запросы (каждое слово + комбинации со всеми окончаниями, с дедупликацией). Аккаунты перебираются по кругу, и по каждому запросу выполняется публичный поиск (до 50 результатов), из которого оставляются только каналы (broadcast), а группы отбрасываются. Дальше идут фильтры: диапазон участников (число дообогащается запросом GetFullChannel при необходимости), фильтр комментариев (любые / только открытые обсуждения / только закрытые) и исключение уже спарсенных. Результаты дедуплицируются по username/id; между запросами пауза (по умолчанию ~2с), лимит результатов настраивается (∞ по умолчанию).',
    together:
      'Парсер использует те же аккаунты и глобальные локи, что и боевые модули, — на время парсинга профили заняты. Это первый шаг пайплайна «парсинг → модули»: найденные каналы попадают в список, который затем скармливается Нейрокомментингу или Массовым Реакциям через папку/историю целей. Чёрный список и уровни защиты/задержек действуют так же, как в остальных модулях; аккаунты в карантине пропускаются.',
    example:
      'Ввели ключевые слова «массаж», «СТО», «нужен разработчик», минимум участников 100, лимит ∞: бот прогоняет запросы по ротации аккаунтов и собирает, скажем, 240 уникальных каналов, отфильтрованных по числу подписчиков и типу обсуждений. Список можно сразу передать в Нейрокомментинг как целевые каналы.',
    tips: [
      'Окончания к ключевым словам расширяют охват — каждое слово комбинируется с каждым окончанием.',
    ],
  },

  'parsing-groups': {
    title: 'Парсер групп Telegram',
    what:
      'Модуль ищет Telegram-группы (чаты) по ключевым словам с фильтрами по числу участников, активности и типу доступа. По сути тот же движок, что и «Парсинг каналов», но нацеленный на группы/мегагруппы.',
    how:
      'Механика идентична парсеру каналов: из ключевых слов и окончаний строятся запросы, аккаунты перебираются по кругу, по каждому запросу выполняется публичный поиск (до 50). Разница в типовом фильтре — оставляются группы/мегагруппы, а чистые каналы отбрасываются. Применяются диапазон участников (по умолчанию минимум 10), дедуп по username/id и исключение уже спарсенного; между запросами выдерживается пауза.',
    together:
      'Как и другие модули, парсер занимает аккаунты локом. Результат — база групп для Нейрочаттинга и Массовых Реакций: найденные чаты передаются в эти модули через папку/историю целей. Уровни защиты, задержки и чёрный список работают единообразно со всей системой.',
    example:
      'Ввели «машина», «сто», «жк», «барбер», лимит 100, минимум участников 10: бот собирает до 100 релевантных групп с указанием числа участников и ссылкой. Дальше этот список идёт в Нейрочаттинг как целевые группы для ИИ-ответов.',
  },

  'parsing-users': {
    title: 'Парсер пользователей Telegram',
    what:
      'Модуль выгружает списки участников из открытых Telegram-чатов и групп с возможностью экспорта в JSON/CSV/Excel. Даёт готовую аудиторию для дальнейшей работы (например, для Масслукинга).',
    how:
      'Бот подключает аккаунт, вступает в указанную группу/чат и запрашивает список участников (по умолчанию до лимита, заданного в настройках), отсекая ботов. На стороне интерфейса доступны дополнительные фильтры — пропуск удалённых/заблокированных, «только с username/фото», «только Premium», «только админы», фильтр активности и задержки между чатами/пользователями; эти опции задаются в конфиге модуля и применяются на уровне пользовательской логики поверх базовой выгрузки участников.',
    together:
      'Парсер занимает аккаунт локом на время выгрузки, как и остальные модули. Это часть пайплайна сбора аудитории: полученный список пользователей можно использовать целями в Масслукинге или выгрузить для внешних сценариев. Источники групп берутся, в том числе, из истории целей, общей с другими модулями.',
    example:
      'Указали 3 группы, лимит участников 1000, включили «Пропустить ботов» и «Пропустить удалённых»: бот заходит в каждую группу и собирает участников, отфильтровав ботов, — на выходе список пользователей, готовый к экспорту в CSV/Excel и к использованию в Масслукинге.',
    tips: [
      'Базовая выгрузка на сервере убирает ботов; расширенные фильтры профиля настраиваются в интерфейсе модуля.',
    ],
  },

  'parsing-messages': {
    title: 'Парсер по сообщениям Telegram',
    what:
      'Модуль предназначен для сбора пользователей по ключевым словам в сообщениях с фильтрацией по датам и статистикой активности (число сообщений, даты первого и последнего сообщения).',
    how:
      'В текущей серверной реализации бот подключает аккаунт, вступает в указанный чат и выгружает его участников (с отсевом ботов) — это общий облегчённый обработчик парсинга. Логика поиска по ключевым словам в сообщениях, фильтр по дням, лимит сообщений, учёт ответов/пересланных и профильные фильтры (username/фото/Premium) заданы в конфиге модуля и относятся к пользовательской логике интерфейса — на уровне серверного воркера они пока не выполняются отдельно от базовой выгрузки участников.',
    together:
      'Модуль использует общий пул аккаунтов и глобальные локи. Источники чатов можно подтянуть из истории групп, общей с другими модулями, а результат — использовать как аудиторию для боевых модулей. Уровни защиты и обработка FloodWait — общие для системы.',
    example:
      'Указали 21 чат, ключевые слова и фильтр «за 30 дней», лимит сообщений 1000: по задумке модуля вы получаете список пользователей, писавших по этим словам, со статистикой активности. Фактически серверный обработчик сейчас вернёт участников указанных чатов (без ботов), а тонкая фильтрация по словам/датам применяется на уровне интерфейса.',
    tips: [
      'Расширенная фильтрация по словам и датам — это пользовательская логика интерфейса; базовый серверный сбор возвращает участников чата.',
    ],
  },

  'parsing-comments': {
    title: 'Парсер комментариев Telegram',
    what:
      'Модуль предназначен для сбора пользователей из комментариев к постам каналов с фильтрами по ключевым словам, лимитам постов/комментариев и минимальной длине комментария.',
    how:
      'В текущей серверной реализации используется тот же облегчённый обработчик: бот подключает аккаунт, вступает в указанный канал и выгружает участников (без ботов). Параметры «лимит постов», «комментариев на пост», «минимальная длина комментария», ключевые слова и опция «сохранять текст комментария» описаны в конфиге модуля и задают пользовательскую логику интерфейса — как отдельная серверная механика чтения комментариев к постам они пока не реализованы поверх базовой выгрузки.',
    together:
      'Как и остальные парсеры, модуль занимает аккаунт локом и питается из общей истории каналов. Результат — аудитория для дальнейших модулей или экспорта. Темп и защита аккаунтов — общие для всей системы.',
    example:
      'Указали канал, лимит постов 50 и 100 комментариев на пост: по задумке вы получаете авторов комментариев, отфильтрованных по словам и длине. Фактически серверный обработчик сейчас вернёт участников канала (без ботов), а фильтры комментариев работают как пользовательская логика интерфейса.',
    tips: [
      'Фильтры по тексту комментариев — уровень интерфейса; серверный воркер выполняет базовую выгрузку участников.',
    ],
  },
}

// ── Матчинг topic-строки к документации ──────────────────────────────────
// helpTopic приходит либо как заголовок модуля (ModuleConfig.title),
// либо как заголовок секции (из SectionCard). Дополнительно поддерживается
// подсказка moduleKey (например, из URL /panel/modules/:moduleKey), которая
// однозначно разрешает неоднозначные заголовки секций внутри модуля.

function normalize(s: string): string {
  return (s || '').toLowerCase().replace(/\s+/g, ' ').trim()
}

/** Уникальные псевдонимы (нормализованные) → ключ модуля. Неоднозначные
 * заголовки секций (напр. «Настройки поиска», «Настройки парсинга») сюда НЕ входят —
 * они разрешаются через moduleKey. */
const ALIASES: Record<string, string> = {}
function addAlias(key: string, ...aliases: string[]) {
  for (const a of aliases) {
    const n = normalize(a)
    if (n && !(n in ALIASES)) ALIASES[n] = key
  }
}

for (const [key, doc] of Object.entries(HELP_DOCS)) {
  addAlias(key, key, doc.title)
}
addAlias('neuro-commenting', 'настройки комментирования')
addAlias('neuro-chatting', 'настройки чаттинга')
addAlias('mass-react', 'массовые реакции', 'настройки реакций', 'эмодзи для реакций', 'реакции от канала')
addAlias('mass-looking', 'масслукинг', 'настройки масслукинга')
addAlias('warming', 'прогрев аккаунтов', 'настройки прогрева', 'прогрев')
addAlias('neuro-dialogs', 'нейродиалоги')
addAlias('ggr', 'ggr', 'gramgpt рейтинг', 'gramgpt рейтинг™', 'ggr · gramgpt рейтинг', 'ggr · рейтинг')
addAlias('parsing', 'парсинг каналов', 'парсер каналов')
addAlias('parsing-groups', 'парсер групп telegram', 'парсинг групп')
addAlias('parsing-users', 'парсер пользователей telegram', 'парсинг юзеров')
addAlias('parsing-messages', 'парсер по сообщениям telegram', 'парсинг сообщений')
addAlias('parsing-comments', 'парсер комментариев telegram', 'парсинг комментариев')

/**
 * Находит документацию по строке-топику и (опционально) ключу модуля.
 * Приоритет: точный псевдоним топика → подсказка moduleKey → вхождение подстроки.
 */
export function findHelpDoc(topic: string, moduleKey?: string): HelpDoc | null {
  const n = normalize(topic)

  // 1) Точное совпадение по заголовку модуля / уникальному псевдониму секции.
  if (n && ALIASES[n] && HELP_DOCS[ALIASES[n]]) return HELP_DOCS[ALIASES[n]]

  // 2) Подсказка из URL — надёжно разрешает секции внутри модуля
  //    (в т.ч. неоднозначные «Настройки поиска» / «Настройки парсинга»).
  if (moduleKey && HELP_DOCS[moduleKey]) return HELP_DOCS[moduleKey]

  // 3) Мягкое совпадение: топик содержит заголовок модуля или наоборот.
  if (n) {
    for (const [alias, key] of Object.entries(ALIASES)) {
      if (alias.length >= 4 && (n.includes(alias) || alias.includes(n)) && HELP_DOCS[key]) {
        return HELP_DOCS[key]
      }
    }
  }

  return null
}
