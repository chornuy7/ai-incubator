/* Собирает интерактивный чек-лист из docs/TEST-PLAN-full.md + результатов прогона.
 *
 * ⚠️ УСТАРЕЛ / НЕ ПЕРЕЗАПУСКАТЬ поверх docs/TEST-PLAN-checklist.html.
 * После блока 9 чек-лист велся вручную (прогон 21–22.07): у части тестов по
 * НЕСКОЛЬКО заметок с разными статусами (напр. 10.1 = прошло + замечания + баг),
 * заметки в СЫРОМ HTML, а не в markdown-lite этого скрипта. RESULTS ниже
 * структурно этого не выражает (одна заметка на тест, свой esc/fmt), и повторный
 * запуск СНЁС бы блоки 10–13. Актуальный файл — результат ручного ведения,
 * скрипт оставлен как исторический генератор каркаса блоков 1–9. */
const fs = require('fs')

/** Итоги ручного прогона 21.07: id → {s, note}. s: ok | warn | fail | skip */
const RESULTS = {
  '1.1': { s: 'warn', note: 'Цель создаётся, редактирование работает. НО дедлайн принимал год 123123 и несуществующие даты (2026-02-31), цель по лидам — 999999999999. Такая цель НЕ ИСТЕКЛА БЫ НИКОГДА, т.е. блокировка задач под просроченную цель молча не работала. ПОЧИНЕНО + 3 регресс-теста. Цель «Тест5» создана до фикса — дедлайн там битый, поправить руками.' },
  '1.2': { s: 'warn', note: 'Кампания создалась: цель, один модуль, закрепление аккаунтов. НО на странице ДВЕ разные «кампании» — старый запускатор «одна цель → несколько модулей» живёт рядом с новой сущностью и генерит свой id camp_xxx, не связанный с cmp_xxx. Плюс поле «Статус» ничего не объясняет, хотя «Завершена» молча освобождает закреплённые аккаунты.' },
  '1.3': { s: 'fail', note: 'ТРИ проблемы. (1) У OpenAI кончилась квота (429 insufficient_quota) — модуль МОЛЧА публикует шаблон вместо ИИ к цели и НЕ останавливается. (2) Разные аккаунты пишут ДОСЛОВНО одинаковый текст: вариант выбирается по promptIndex — настройке задачи, а не по аккаунту, поэтому все берут один элемент массива. Одинаковый текст с нескольких аккаунтов под одним постом = сигнатура ботофермы, прямой путь к спам-блоку. ДОКАЗАНО НА ЖИВОЙ ГРУППЕ: 17 июля, когда квота была жива, три аккаунта написали три РАЗНЫХ осмысленных комментария по теме поста. 21 июля с мёртвой квотой — три ОДИНАКОВЫХ «Привет! Рад быть здесь, буду следить за обновлениями.», причём ОДИН аккаунт (Регото) повторил тот же текст ДВАЖДЫ в ту же группу с разницей в два часа (16:42 и 18:37). Это уже не «разные аккаунты пишут одинаково», а аккаунт, дословно повторяющий сам себя. (3) Запуск со страницы «Кампания» ушёл мимо кампании, с фантомным camp_...' },
  '1.4': { s: 'skip', note: 'Заблокировано. Комментарий физически ушёл в Telegram (sent), но шаблонный — проверяем не «дошло ли сообщение», а «сработал ли ИИ к цели». Вернуться после пополнения квоты OpenAI.' },
  '1.5': { s: 'ok', note: 'На карточке задачи видно статус, прогресс, цель, инициатора, время; логи и результаты — внутри вьюшки. Наблюдение: в списке НЕ видно кампанию (только цель), хотя кампания теперь средний уровень модели. При нескольких кампаниях под одной целью список станет неразличимым.' },
  '2.1': { s: 'ok', note: 'ГЛАВНЫЙ ВЫВОД: правильный путь работает. В блоке «Запуск» есть селектор «Кампания», и задача ушла с настоящим campaignId=cmp_285edb73 («Тест 6»), а не с фантомным camp_. Значит чинить надо не архитектуру, а убрать старый запускатор со страницы «Кампания». Попутно в этом же блоке найдено 5 проблем UI — см. тест 8.x и журнал (сумма типов 102% из коробки блокирует запуск; ошибка ссылается на кнопку «= 100%», которой нет — она подписана «поровну»; текст «нормируется к 100%» противоречит требованию «уменьшите»; числа в полях обрезаются спиннерами; непонятно, что делать).' },
  '2.2': { s: 'ok', note: 'Во вьюшке задачи: КАМПАНИЯ «Тест 6» → ЦЕЛЬ КАМПАНИИ «Тест5». Цель подтянулась сама, прямой выбор цели скрывается при выбранной кампании.' },
  '2.3': { s: 'ok', note: 'Пока кампания не выбрана, доступен блок «Цель напрямую (без кампании)» — старый путь не сломан.' },
  '3.1': { s: 'warn', note: 'Колонка «Кампания» на месте: закреплённые показывают 🔒 + имя, остальные «в общем пуле». НО НАЙДЕН БАГ: список кампаний и карта закреплений грузятся ОДИН РАЗ при открытии страницы (useEffect с пустыми зависимостями), тогда как аккаунты обновляются каждые 30 с. Создали вторую кампанию на другой странице — менеджер о ней не узнал: её не было в фильтре, а её 4 аккаунта показывались «в общем пуле», хотя закреплены. После F5 всё стало верно. Последствие как у бага 3-a: оператор видит «свободные» аккаунты и раздаёт их другой кампании, ломая гарантию закрепления — только здесь причина не старый блок, а устаревший экран.' },
  '3.2': { s: 'ok', note: 'Замок рисуется, подсказка внятная: «Закреплён за кампанией «Тест 6» — вышел из общего пула». На странице кампании своя подсказка «Аккаунты закреплены — вышли из общего пула».' },
  '3.11': { s: 'ok', note: 'Балл показан у всех пяти аккаунтов (75/81/79/78/89), не только у проблемных, с цветом по банду.' },
  '3.3': { s: 'warn', note: 'Функция работает: сняли галочку «Закрепить аккаунты» — бэкенд сохранил pinned=false, в карте закреплений остался только аккаунт второй кампании. Замечание: отличить состояния глазами почти нельзя. Закреплён — замок закрытый янтарный, без лока — открытый серый, оба 11px; тестировщик не смог их различить на скриншоте. Нужна явная подпись («закреплён» / «без лока»), а не только форма иконки. Второе: изменение видно ТОЛЬКО после F5 — см. баг 3.1 про устаревшие данные.' },
  '3.7': { s: 'ok', note: 'Отработало точно как задумано: кнопка показала «В корзину (1 в работе)», тост объяснил что делать — «Сначала остановите работу. 1 акк. в работе/прогреве — удалить нельзя. Нажмите Стоп/освободить, затем в корзину». Отказ с инструкцией, а не молчаливое игнорирование. ПОПУТНО НАЙДЕНО: аккаунт Trader Diares висит в статусе «В работе» при НУЛЕ запущенных задач и без лока. reconcileStaleTasksOnBoot (accountLocks.js:178) при старте чистит локи и помечает зависшие задачи stopped, но статус аккаунта в accountsMeta не сбрасывает — после падения или рестарта аккаунт остаётся «в работе» навсегда. Проверено: назначению это НЕ мешает (assertAccountsAssignable пропускает), то есть интерфейс и бэкенд расходятся: оператор видит «занят» и обходит аккаунт стороной, а автоматика его спокойно берёт.' },
  '3.5': { s: 'warn', note: 'Перенос работает: аккаунт убирается из прежней кампании и не висит в двух местах, «вернуть в общий пул» тоже отработало. БАГ: назначение ОДНОГО аккаунта переписывает политику ВСЕЙ кампании — в AccountsPage.tsx:117 вместе со списком уходит pinned: lock, то есть галочка «закрепить» из маленького диалога меняет режим закрепления целой кампании и всех остальных её аккаунтов. Проверено: «Тест 6» была pinned=true, её не редактировали — после назначения стала pinned=false, закрепление слетело со всех.' },
  '3.6': { s: 'skip', note: 'Недоказуемо на текущем состоянии: к моменту теста pinned уже был false (следствие бага 3.5), освобождать было нечего. Сама логика «done отпускает аккаунты» покрыта юнит-тестом (pinnedAccountMap пропускает done). Вернуться после правок. ПОПУТНО: у завершённой кампании остаётся полностью активная кнопка «Запустить» (CampaignPage.tsx:464, без проверки статуса) — запуск пойдёт на аккаунтах, которые статусом done уже освобождены и могут быть заняты кем-то другим; статус при этом останется done (авто-переключение есть только из draft). И вторая: завершённая кампания продолжает показываться в колонке «Кампания» у аккаунта, хотя она уже ничего не использует.' },
  '3.8': { s: 'ok', note: 'Три состояния работают: при частичном выборе чекбокс шапки показывает риску, клик по нему снимает выбор ПОЛНОСТЬЮ (а не выделяет все — типичная ошибка таких таблиц). Панель массовых действий вернулась в «Выберите аккаунты для управления».' },
  '3.12': { s: 'warn', note: 'Половина работает: «Управление» открыло вьюшку, слева ровно 3 выбранных, кнопка «Только выбранные · 3 — показать все» на месте. БАГ (мой, от 21.07): при клике по аккаунту в левой колонке навигация идёт на /panel/accounts/:id БЕЗ сохранения ?sel= (AccountOverviewPage.tsx:136), поэтому выборка теряется с первого же переключения, кнопка исчезает и вернуться к ней нельзя. А переключение между аккаунтами — основной сценарий этого экрана, ради него он и делался. Фикс на один рядок.' },
  '4.1': { s: 'ok', note: 'Обзор — отдельная страница /panel/accounts/:id с «← Назад к менеджеру», оверлея модалки нет.' },
  '4.2': { s: 'ok', note: 'Раскладка как почта: слева колонка аккаунтов с поиском, справа табы; переключение аккаунта не выкидывает со страницы.' },
  '4.3': { s: 'ok', note: 'Таб переживает переключение: открыли «Каналы» у Ілля, перешли на Claude Bet AI — остались «Каналы». С галочкой «сбрасывать на профиль» переключение вернуло на «Профиль». Обе ветки отработали.' },
  '4.4': { s: 'ok', note: 'Стакания нет, проверено сверкой UI с API. Ілля — 50 каналов (X Empire Apps unread=0, Hrum Family 41), Claude Bet AI — 28 каналов (unread=5, Hrum 982). Совпадающие числа (63/218/129/6) — реальные данные Telegram: оба аккаунта вступили в эти каналы и одинаково их не читали, это не копирование списка. Остаточного отображения при переключении тоже не было.' },
  '4.5': { s: 'warn', note: 'Превью РИСУЮТСЯ — бэкенд отдаёт image/jpeg, проверено: 3 из 3 запросов вернули валидный JPEG. НО они бесполезно мелкие: беру thumb: 0, то есть САМУЮ мелкую миниатюру — отсюда 660-800 байт и размытые квадратики; последнее фото (таблица) вообще почти белое пятно. Для «не хранить оригинал» это хорошо, для «увидеть, что прислали» — нет. Нужен размер побольше с потолком по весу (20-30 КБ). Плюс пузырёк сужается под короткий текст «📷 Фото», и картинка ужимается вместе с ним.' },
  '4.6': { s: 'fail', note: 'Тот же компонент, но вылез баг ХУЖЕ превью: ПЕРЕПИСКА НЕ ОБНОВЛЯЕТСЯ. msgCache (NeuroDialogsModule.tsx:238) при повторном открытии диалога отдаёт кэш и НИКОГДА не перезапрашивает; чистится только при смене набора аккаунтов. Кнопка обновления освежает лишь список слева — поэтому там появилось «📷 Фото 18:07», а справа висело старое. Пришлось перезагружать всю страницу и заново выбирать аккаунт. Проверено по API: бэкенд отдаёт 9 сообщений, включая 4 новых входящих (2 фото, ссылка, «А ти тут»), UI показывал только до «Тут?». Для модуля переписки это значит, что ОПЕРАТОР НЕ ВИДИТ ВХОДЯЩИХ. ВТОРОЕ: список диалогов отсортирован НЕ ПО ВРЕМЕНИ, а по непрочитанным и дальше ПО АЛФАВИТУ (service.js:52-55) — свежий диалог 18:07 оказался шестым, октябрьский ниже майского. Хуже того, в объекте диалога вообще нет поля с датой, только готовая строка time «28 мая», то есть фронт не пересортирует даже при желании — нужно отдавать сырой timestamp с бэкенда. Для инбокса ожидание однозначное: новое сверху.' },
  '4.7': { s: 'ok', note: 'Поле — многострочная textarea с подсказкой «Enter — отправить, Shift+Enter — новая строка», отрисовано в обоих экранах (НейроДиалоги и Обзор аккаунта) через общий ReplyBox. Само нажатие Shift+Enter вживую не кликали.' },
  '4.8': { s: 'ok', note: 'Панель форматирования на месте: B, I, ссылка + подсказка «**жирный** · __курсив__ · [ссылка](url)». Обёртывание выделения вживую не кликали.' },
  '5.1': { s: 'ok', note: 'Форма цели — полноэкранная вьюшка с «← Назад к целям», список скрыт, модалки нет.' },
  '5.2': { s: 'ok', note: 'Все три шага. (1) Год 123123 отбит с внятным текстом «Дата вне допустимого диапазона (2000-2046) — проверьте год», значение НЕ сохранилось (это фикс от 21.07, раньше проходило молча). (2) Прошедшая дата сохранилась, на карточке красный бейдж «⏱ Дедлайн истёк». (3) Запуск задачи под просроченной целью → 409 «Цель «Тест5» просрочена (дедлайн 2025-07-24) — работа по ней остановлена». ОТДЕЛЬНО: пустой goalId проходит как «без цели» и гард дедлайна просто не срабатывает — если id где-то потеряется во фронте, защита тихо обойдётся.' },
  '5.4': { s: 'ok', note: 'Подпись «(0 = не задано, максимум 1 000 000)» на месте, потолок проверен на бэкенде (999999999999 → 1000000). На карточке прогресс-бар «Лиды к цели 0/199».' },
  '5.5': { s: 'ok', note: 'Обе ветки: PNG (116 КБ) — превью картинки + «открыть файл»; .txt (146 КБ) и .xlsx (328 КБ) — ссылка «открыть файл» + тост «Файл добавлен в базу знаний». Удаление записи доступно. Видео не принимается — это по замыслу (лимит 3 МБ), подпись «картинка или документ» честная. ВОПРОС К ЗАКАЗЧИКУ: нужно ли видео в КБ и нужен ли отдельный тип записи «ссылка» — сейчас URL можно вставить только текстом.' },
  '5.6': { s: 'fail', note: 'Лимит 3 МБ практически не работает, и ошибка ВВОДИТ В ЗАБЛУЖДЕНИЕ. Файл 4 МБ → base64 5.33 МБ → превышает лимит тела запроса express (5mb), и запрос умирает ДО моей проверки размера: PayloadTooLargeError, express отдаёт HTML-страницу вместо JSON. Фронт не может её разобрать и показывает «API недоступен — перезапустите npm run dev», хотя API исправен. Итог: вежливое «Файл больше 3 МБ — уменьшите» показывается только в узком окне 3-3.6 МБ; всё тяжелее даёт ложную ошибку с инструкцией для разработчика, бессмысленной на боевом стенде. Проверять размер надо в браузере ДО отправки.' },
  '5.7': { s: 'ok', note: 'На карточке цели блок «КАМПАНИИ ЦЕЛИ (2)» с обеими кампаниями и переходом.' },
  '5.8': { s: 'ok', note: 'Сработали все четыре сигнала сразу: тост «🔥 Горячий лид! @MiGrain1231 — свяжитесь немедленно», баннер вверху страницы, счётчик «Горячий 1 🔥» подсвечен красным, бейдж на карточке. НАЙДЕНО ПОПУТНО: два одинаковых выпадающих списка «Все цели» рядом — слева фильтр, справа цель для НОВОГО лида, и оба используют один goalOptions, где пустое значение подписано «Все цели» (LeadsPage.tsx:84,114,118). В форме создания оно должно называться «Без цели». Из-за этого лид создался без цели при активном фильтре по цели, и система одновременно сказала «Лид добавлен» и «Лидов пока нет» — без намёка, что запись скрыта фильтром. Список при этом обновляется корректно, дело не в нём.' },
  '5.9': { s: 'skip', note: 'Заблокировано квотой OpenAI: авто-запись лида идёт из воркера авто-ответчика, без работающей генерации проверить нельзя.' },
  '5.10': { s: 'skip', note: 'Заблокировано вместе с 5.9. Сама логика «воронка не откатывается назад» покрыта юнит-тестом (advanceLeadStatus).' },
  '6.2': { s: 'fail', note: 'ДВА бага. (1) «Стоп» НЕ РАБОТАЕТ на задаче с паузы: stopWorker (workers.js:80) только ставит stopRequested и ждёт, что РАБОТАЮЩИЙ воркер его заметит — у паузы воркера нет, флаг обрабатывать некому. Два запроса подряд вернули 200, статус остался paused. Оператор видит успех, а задачу можно возобновить кнопкой «Запустить» — для боевого модуля это значит, что «остановленная» задача может ожить. Пришлось править файл задачи вручную. Показательно, что pauseWorker такую проверку статуса ИМЕЕТ, а stopWorker — нет. (2) Перезапуск создаёт НОВУЮ задачу с новым id, но оставляет пользователя на странице СТАРОЙ, которая честно показывает «Остановлена» — выглядит как «ничего не произошло», новую видно только в дашборде. Надо либо перекидывать на новую, либо писать её номер.' },
  '6.3': { s: 'warn', note: 'Гейт работает правильно: у завершённой/остановленной задачи карандаш заблокирован. НО объяснение НЕДОСТУПНО: причина спрятана в title disabled-кнопки, а браузеры не показывают подсказки на disabled-элементах — они не получают событий мыши. Тестировщик увидел ровно то, что увидит оператор: «не могу нажать» и ни намёка почему. Мой просчёт при реализации: объяснение надо выносить текстом рядом с кнопкой.' },
  '6.7': { s: 'ok', note: 'Задача открывается отдельной страницей /panel/tasks/:id с «← Назад к задачам», видны полные логи, аккаунты в работе и каналы.' },
  '6.9': { s: 'ok', note: 'Поля разведены и не путаются: «МОДУЛЬ», «КАМПАНИЯ», «ЦЕЛЬ КАМПАНИИ» и отдельно «КАНАЛОВ / ЧАТОВ» + блок «Каналы / чаты — где работает модуль».' },
  '6.4': { s: 'warn', note: 'Панель «Правка задачи (на паузе)» открывается на задаче с паузы, поля каналов и лимитов на месте, сохранение доступно. НО ОПАСНЫЙ ДЕФОЛТ (мой баг): если лимиты в задаче НЕ ЗАДАНЫ (в settings они null), панель показывает 0, потому что читает `s.maxActions ?? 0`. Оператор видит нули, считает их настоящими значениями и жмёт «Сохранить» — в настройки пишутся явные нули. Дальше resolveTotalTarget использует `??`, который ноль НЕ пропускает, поэтому дефолт 100 больше не срабатывает. Проверено на живой задаче nc_ebda4fe0: лимиты не заданы → цель 92; после сохранения нулями → цель 0. Задача запустится и молча ничего не сделает. В коде targets.js уже стоит защита с комментарием «Защита от тихого нуля» — панель обходит её с другой стороны. Правильно: показывать пустое поле с подсказкой «не задано (по умолчанию 100)» и не писать в настройки то, чего пользователь не трогал.' },
  '6.5': { s: 'ok', note: 'Жёлтое предупреждение на месте и объясняет причину: «Состав аккаунтов здесь не меняется: за задачей держатся блокировки профилей. Нужны другие исполнители — остановите задачу и создайте новую». Полей аккаунтов в панели нет.' },
  '6.10': { s: 'warn', note: 'Модуль ИЗ КАМПАНИИ подтягивается корректно: выбрал «Тест 6» — поле «МОДУЛЬ (ИЗ КАМПАНИИ)» заполнилось «Нейрокомментинг». НО подсказка обещает «Модуль, аккаунты и пресет — из кампании», а блок аккаунтов остаётся ПУСТЫМ и форма требует выбрать вручную («Выберите аккаунты» блокирует сохранение). При этом БЭКЕНД это умеет: resolveRuleTarget (automation/store.js:165) берёт accountIds кампании, если у правила их нет. То есть логика есть, подсказка правдива, а фронт не даёт ей воспользоваться. ХУЖЕ: нет проверки, что выбранные аккаунты вообще принадлежат кампании — правило привязано к «Тест 6» (аккаунт Регото), а работает на Ілля; пересечение ПУСТОЕ. Та же болезнь, что баг 3-a, только здесь модель закрепления обходит автоматика, которая сработает без человека.' },
  '6.11': { s: 'warn', note: 'Валидация есть — без аккаунтов сохранить не даёт («Выберите аккаунты»). Но проверить путь «без кампании И без модуля» не удалось: кампания выбирается сразу и подтягивает модуль, так что пустое правило в этой форме собрать нельзя. Отдельно: у ПРАВИЛА нет подтверждения при запуске боевого модуля — см. 6.12.' },
  '6.12': { s: 'warn', note: 'Правило реально создаёт задачу: ручной запуск дал nc_b1eaaa42 с инициатором «Автоматизация: 1» — узнаваемость в дашборде работает. НО НАЙДЕНО 🔴: кнопка ▶ в строке правила запускает БОЕВОЙ модуль БЕЗ ПОДТВЕРЖДЕНИЯ. runAutomationRuleNow вызывается напрямую (AutomationPage.tsx:71), тогда как запуск из модуля (LiveModule.tsx:246) и перезапуск из дашборда (TasksPage.tsx:145) спрашивают «Реальные действия в Telegram». Это единственный путь, обходящий защиту, и кнопка стоит вплотную к карандашу и корзине — попасть случайно легко. Именно так и вышло при тесте: один клик → реальная задача → шаблонный комментарий в группу без единого вопроса. Плюс правило было ВЫКЛЮЧЕНО, а ▶ всё равно сработала.' },
  '6.1': { s: 'fail', note: 'ПРОДОЛЖЕНИЕ НЕ ПРОДОЛЖАЕТ, А ПЕРЕЗАПУСКАЕТ. Проверено на парсере (безопасный модуль, ничего не публикует). До паузы: 10/100 действий, 10 результатов, в логах «Готово · найдено 11 каналов». После «Запустить/возобновить» в логах сразу «Парсинг запущен · запросов: 11» — модуль стартовал С НАЧАЛА: счётчик найденного сбросился в ноль («crypto» → +3 (всего 3)), total пересчитался со 100 на 11, результаты 10 → 0 и собрались заново. Итог 33/33. То есть прогресс НЕ сохраняется и ранее собранные результаты ТЕРЯЮТСЯ. В роадмапе записано, что пауза/продолжение проверялись вживую на ПРОГРЕВЕ и работали — значит поведение зависит от модуля, общей гарантии «продолжит с места» нет, хотя интерфейс обещает её одинаково для всех.' },
  '6.6': { s: 'fail', note: 'Подпись задачи для ВСЕХ ПАРСЕРОВ вырождается в один аккаунт. taskSignature складывает аккаунты + цель + каналы/targets, но парсеры каналов не используют — они ищут по keywords, а ключевые слова в подпись НЕ входят. Проверено напрямую: поиск «crypto» и поиск «nft» на одном аккаунте дают идентичную подпись {"a":["acc_1"],"g":"","t":[]}. Значит две РАЗНЫЕ параллельные выгрузки одним аккаунтом будут отклонены как «идентичная задача уже запущена». И наоборот — для парсеров проверка вырождается в «одна задача на аккаунт», дублируя лок аккаунта, но с вводящим в заблуждение текстом. Это мой код (taskDedup.js, §7): проверял на модулях с каналами и не учёл семейство парсеров, а их пять. В UI баг не проявился только потому, что парсер отрабатывает за ~30 секунд и первая задача успевала завершиться до второго клика.' },
  '6.13': { s: 'warn', note: 'Блок «Запуск по расписанию» есть, но НЕ ВЕЗДЕ. Реализован только в LiveModule.tsx, а ModuleLiveRouter (features/modules/index.tsx) отправляет парсеры в ChannelParserModule / ParticipantsParserModule, а НейроДиалоги — в свой компонент. Итог: расписание доступно в нейрокомментинге, нейрочатинге, реакциях, масслукинге, прогреве и AIR — и отсутствует во всех 5 парсерах и в НейроДиалогах. Тестировщик искал его в парсере и не нашёл. Сама автоматизация парсеры умеет (они есть в списке AUTOMATABLE на странице «Автоматизация») — нет только быстрого пути из модуля.' },
  '6.8': { s: 'ok', note: 'Рейтинг по каждому аккаунту отрисован как задумано: таблица «Аккаунт | Балл | Статус», у Claude Bet AI и Дмитрій Руб по 75 с цветной шкалой и бейджем «валиден». Не сырая строка. Задача ggr_07657b70, 2/2, модуль ничего не публикует — только подключается и оценивает.' },
  '7.1': { s: 'ok', note: 'Гейтинг сайдбара работает. Сначала модератор видел всё — но это оказалось ВЕРНЫМ поведением: у тестового пользователя было ДВЕ роли (roleIds: role_moderator + role_operator), а mergePermissions — объединение, и «Оператор» давал полный доступ. После снятия второй роли осталось ровно то, что выдано: Менеджер аккаунтов, Дашборд задач, Поддержка, Мой аккаунт. Остальное скрыто. Замечание: имя пользователя «Тестовый модератор» вводит в заблуждение — фактические права шли от второй роли, и понять это из интерфейса нельзя.' },
  '7.2': { s: 'ok', note: 'Прямой заход по URL не обходит гейт: /panel/roles под модератором даёт экран «Нет доступа к разделу» с объяснением и кнопкой в поддержку, а не страницу ролей.' },
  '7.3': { s: 'ok', note: 'ГЛАВНЫЙ ТЕСТ БЛОКА — ПРОШЁЛ. Доступ роли на ГРУППУ аккаунтов работает сквозняком под реальным логином. Роли выдана только группа «3» (Trader Diares + Регото Папіроні), точечных выдач НОЛЬ — проверено по API. Модератор видит ровно этих двоих, счётчики статусов тоже считаются по видимым. Это та самая фича, которая не работала НИ РАЗУ с момента появления: mergePermissions теряла accountGroups, а can() не знал такого kind. Починено 21.07, теперь подтверждено вживую.' },
  '7.6': { s: 'ok', note: 'Красная пометка на месте: в матрице ролей у модуля «Прогрев» стоит бейдж «СТОП — ТОЛЬКО АДМИН». Добавлено 21.07 по итогам аудита §12.' },
  '7.4': { s: 'fail', note: 'ТОЧЕЧНЫЙ ЗАПРЕТ НЕ РАБОТАЕТ ВООБЩЕ — цепочка рвётся в ТРЁХ местах подряд. (1) Матрица ролей показывает НЕЗАДАННОЕ право как «Убрать»: aPerm = perms.resources.accounts?.[id] ?? deny (RolesPage.tsx:125). Три состояния (allow / deny / не задано) схлопнуты в два, а ведут себя по-разному: явный deny бьёт групповой allow, а «не задано» нейтрально. Админ видит красную «Убрать» у аккаунта, который на самом деле доступен через группу, и проверить правду из интерфейса нельзя. (2) Клик по «Убрать» ничего не сохраняет: чекбокс уже checked (value===deny из-за фолбэка), поэтому поставить явный запрет через UI практически невозможно — проверено дважды по API, запись не появилась. (3) Даже записанный через API deny ТЕРЯЕТСЯ: mergePermissions копирует только ALLOW (roles.js), поэтому до клиента запреты не доезжают никогда. Проверено: в роли {acc_99a46d69fa1e: deny, acc_9c93eedbb6f2: deny} → после merge {}. Модератор после перезахода по-прежнему видит запрещённый аккаунт. ВАЖНО: 21.07 я видел этот же рядок и записал в комментарий, что это «следствие union-семантики, а не недосмотр» — классификация была НЕВЕРНОЙ. §12 ТЗ прямо требует «точечный deny сильнее группового allow», и кнопка «Убрать» это обещает. Ошибку допустил потому, что проверял чистую функцию isAccountAllowedViaGroups (она верна и покрыта тестом), а ломается слоем выше — та же ловушка, что с группами: логика верная, доставка сломана.' },
  '7.8': { s: 'ok', note: 'Доступ на каналы через папки работает. Проверено на реальных данных роли «Модератор»: разрешённая папка «1» (1 канал) → роль видит [dadaqqqeqw]; запрещённая папка «1» (11 каналов) → []. Правило «ничего не выбрано = все каналы папки, включая будущие» подтвердилось. Оговорка: проверено на уровне логики (allowedFolderTargets с реальными данными), а не кликами — модератору не выдан ни один модуль с выбором каналов из папок, поэтому UI-путь не пройден.' },
  '7.9': { s: 'fail', note: 'УЧЁТ РАБОЧЕГО ВРЕМЕНИ СЧИТАЕТ НЕ РАБОТУ, А КАЛЕНДАРЬ. Тестировщик заметил несуразицу: «сегодня 5м» рядом с «7 дней 149ч 42м». В логе нашлась запись usr_admin: старт 17.07 16:53, длительность 98.47 ч — одна сессия длиной четверо суток. Две причины. (1) Открытая сессия растёт круглосуточно: workSummary берёт `const end = e.end == null ? now : e.end`, а закрытия по таймауту неактивности и закрытия открытых сессий при рестарте бэкенда нет — закрыл вкладку и «работаешь» всю ночь. (2) Сессия целиком засчитывается в окно своего НАЧАЛА: `if (e.start >= dayStart) todayMs += dur` — в коде это честно названо «простая аппроксимация». Поэтому 98-часовая сессия от 17-го полностью легла в «7 дней» и ничего не дала в «сегодня»: две цифры считаются по разным сессиям и между собой не связаны. §8.1 в роадмапе помечен готовым — но если по этим числам платить операторам, они показывают время, когда человек просто не нажал «Выйти». Нужно: таймаут неактивности, закрытие открытых сессий на старте бэкенда, и резать длительность по границе суток.' },
  '7.7': { s: 'ok', note: 'Подтверждение массовой остановки работает: «Остановить задач: 2?» — счёт верный, в цели попадают только активные (running/queued/paused), завершённые не трогаются. Отдельная оговорка про прогрев в коде есть и подставляется условно: warmN = stopTargets.filter(t => t.moduleKey === warming), текст «Из них прогрева: N — прогресс будет потерян». В этом прогоне прогрева среди выбранных не было, поэтому фраза не показалась — ветка осталась непроверенной вживую. Второй барьер (ввод числа) есть в коде и включается от 100 задач; у нас их ~50, поэтому не сработал — формула покрыта юнит-тестом.' },
  '11.1': { s: 'skip', note: 'Передано Илье 21.07 — парсер каналов и парсер пользователей он тестирует сам.' },
  '11.2': { s: 'skip', note: 'Передано Илье вместе с парсером каналов.' },
  '11.4': { s: 'skip', note: 'Передано Илье 21.07 — парсер пользователей он тестирует сам.' },
  '11.5': { s: 'skip', note: 'Передано Илье вместе с парсером пользователей (пересечение аудиторий — его часть).' },
  '11.8': { s: 'skip', note: 'Передано Илье вместе с парсером пользователей (закрытые группы).' },
  '8.1': { s: 'ok', note: 'AI-промты стоят сразу под выбором аккаунтов — как основа модуля. Подтверждено при прокрутке страницы.' },
  '8.2': { s: 'ok', note: 'Тайминги и задержки — в самом низу, после блока «Запуск». Меняются редко, поэтому убраны с глаз.' },
  '8.3': { s: 'ok', note: 'Чёрный список — компактный СВЁРНУТЫЙ блок внутри секции каналов, с переключателем «показать / скрыть». В развёрнутом виде объясняет, что цели исключаются из всех модулей (нейрокомментинг, реакции, чаттинг, масслукинг).' },
  '8.4': { s: 'ok', note: 'Пресеты с цветовой меткой и владельцем: модалка предлагает 6 цветов и поле владельца; после сохранения чип в «Моих пресетах» показывает цветную точку и бейдж владельца («Тест1 · ТЕСТ2»). Подпись честно уточняет, что выбор аккаунтов пресетом не меняется.' },
  '8.5': { s: 'ok', note: 'Нижняя панель прилипает: при прокрутке к «Целевым каналам» внизу экрана осталась строка «Готов к запуску · 2 акк. · … · Задачи модуля в Дашборде». Быстрый доступ к статусу без возврата наверх.' },
  '8.6': { s: 'ok', note: 'Переход в дашборд предфильтрован по модулю: до клика 53 задачи и фильтр «Все модули», после — фильтр «Нейрокомментинг» и 18 задач, сводка пересчиталась (39%, 7 готово / 10 остановлено / 1 на паузе). Открывается именно срез модуля, а не общий список.' },
  '8.8': { s: 'ok', note: 'Режимы комментирования переключаются и меняют форму: при выборе «По ключевым словам» появляется поле ввода слов, которого нет в режиме «Случайный». Рядом — вероятность комментария и выбор, какие посты обрабатывать.' },
  '8.7': { s: 'warn', note: 'АВТОСКРОЛЛ РАБОТАЕТ — правка от 21.07 подтверждена: нажали «?» у нижнего блока «Тайминги и задержки», панель открылась справа, страница перевёрсталась, и блок остался перед глазами. Тестировщик подтвердил словами «Тайминги и задержки норм отображает». НО ВЫЯВЛЕНА ДРУГАЯ ПРОБЛЕМА, которую он же и заметил («некоторые работают, а где-то нет»): своя статья есть только у 3 блоков из 7. Есть: «Выбор аккаунтов», «Тайминги и задержки», «Запуск/Выполнение». Нет: «AI / промпты», «Эмодзи», «Целевые каналы», «Результаты». Причина в findHelpDoc (helpDocs.ts:596): шаг 2 «подсказка из URL» срабатывает РАНЬШЕ мягкого поиска, поэтому любой блок без точного псевдонима на странице модуля молча возвращает статью ВСЕГО модуля. Отсюда и «Настройки комментирования» → «Нейрокомментинг». Пользователь жмёт «?» у конкретного блока и получает общий текст, не понимая почему.' },
  '9.1': { s: 'fail', note: 'Строка-заголовок схемы («http:» отдельной строкой, как присылают продавцы) игнорируется — падает в errors, а прокси разбираются с дефолтной socks5. HTTP-прокси на портах 7063/7469 сохранились как socks5. Проверено curl-ом: 7063 работает ТОЛЬКО как http, 7163 — только как socks5. Половина списка легла в базу нерабочей. Репро и разбор — docs/BUGS-2026-07-21-proxy-import.md. Зона Ильи.' },
  '9.2': { s: 'fail', note: 'Нерабочий прокси показывается зелёным «ok»: живость меряется tcpPing(host, port), а порт открыт независимо от того, тем ли протоколом на него говорить. Прокси со сломанной схемой выглядит здоровым и раздаётся аккаунтам, где отваливается уже в бою. TCP-пинг годится отсечь мёртвый хост, но не как признак «прокси рабочий». Зона Ильи.' },
  '9.3': { s: 'fail', note: 'Страна подменяется на гео ШЛЮЗА. При импорте: probeProxyExitGeo, а если не вышло — фолбэк probeProxyGeo(host). Со сломанной схемой exit-проба падает и молча уходит в фолбэк: у порта 7063 записалась Германия (Hetzner, Falkenstein — сервер провайдера), хотя реальный выход Польша (37.248.166.213, Polkomtel). Оператор раздаёт «немецкий» прокси, а Telegram видит Польшу. Сама функция исправна — при верной схеме отдаёт правильный выход. Зона Ильи.' },
  '9.4': { s: 'fail', note: 'geoSource не сохраняется: роуты проверки возвращают exit | gateway, но в запись прокси это поле не пишется (в базе null у всех). UI не может отличить «гео реального IP» от «гео сервера (примерно)» — а именно ради этого различия §9.10 и заводился. Зона Ильи.' },
  '9.5': { s: 'fail', note: '«Проверить все» не обновляет гео: checkProxyLiveness пишет только status и lastCheckAt. Страна пересчитывается лишь при импорте и в точечном POST /api/proxies/:id/check. После исправления схемы кнопка «проверить все» оставила старую (неверную) страну — вылечилось только точечной перепроверкой. Побочно: note остаётся от прошлой пробы и противоречит стране. Зона Ильи.' },
  '9.11': { s: 'fail', note: 'Ссылки ротации IP (http://host:8881/changeip/<token>), которые продавец присылает вместе со списком, считаются ошибкой формата и попадают в errors. Логично либо игнорировать молча, либо привязывать к прокси как «ссылку на смену IP». Зона Ильи.' },
  '9.7': { s: 'fail', note: 'Выбор из базы прокси НЕ ВИДИТ БАЗУ. Модалка «Сменить прокси» в менеджере читает пул из клиентского мок-стора: `const pool = useApp(s => s.data.proxies)` (AccountsPage.tsx:989), а он инициализируется пустым (seeds.ts) и наполняется только локальной кнопкой в этой же сессии. Настоящие 4 прокси лежат на бэкенде; fetchProxies() вызывается ТОЛЬКО на странице «Прокси» и в мастере импорта. Итог: список всегда пуст, надпись «База прокси пуста — введите новый, он попадёт в базу», и оператора каждый раз заставляют вбивать прокси руками. Обещание «он попадёт в базу» тоже не выполняется — введённое значение в базу не попало. В роадмапе пункт помечен «Проверено LIVE» — скорее всего проверяли сразу после добавления прокси через ту же локальную кнопку, когда массив был непуст. ВТОРОЕ, ХУЖЕ: поле принимает ЛЮБОЙ текст без валидации. Ввели «123іфвіф» — сохранилось, тост «Прокси обновлён», в колонке ПРОКСИ красуется мусор. При подключении parseProxy вернёт null, clientOptions не добавит прокси, и аккаунт молча пойдёт НАПРЯМУЮ с реального IP сервера. Ни ошибки при сохранении, ни при коннекте. Одна опечатка в схеме — и аккаунт выходит наружу без прокси, а интерфейс показывает, что прокси есть.' },
  '9.9': { s: 'fail', note: 'ПОТЕРЯ ДАННЫХ В САМОМ ВАЖНОМ ФАЙЛЕ. Назначение прокси на несколько аккаунтов разом стирает мету всех остальных. Механизм: AssignModal шлёт patchAccount параллельно (`await Promise.all(ops)`, ProxiesPage.tsx:283), а setAccountMeta делает read-modify-write целого файла: loadAllMeta() → all[id] = {...} → saveAllMeta(all) — без сериализации (accountsMeta.js:32). Классическая потеря обновлений: каждый запрос прочитал весь файл, поменял свой ключ и записал всё назад; победил последний, остальное исчезло. Наблюдали живьём: выбрали 4 аккаунта — назначился 1; у трёх аккаунтов пропали ранее выданные прокси; из accounts-meta.json осталось 2 записи вместо 5, а в них name/phone/username/role/project/country = null. В списке вместо имён появились id (acc_d4237fa5793f), телефоны «—», trust упал с 88 до 65 «без мейлинга» — он считается из той же меты. Правило «1 прокси = 1 аккаунт» при этом тоже не сработало: два аккаунта оказались на одном прокси без единого предупреждения. ЭТО ТА ЖЕ ОШИБКА, что чинилась 21.07 через mutateJson в campaigns/accountGroups/leads — но accountsMeta.js (чужая дорожка) остался на голом writeFile, хотя хранит прокси, статусы, профили, роли и проекты. ВОССТАНОВЛЕНО: прогон ?verify=1 вернул имена/телефоны из Telegram, прокси разведены по одному на аккаунт ПОСЛЕДОВАТЕЛЬНО — 5 записей подряд сохранились все, что подтверждает: причина именно в параллельности.' },
  '9.10': { s: 'warn', note: 'ГЛАВНЫЙ ВОПРОС БЛОКА ЗАКРЫТ: для SOCKS прокси реально применяется к сессии. Прочитали account.GetAuthorizations под аккаунтом Claude Bet AI (socks5 :7163) — Telegram показывает текущую сессию как «Poznan, Poland», то есть видит выход прокси, а не реальное расположение сервера. Город отличается от пробы (Słupno vs Poznan) — нормально для мобильных прокси с ротацией IP. НО HTTP-ПРОКСИ НЕ РАБОТАЮТ ВООБЩЕ: тот же вызов под аккаунтом с http-прокси (:7063) падает сразу — «Invalid sockets params: ip=…, port=7063, socksType=undefined». Причина: parseProxy (proxy.js:9) ПРИНИМАЕТ http, но возвращает объект без socksType, а clientOptions просто кладёт его в opts.proxy — GramJS без socksType не умеет. При этом http-прокси спокойно сохраняются, показываются зелёным «Рабочий» (tcpPing проходит) и раздаются аккаунтам. Накладывается на баг 9.1: импорт клал http как socks5, и «исправление» схемы на верную ломает подключение ещё надёжнее. Нужно либо поддержать http на уровне клиента, либо не принимать http вовсе — с внятным отказом при вводе и импорте.' },
  '10.7': { s: 'skip', note: 'Передано Илье 21.07 — мейлинг он взял на себя («Мейлінг не трогай, він теж взяв його»). Реальная отправка по номерам — его часть.' },
  '10.8': { s: 'skip', note: 'Передано Илье вместе с мейлингом — предохранители по trust проверяет он.' },
  '10.9': { s: 'skip', note: 'Передано Илье вместе с мейлингом — обработка номеров не из Telegram.' },
  '2.5': { s: 'skip', note: 'Заблокировано квотой OpenAI: без работающей генерации нельзя проверить, учитывает ли она цель и базу знаний.' },
}

const md = fs.readFileSync('docs/TEST-PLAN-full.md', 'utf8').split('\n')
let block = null
const all = []
for (const l of md) {
  const b = l.match(/^## (Блок [^\n]+)/)
  if (b) { block = b[1]; continue }
  const m = l.match(/^\|\s*([0-9]+\.[0-9]+[a-z]?)\s*(🔴|🟡|⚪)?\s*`?(API|UI|TG)?`?\s*\|\s*(.+?)\s*\|\s*(.+?)\s*\|\s*(.+?)\s*\|\s*$/)
  if (m && block) all.push({ id: m[1], prio: m[2] || '🟡', type: m[3] || 'UI', block, name: m[4], how: m[5], exp: m[6] })
}
const tests = all.filter((t) => t.type !== 'API')

const esc = (s) => String(s).replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')
const fmt = (s) => esc(s).replace(/\*\*(.+?)\*\*/g, '<b>$1</b>').replace(/`(.+?)`/g, '<code>$1</code>')
const LABEL = { ok: 'Прошло', warn: 'Прошло с замечанием', fail: 'УПАЛО', skip: 'Пропущено' }

const blocks = [...new Set(tests.map((t) => t.block))]
const body = blocks.map((b) => {
  const rows = tests.filter((t) => t.block === b).map((t) => {
    const r = RESULTS[t.id]
    const note = r ? `<tr class="note ${r.s}" data-for="${t.id}"><td></td><td colspan="6"><b>${LABEL[r.s]} · 21.07:</b> ${fmt(r.note)}</td></tr>` : ''
    return `
    <tr class="t" data-p="${t.prio}" data-ty="${t.type}" data-id="${t.id}">
      <td class="c"><input type="checkbox"></td><td class="id">${t.id}</td><td class="p">${t.prio}</td>
      <td class="ty">${t.type === 'TG' ? 'Telegram' : t.type}</td>
      <td class="nm">${fmt(t.name)}<div class="how">${fmt(t.how)}</div></td>
      <td class="ex">${fmt(t.exp)}</td>
      <td class="fx"><button class="fail" title="Отметить упавшим">✕</button></td>
    </tr>${note}`
  }).join('')
  return `<section class="blk"><h2>${fmt(b)}</h2><div class="tw"><table><thead><tr><th></th><th>#</th><th>!</th><th>тип</th><th>тест</th><th>ожидаем</th><th></th></tr></thead><tbody>${rows}</tbody></table></div></section>`
}).join('\n')

const head = `<title>Чек-лист тестирования — AI Incubator</title>
<style>
:root{--bg:#0f1117;--card:#171a22;--line:#262b36;--fg:#e8ecf4;--mut:#98a1b3;--ok:#22c55e;--bad:#ef4444;--warn:#f59e0b;--acc:#7c8cff}
@media (prefers-color-scheme:light){:root{--bg:#f6f7fb;--card:#fff;--line:#e2e6ef;--fg:#151922;--mut:#5d6678}}
:root[data-theme=dark]{--bg:#0f1117;--card:#171a22;--line:#262b36;--fg:#e8ecf4;--mut:#98a1b3}
:root[data-theme=light]{--bg:#f6f7fb;--card:#fff;--line:#e2e6ef;--fg:#151922;--mut:#5d6678}
*{box-sizing:border-box}
body{margin:0;background:var(--bg);color:var(--fg);font:14px/1.5 -apple-system,BlinkMacSystemFont,'Segoe UI',Roboto,sans-serif}
.wrap{max-width:1100px;margin:0 auto;padding:20px 16px 80px}
h1{font-size:20px;margin:0 0 4px}.sub{color:var(--mut);font-size:13px;margin-bottom:14px}
.bar{position:sticky;top:0;z-index:5;background:var(--bg);padding:10px 0;border-bottom:1px solid var(--line);margin-bottom:14px}
.prog{height:8px;border-radius:99px;background:var(--line);overflow:hidden;margin-bottom:8px;display:flex}
.prog i{display:block;height:100%;transition:width .3s}
.prog .pOk{background:var(--ok)}.prog .pWarn{background:var(--warn)}.prog .pFail{background:var(--bad)}
.stats{display:flex;gap:14px;flex-wrap:wrap;font-size:12px;color:var(--mut);align-items:center}
.stats b{color:var(--fg)}
.f{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}
.f button{background:var(--card);border:1px solid var(--line);color:var(--mut);border-radius:8px;padding:4px 10px;font-size:12px;cursor:pointer}
.f button.on{border-color:var(--acc);color:var(--fg)}
.blk{margin-bottom:22px;background:var(--card);border:1px solid var(--line);border-radius:12px;overflow:hidden}
.blk h2{font-size:14px;margin:0;padding:11px 14px;border-bottom:1px solid var(--line);background:rgba(124,140,255,.06)}
.tw{overflow-x:auto}
table{width:100%;border-collapse:collapse}
th{text-align:left;font-size:11px;color:var(--mut);font-weight:600;padding:6px 10px;border-bottom:1px solid var(--line)}
td{padding:8px 10px;border-bottom:1px solid var(--line);vertical-align:top}
tr:last-child td{border-bottom:none}
.id{color:var(--mut);font-size:12px;white-space:nowrap}.p,.ty{white-space:nowrap;font-size:12px;color:var(--mut)}
.nm{min-width:230px}.how{color:var(--mut);font-size:12px;margin-top:3px}.ex{color:var(--mut);font-size:12px;min-width:180px}
code{background:rgba(124,140,255,.12);padding:1px 4px;border-radius:4px;font-size:12px}
input[type=checkbox]{width:17px;height:17px;cursor:pointer;accent-color:var(--ok)}
tr.done{opacity:.5}tr.done .nm{text-decoration:line-through}
tr.failed{background:rgba(239,68,68,.09)}tr.failed .id{color:var(--bad);font-weight:700}
tr.warned{background:rgba(245,158,11,.08)}tr.warned .id{color:var(--warn);font-weight:700}
tr.skipped{background:rgba(148,163,184,.08)}tr.skipped .id{color:var(--mut);font-weight:700}
.fail{background:none;border:1px solid var(--line);color:var(--mut);border-radius:6px;cursor:pointer;padding:1px 6px;font-size:12px}
tr.failed .fail{border-color:var(--bad);color:var(--bad)}
tr.note td{font-size:12.5px;line-height:1.6;padding-top:2px;border-top:none}
tr.note.warn td{color:#fbbf24;background:rgba(245,158,11,.07)}
tr.note.fail td{color:#fca5a5;background:rgba(239,68,68,.08)}
tr.note.ok td{color:#86efac;background:rgba(34,197,94,.07)}
tr.note.skip td{color:#cbd5e1;background:rgba(148,163,184,.07)}
.hide{display:none}
.rst{background:none;border:1px solid var(--line);color:var(--mut);border-radius:8px;padding:4px 10px;font-size:12px;cursor:pointer;margin-left:auto}
button:focus-visible,input:focus-visible{outline:2px solid var(--acc);outline-offset:2px}
@media(max-width:720px){.ex{display:none}th:nth-child(6){display:none}}
</style>`

const script = `<script>
var KEY="aiinc-testplan-v3";
var SEED=${JSON.stringify(Object.fromEntries(Object.entries(RESULTS).map(([k, v]) => [k, v.s])))};
var st=JSON.parse(localStorage.getItem(KEY)||"{}");
// Итоги прогона задаются из файла: они общие, а не «у того, кто кликал».
for(var k in SEED) st[k]=SEED[k];
var rows=[].slice.call(document.querySelectorAll("tr.t"));
function paint(){
  var ok=0,warn=0,fail=0,skip=0,crit=0,critOk=0;
  rows.forEach(function(r){
    var id=r.dataset.id,s=st[id]||"";
    r.querySelector("input").checked=(s==="ok"||s==="warn");
    r.classList.toggle("done",s==="ok");
    r.classList.toggle("warned",s==="warn");
    r.classList.toggle("failed",s==="fail");
    r.classList.toggle("skipped",s==="skip");
    if(s==="ok")ok++; else if(s==="warn")warn++; else if(s==="fail")fail++; else if(s==="skip")skip++;
    if(r.dataset.p==="\\u{1F534}"){crit++;if(s==="ok"||s==="warn")critOk++;}
  });
  var n=rows.length;
  document.getElementById("bOk").style.width=(ok/n*100)+"%";
  document.getElementById("bWarn").style.width=(warn/n*100)+"%";
  document.getElementById("bFail").style.width=(fail/n*100)+"%";
  document.getElementById("s1").textContent=(ok+warn)+" / "+n;
  document.getElementById("s2").textContent=fail;
  document.getElementById("s4").textContent=warn;
  document.getElementById("s5").textContent=skip;
  document.getElementById("s3").textContent=critOk+" / "+crit;
  localStorage.setItem(KEY,JSON.stringify(st));
}
rows.forEach(function(r){
  var id=r.dataset.id;
  r.querySelector("input").addEventListener("change",function(e){st[id]=e.target.checked?"ok":"";paint()});
  r.querySelector(".fail").addEventListener("click",function(){st[id]=(st[id]==="fail")?"":"fail";paint()});
});
var fp="",ft="";
function filt(){rows.forEach(function(r){
  var hide=!((!fp||r.dataset.p===fp)&&(!ft||r.dataset.ty===ft));
  r.classList.toggle("hide",hide);
  var nt=document.querySelector('tr.note[data-for="'+r.dataset.id+'"]');
  if(nt)nt.classList.toggle("hide",hide);
})}
[].forEach.call(document.querySelectorAll("[data-fp]"),function(b){b.addEventListener("click",function(){
  fp=(fp===b.dataset.fp)?"":b.dataset.fp;
  [].forEach.call(document.querySelectorAll("[data-fp]"),function(x){x.classList.toggle("on",x.dataset.fp===fp)});filt();})});
[].forEach.call(document.querySelectorAll("[data-ft]"),function(b){b.addEventListener("click",function(){
  ft=(ft===b.dataset.ft)?"":b.dataset.ft;
  [].forEach.call(document.querySelectorAll("[data-ft]"),function(x){x.classList.toggle("on",x.dataset.ft===ft)});filt();})});
document.getElementById("rst").addEventListener("click",function(){if(confirm("Сбросить свои отметки? Итоги прогона останутся.")){st={};for(var k in SEED)st[k]=SEED[k];paint()}});
paint();
<\/script>`

const page = head + `
<div class="wrap">
<h1>Чек-лист ручного тестирования — AI Incubator</h1>
<div class="sub">Прогон 21.07 · ${tests.length} ручных тестов. Под пройденными строками — <b>результат</b>: зелёный «прошло», жёлтый «с замечанием», красный «упало», серый «пропущено». Свои отметки ставьте галочкой, они сохраняются в браузере.</div>
<div class="bar">
  <div class="prog"><i id="bOk" class="pOk"></i><i id="bWarn" class="pWarn"></i><i id="bFail" class="pFail"></i></div>
  <div class="stats">
    <span>Пройдено: <b id="s1">0</b></span>
    <span>С замечанием: <b id="s4" style="color:var(--warn)">0</b></span>
    <span>Упало: <b id="s2" style="color:var(--bad)">0</b></span>
    <span>Пропущено: <b id="s5">0</b></span>
    <span>Критичных: <b id="s3">0</b></span>
    <button class="rst" id="rst">Сбросить свои</button>
  </div>
  <div class="f">
    <button data-fp="🔴">🔴 критичные</button><button data-fp="🟡">🟡 важные</button><button data-fp="⚪">⚪ потом</button>
    <button data-ft="TG">нужен Telegram</button><button data-ft="UI">UI</button>
  </div>
</div>
` + body + `
</div>` + script

fs.writeFileSync(process.argv[2], page)
console.log('готово:', tests.length, 'тестов,', blocks.length, 'блоков, с результатами:', Object.keys(RESULTS).length)
