# Myrmex — корень

AI-платформа массового управления Telegram-аккаунтами: прогрев, нейрокомментинг, нейрочатинг,
парсинг каналов, автоматизации, биллинг действий.

## Карта верхнего уровня

| Путь | Что |
|---|---|
| `src/` | фронт, React/Vite, слои FSD → `mem:src/core` |
| `server/` | бэкенд, Node/Express, ESM → `mem:server/core` |
| `server/data/` | файловое хранилище: `users.json`, `accounts-meta.json`, `roles.json` |
| `scripts/` | `sync.mjs`, миграции, генераторы |
| `docs/CONTRACT-*.md` | контракты: state machine, lease, RBAC, логи действий и аудита |

## Инварианты проекта

- **Аккаунт ≠ Профиль.** Аккаунт — управляемый бот (`/api/accounts`, `accounts-meta.json`).
  Профиль — человек, пользователь панели (`/api/me`, `users.json`). Разные таблицы, права,
  лимиты. Самая частая и дорогая ошибка в обсуждениях требований.
- **Хранилище двойное:** файлы в `server/data/*.json` и Supabase/Postgres. Не предполагать, что
  данные только в одном месте, — смотреть по конкретному модулю.
- **Язык:** комментарии и документация по-русски, содержимое MCP — только по-английски
  (проверяется тестом). Подробности в `mem:conventions`.

## Процесс работы над задачей

Регламент (Jira, ветки, статусы, что нельзя заливать) живёт **не здесь**, а в `CLAUDE.md` и
скиллах Claude Code `.claude/skills/`: `task-workflow`, `verify-changes`, `mcp-descriptors`,
`project-glossary`, `code-navigation`. Не дублировать сюда — расходится.

## Дальше

- стек, версии, чем что собирается: `mem:tech_stack`
- команды запуска и прогона, особенности Windows/PowerShell: `mem:suggested_commands`
- стиль кода, именование, где какой язык, устройство модулей: `mem:conventions`
- что обязательно выполнить, прежде чем считать задачу готовой: `mem:task_completion`
