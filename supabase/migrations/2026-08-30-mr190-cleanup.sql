-- MR-190, часть 1: убрать из боевой базы то, что там быть не должно.
--
-- Аудит в задаче делали 25.08. К 30.08 половина пунктов уже закрыта кем-то другим:
-- тестовых пользователей (usr_a…usr_client) в wallet_log/coin_balance/subscriptions
-- больше нет, дублей каналов по username нет, JSON-колонка setups.modules снята.
-- Здесь — только то, что подтверждено запросами к проду 30.08 и осталось живым.

-- 1. Тестовый набор на витрине подписок.
--    Клиент видит в списке готовых наборов «Тест 2» за $77 — это остаток от проверок.
--    Два других тестовых набора (__TEST_BUNDLE__, ТЕСТ drop uid) уже удалены.
delete from bundle_modules where bundle_id = 'bun_4ac8a270';
delete from bundles where id = 'bun_4ac8a270';

-- 2. Мёртвая таблица: 0 строк и ни одного обращения из кода.
drop table if exists parsed_channels;

-- 3. Две роли-пустышки.
--
--    ВАЖНО: в задаче предложено удалить ВСЕ роли, никому не назначенные. Так делать
--    нельзя. Роль без владельца показывается каждому владельцу как СИСТЕМНЫЙ ШАБЛОН
--    (server/roles.js), и «Оператор», «Sales», «Viewer» — это стартовые шаблоны из кода,
--    а не мусор. Ещё две роли без назначения принадлежат конкретному человеку
--    (usr_3b1ecf53a065) — это его заготовки, трогать чужое мы не вправе.
--
--    Удаляем ровно две автоименованные пустышки: они ничьи, никому не выданы и при этом
--    показываются КАЖДОМУ владельцу в списке шаблонов, засоряя его.
delete from user_types where id in (7, 10);
delete from roles where id in ('role_320e04f5', 'role_73a25402');
