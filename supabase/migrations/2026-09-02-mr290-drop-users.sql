-- MR-290: таблицы `users` и `user_roles` больше нет. Операторы живут в `profiles`.
--
-- ОТКУДА ВЗЯЛОСЬ РАСХОЖДЕНИЕ. Изначально операторы лежали в `public.users` со своим
-- `password_hash` (scrypt). 29.07 вход перевели на Supabase Auth: пароли уехали в
-- `auth.users`, сами операторы — в `profiles`, связь через `profiles.legacy_id`. Миграция
-- `2026-08-03-drop-users-stage4.sql` таблицу дропнула.
--
-- Но на боевой базе она осталась: дроп туда не доехал. Аудит MR-290 нашёл её среди трёх
-- объектов, живущих вне git, и миграция `2026-09-01-mr290-drift.sql` вернула её в цепочку
-- — чтобы схема в репозитории воспроизводила боевую. Формально верно, по сути —
-- законсервировали то, что должно было умереть. Здесь это исправляется.
--
-- ЧТО В НЕЙ БЫЛО НА МОМЕНТ СНОСА (боевая база, 02.09):
--
--   users        — 2 строки: `usr_admin` и `usr_test`, сиды от 15.07
--   profiles     — 89 строк, из них 87 не имеют пары в `users` вовсе
--   user_roles   — 2 строки против 66 в `profile_roles`; не читает никто
--
-- Оба оставшихся e-mail есть в `auth.users`, то есть войти эти двое могут и без таблицы.
--
-- ЧТО ТЕРЯЕТСЯ. Аварийный вход по `AUTH_ALLOW_LEGACY` — он читал `users.password_hash`.
-- Флаг был выключен по умолчанию и страховал ровно эти два аккаунта, которые и так есть
-- в Supabase Auth. Описание рычага убрано из docs/RUNBOOK-auth-profiles.md тем же
-- выпуском, чтобы в инструкции не осталось шага, который больше ничего не делает.
--
-- ПОРЯДОК БЕЗОПАСЕН. Код, читавший таблицу, удалён этим же выпуском. Между накаткой и
-- выкатом работает предыдущая версия, и она обращается к `users` в двух местах:
-- `typesSync` (крон, ошибка проглатывается — он дублировал запись, которая и так идёт в
-- `profiles`) и `authenticate` (только при выставленном `AUTH_ALLOW_LEGACY`). Ни то, ни
-- другое не ломает вход и не теряет данные.

begin;

-- Сначала зависимая таблица: на `users` смотрит её внешний ключ.
drop table if exists public.user_roles;
drop table if exists public.users;

comment on table public.profiles is
  'Операторы платформы. Единственная таблица людей: пароли — в auth.users (Supabase Auth), связь по legacy_id. Таблица public.users снесена в MR-290.';

commit;
