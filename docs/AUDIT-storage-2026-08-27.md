# Аудит хранилищ — MR-186 (27.08.2026)

> Правило владельца с созвона 24.08: пройти по кодовой базе и убедиться, что нет никакого
> другого способа хранения данных, кроме общей базы. Ни памяти процесса, ни файлов, ни JSON,
> ни локального SQLite, ни памяти браузера.
>
> Повод был конкретный: тексты промптов лежали в `localStorage` без имени владельца —
> правка одного человека доставалась всем (MR-185), а шаблон не возвращал текст промпта
> (MR-176). Аудит подтвердил: это было не единственное место.

## Как читать

По каждой находке отвечаем на два вопроса, и они же определяют группу:

- **что будет, если этих данных не станет** — потеря чьей-то работы или ничего;
- **что увидит другой человек, зашедший с того же компьютера / этот же человек с другого**.

Группы: **[переносим]** — место данным в базе · **[оставляем]** — осознанно, с причиной ·
**[удаляем]** — мёртвое или лишнее.

---

## 1. Браузер (localStorage / sessionStorage)

| место | что лежит | группа | что сделали |
|---|---|---|---|
| `mocks/store.ts` | тариф, монеты, задачи, тикеты, прокси, уведомления, **имя и почта** | **[переносим]** | из браузера убрано, осталось только язык/тема/демо-режим |
| `neuro-dialogs/NeuroDialogsModule.tsx` | «отвечать всем», цель диалога, разбор картинок, цель кампании | **[удаляем]** | убрано: поля и так уходят в задачу и восстанавливаются из шаблона |
| `widgets/AppHeader.tsx` | закрытые уведомления | **[оставляем]** | ключ стал именным — у каждого свои |
| `modules/shared/promptDefaults.ts` | старые промпты | **[переносим]** | сделано в MR-185: разовый перенос в базу, локальная копия стирается |
| `pages/ProfilePage.tsx` | аватар | **[оставляем]** | ключ именной; картинка на сервер не грузится |
| `features/billing/plan.ts` | состав подписки | **[оставляем]** | ключ именной, ответ сервера сразу поправляет — иначе меню моргает пустотой |
| `widgets/AppSidebar.tsx` | какие группы меню свёрнуты | **[оставляем]** | вид, а не данные |
| `modules/shared/activeTaskStorage.ts` | какая задача открыта во вкладке | **[оставляем]** | `sessionStorage`, живёт до закрытия вкладки |
| `auth/*`, `api/usersApi.ts`, `AdminStatsPage.tsx` | токен и профиль сессии, вход под клиентом | **[оставляем]** | без этого вход не переживёт перезагрузку |
| `LowBalanceLoginModal.tsx` | «больше не показывать», «уже видел» | **[оставляем]** | поведение поп-апа, не данные |

### Что именно было сломано

**Данные кабинета переживали выход из аккаунта.** `logout` чистит только токен, сессию и
отметку активности — ключ `ai-incubator:v3` оставался. В нём лежали тариф, монеты, задачи,
тикеты, прокси, уведомления и имя с почтой. На общем компьютере следующий человек при
загрузке видел данные предыдущего, пока не придёт ответ сервера; со своего второго
устройства он их, наоборот, не видел вовсе — и решал, что данные пропали.

**Настройки нейродиалогов помнились в браузере.** Тот же эффект в миниатюре: на общем
компьютере доставались следующему, на втором устройстве не находились. При этом все эти
поля и так уходят в настройки задачи и восстанавливаются из шаблона — а шаблоны с 26.08
лежат в общей базе. Второе хранилище было лишним.

**Закрытые уведомления были общими на браузер.** Один закрыл «аккаунт отвалился» — следующий
человек этого предупреждения не видел вовсе.

---

## 2. Сервер: файлы и SQLite

Работает переключатель: включён `DATA_BACKEND=supabase` — пишем в общую базу, иначе в файлы.
Файловый режим нужен для локального запуска и тестов, и сам по себе нарушением не является:
вопрос в том, есть ли у стора ветка базы вообще.

**Ветка базы есть** (26 сторов): пользователи, роли, баланс, журнал токенов, подписки,
промпты, ИИ-настройки, шаблоны модулей, подарки, каналы, цели, лиды, кампании, сообщения,
цены, наборы, чёрный список, активность аккаунтов, группы аккаунтов, агенты, API-ключи,
настройки крона, витрина оплат, кэш парсера, дневные лимиты, кэш trust.

**Ветки базы НЕТ** — файл единственное хранилище:

| стор | что лежит | группа | почему |
|---|---|---|---|
| `proxies.js` | каталог прокси | **[переносим]** | уже заведено отдельной задачей **MR-169** (таблица + UID + уникальность) |
| `tickets.js` | обращения в поддержку и переписка | **[переносим]** | переписка клиента с поддержкой; при втором сервере половина тикетов не видна |
| `workLog.js` | учёт рабочего времени операторов | **[переносим]** | по нему считают смены; файл на одной машине |
| `knowledgeBase.js`, `kbFiles.js` | база знаний цели и файлы к ней | **[переносим]** | контент кампании, вводится руками |
| `targetFolders.js` | папки целей | **[переносим]** | ручная раскладка каналов |
| `automation/store.js` | правила автоматизации | **[переносим]** | расписания запусков |
| `linkTracker.js` | счётчик переходов по ссылке | **[переносим]** | это измеримый результат кампании (§1.3) |

**SQLite** остался в одном месте — `parserCache.js`, и только как запасной путь: при
включённой базе кэш пишется в `parser_cache`. Проверено на боевой: таблица есть и в ней
данные. Пункт 5 задачи (три миграции от 24.08 — кэш парсера, слежение за запросом, витрина
оплат) **закрыт**: таблицы `parser_cache`, `payments` на месте, колонки слежения тоже.

---

## 3. Память процесса

Живёт дольше запроса, но осознанно и с коротким сроком:

| место | что | группа |
|---|---|---|
| `accountActivity.js` — `scheduleHold` | отложенный бросок распорядка, десятки секунд | **[оставляем]** после перезапуска бросок новый — смена и так начинается заново |
| `accountLocks`, `accountBusy` | кто сейчас работает аккаунтом | **[оставляем]** защита от двух действий одним аккаунтом внутри процесса |
| `aiSettings`, `aiSafety`, `trustCache`, `modelPricing` | кэш настроек для синхронного чтения | **[оставляем]** источник правды — база, это только кэш |
| `worker.running` | запущенные задачи | **[оставляем]** воркеры живут в процессе по устройству |

⚠️ Общее для всей группы: **при втором сервере они разъедутся**. Блокировки аккаунтов —
самое опасное: два инстанса не увидят работу друг друга и смогут взять один аккаунт
одновременно. Это не «починить в MR-186», это условие перед горизонтальным масштабированием
(MR-65, нагрузочное тестирование).

---

## 4. Что сделано в этой задаче

1. Собран полный список — выше.
2. По каждому месту записано, что лежит и что будет при потере/втором сервере.
3. Разделено на три группы.
4. Перенесено/убрано: данные кабинета из браузера, настройки нейродиалогов, именной ключ
   уведомлений. Промпты и шаблоны переехали раньше (MR-185, MR-176).
5. Три миграции от 24.08 — уже применены, проверено на боевой базе.
6. Закрыта дорога назад: `server/__tests__/localStorageAudit.test.js` падает, если в
   интерфейсе появляется новое обращение к памяти браузера вне разрешённого списка, и
   отдельно — если настройки модуля снова начнут запоминаться в браузере или если данные
   кабинета вернутся в `localStorage`. Каждый пункт списка обязан иметь человеческую причину.

## 5. Что осталось

Семь серверных сторов из таблицы выше ждут переезда в базу. Каждый — отдельная миграция со
своей схемой и переносом существующих данных; делать их одним заходом означало бы менять половину платформы одним коммитом. Прокси уже заведены как **MR-169**. Порядок предлагаю по
болезненности потери: тикеты → учёт времени → база знаний с файлами → папки целей →
автоматизация → счётчик переходов.
