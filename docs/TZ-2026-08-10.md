# ТЗ / правки от 10.08.2026 (созвон-ревью)

Источник: созвон 10.08 (запись `2026-08-10 13-12-13`). Заказчик прошёлся по локальной сборке.
Приоритет над предыдущими скоупами по перечисленным пунктам. Метка задач в Jira: `tz1008`.

> **Главный вывод:** по МОДУЛЯМ всё «более-менее нормально». Две вещи — **Менеджер аккаунтов** и
> **Дашборд задач** — сейчас работают плохо и это **приоритет −1** (важнее всего): без них нельзя
> подключать пользователей и оплату.

---

## 🔴 P−1. Менеджер аккаунтов — починка + переработка (ACC-MGR)
Сейчас «ни хрена не работает». Протестировать РУКАМИ (не через ИИ).

**Что не работает (баги):**
- Смена/сброс прокси не срабатывает.
- Переходы между аккаунтами не работают.
- Не сохраняется позиция/вкладка при переключении аккаунта (сбрасывает на начальную).
- Проверка (прокси / статус / сессия) не работает.
- Реавторизация — «ничего не происходит».
- Раздел «Прокси» не загружается (пусто, нет сетевых запросов), хотя должен показывать инфо/ошибку прокси.
- Вечный лоудер «Загрузка данных из Telegram…» — висит.

**Переработка карточки аккаунта:**
- Табы переставить ПО ЗНАЧИМОСТИ: важное в начале, второстепенное (Даты, Действия) в конце.
- **Статус (таб) — убрать вообще** (статус уже в шапке профиля: валидный / без спамблока / активен).
- **Даты — в Профиль** (бесполезный отдельный таб).
- **Действия — в Профиль или в верхнюю шапку**.
- **Здоровье — сдвинуть влево + вынести в шапку** (сверхважный критерий, а в шапке его нет).
- **Работа — в верхнюю шапку** (шапка огромная и нефункциональная — имя+3 кнопки+вечный лоудер).
- **Папки — убрать/переименовать в Группы** (непонятно зачем «Папки» здесь; группы бывают только для каналов).
- Использовать огромную фиолетовую шапку: вынести туда статус-кнопки (Проверить/Обновить), здоровье, действия.
- «Усталость 0/15» — пояснить формулировку («15 действий до отдыха»).

**Карточка/оверлей аккаунта — на весь экран** (убрать ограничение ширины).

---

## 🔴 P−1. Дашборд задач — синхронное управление (TASKS-MGR)
Сейчас задачи запускаются АСИНХРОННО «в воздух», без привязки → Стоп не останавливает, очередь не чистится.

- Привязать выполнение к переменной/секвенсу (трекать статус), НЕ запускать несвязанные async-задачи.
- **Единый список** задач (сейчас два независимых).
- Статусы: **в очереди / в ожидании** (когда система запускает N за раз, остальные ждут), **в работе**, **ошибка**, **на паузе**.
- **Play:** при нажатии — loader на этой задаче («стартер крутится»), Pause/Stop заблокированы, пока не исполнится.
- **Pause:** loader на паузе, Play/Stop заблокированы, статус «в процессе остановки/паузы». Нельзя слать миллиард пауз/стопов на одну задачу.
- Задачи должны ВИЗУАЛЬНО показывать статус (не «голые стены»).
- Парсер групп запускается с ошибкой (alert справа снизу) — разобраться.

---

## 🟠 P0. Статус аккаунта = реальная ситуация, «зона риска» (ACC-STATUS)
- НЕ показывать «Активный», когда прокси мёртв/отсутствует.
- Показывать статус РИСКА с **конкретикой: срок + цена риска**. Пример: «Высокая вероятность блокировки в течение недели», «заблокируется через ~N дней». НЕ «повышенный риск» — конкретно: последствие (блокировка) + срок.
- **Разделить**: «нерабочий прокси» ≠ «нерабочий статус» (мёртвый прокси vs статус/здоровье/бан аккаунта). Кэшировать/выводить отдельно.
- Автоматически: проверять здоровье (блокировки) + проверять прокси → писать статус.
- Аккаунт без прокси в Telegram работает, но риск бана высокий → статус обязан предупреждать ЗАРАНЕЕ (а не постфактум, когда уже забанили). Мотив: чтобы пользователи покупали прокси.

---

## 🟠 P0. Пикер «Выбор аккаунтов»: только с прокси + переход к настройкам (ACC-PICK)
- Показывать в основном выборе **только аккаунты С рабочим прокси**; без прокси / с мёртвым — в список «нерабочие» (расширение MR-96).
- Сломанный аккаунт (мёртвый прокси) — строка **кликабельна**: иконка-шестерёнка ⚙️ (не замок 🔒) → клик → переход в **Менеджер аккаунтов → таб «Прокси»** (назначить прокси).
- **Не дублировать** настройки аккаунта по всей панели — они только в Менеджере; здесь только ссылка/переход.

---

## 🟠 P0. Страница «Прокси» (PROXY-UI)
- Объединить блок «Прокси» и «Гео-рекомендации» в ОДИН блок (сейчас два; рекомендации сжирают половину карточки).
- Карточку — на всю доступную ширину (убрать ограничение для этого экрана, как в Дашборде задач).
- Раздел прокси не загружается при открытии — починить.
- Дать возможность пометить/«убить» прокси в UI (сейчас нельзя).

---

## 🟡 P1. Уведомления — расширить (NOTIFY)
- По **статусу задачи** (в работе / ошибка / на паузе / завершена).
- По **статусу аккаунтов** внутри задачи.
- По **пропущенным ЛС** в диалогах (если ИИ не ответила в заданный таймаут → уведомление).
- В модуле (блок запуска) — галочка вкл/выкл получения уведомлений по статусу конкретной задачи.
- Уведомление о новом сообщении → цвет / в колокольчик.
- Админ может подписаться на уведомления.

---

## 🟡 P1. Диалоги / переписки (DIALOGS)
- Любой диалог / комментарий / реакция — **привязан к задаче**; показывать список задач, участвовавших в формировании конкретного диалога/реакции.
- **Бот vs человек**: рядом с названием диалога — большая зелёная плашка «БОТ» (сейчас мелким шрифтом). Понимать, откуда сообщение (аккаунт / бот / группа / канал).
- В группах не видно, что происходило — сделать видимым.

---

## 🟢 P1. Правки модулей 10.08 (MOD-FIXES)
- **Тайминги — Custom:** добавить кнопку «Custom» рядом с Мин/Рекомендуемые/Макс. При выборе Мин/Рек/Макс значения внутри настроек должны СРАЗУ меняться (сейчас БАГ — не меняются). При ручном изменении (± / ввод) — авто-переключение на Custom. При клике «Custom» — принудительно открыть блок «Расширенные настройки». → **доработка MR-103**.
- **«Режим комментирования» vs «Какие посты комментировать»** — непонятна разница, кажется одно и то же → уточнить/переработать (доработка MR-110 / NC-001).
- **«Обязательно»** у блока «Выбор аккаунтов» — добавить метку (как «0 · Обязательно» у «Группы»).
- **Стоимость запуска — расписать инлайн** (без наведения на «?»): «N действий × 0.5 = M токенов», видно сразу, чтобы понимать, сколько платит.
- Иконка «Лимит» в нижней панели — поправить размещение (не попадает никуда / не под своей иконкой).
- **Нижняя панель запуска — доделать** (в процессе): если не хватает места — убрать текстовые подписи, оставить иконки.
- «Запуск по расписанию» — оставить в «Параметрах», серым необязательным.
- Мейлинг: «Защита» после «Получателей» (3-м блоком); структуру получатели/сообщения переделать позже.

---

## 🟢 P2. Навигация / меню (NAV)
- «Поддержка» — вниз, в раздел «Аккаунт» (не отдельным пунктом вверху; и внутри карточки аккаунта убрать).
- Скрыть «Автоматизацию» (пока).
- «Обзор аккаунта» — убрать пред-выделение аккаунта (по умолчанию «Выберите аккаунт»).
- Порядок меню: Обучение — в «Главной»; Обзор аккаунта / Логи — вниз, в «Аккаунт»; Статистика / CRM — серым; Дашборд / Авторизация / Прокси.

---

## 🔵 Процесс / Git (PROC)
- Перед КАЖДЫМ push делать fetch/pull (подтягивать чужие изменения); после изменений — прогонять тесты. (Заметили: PR нет, только push non-stop.)
- Meeting-review задачи держать в **Backlog**, НЕ переводить в Done (чтобы можно было к ним обращаться): «Meeting — Admin Panel Review», «Meeting — Modules Review».
- Проверить группировку/сортировку аккаунтов в «Выборе аккаунтов» и «Менеджере» (одинаково ли: по странам, потом по имени).
- Логи — протестировать: включают ли действия аккаунтов + панели.

---

### Расписание
Промежуточный созвон — среда, 14–15:00. Полный — пятница.
На этой неделе подключаем оплату + начинаем авторизацию/регистрацию аккаунтов (SIM с Андреем).
