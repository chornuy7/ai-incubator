# TASKS — созвон 27.07.2026 (продакшен + коммерциализация)

> Источник: [meeting-2026-07-27.md](./meeting-2026-07-27.md) · требования: [TZ §10](./TZ-2026-07-10.md#10-дополнения-от-27072026).
> Статусы: `☐` не начато · `◐` в работе · `☑` готово · `🔒` ждёт ответа бизнеса.
> Дедлайн деплоя — **сегодня**. Обязательный минимум недели 1 — **4 пункта** (админка · цены · управление аккаунтами · всё в БД).
> Чек-ин — **среда**.

## 🎯 Неделя 1 — обязательно 100% (§10.10)

### БЛОК A. Supabase — БД/Auth/Storage (§10.2) `[БД и адаптер — ГОТОВО; auth/storage — далее]`
- ☐ Создать проект на supabase.com + подключить **Supabase MCP** к рабочему AI (доступ + удалённое управление).
- ☐ Спроектировать схему в Postgres: `users`, `admins`, `roles`, `role_permissions`, `subscriptions`,
  `module_prices`, `token_balance`, `token_ledger`, `nested_users`; парсер-результаты/аккаунты.
- ☑ Мигрировать JSON-стор → Supabase: схема (14 таблиц), скрипт migrate-to-supabase, данные залиты.
- ☐ Auth: логин e-mail/пароль + **OTP** + **капча** на регистрацию/логин; OAuth 2.0 (позже).
- ☐ Storage: bucket только под файлы (картинки/видео/аудио); текст — в таблицы.
- ☐ Оплатить проект после запуска (иначе пауза через 2 недели неактивности).
- ☑ **Всё из БД**: адаптер сторов (balance/prices/bundles/apiKeys/users/roles/tokenLedger/goals/campaigns/leads/accounts) — переключатель DATA_BACKEND=supabase. Приложение проверено на живой БД.

### БЛОК B. Биллинг и токены (§10.1)
- ☐ Починить **трекинг токенов** на странице задачи (сейчас считает неверно).
- ☑ **Estimate перед запуском**: стоимость (была) + ВРЕМЯ (min–max) из аккаунтов×задержки×действий. LaunchCost.
- ◐ **Токен → доллар**: поле «цена токена» и «картинка ×N» в админке (priceStore), значение **[❓ у Николая]**.
- ☐ Поведение при нехватке баланса:
  - ☑ кнопки не блокировать — проверено: кнопки запуска не гаснут по балансу, сервер отдаёт 402 → client.ts открывает модалку пополнения; лента/модалка входа готовы;
  - ☑ постоянная **лента сверху** (full-width, без крестика, клик → биллинг) — LowBalanceBar;
  - ☑ модалка «баланс кончается» на входе; для админов чекбокс «не показывать» — LowBalanceLoginModal.
- ☐ Подключить платёжную систему (с Андреем).

### БЛОК C. Админка — юзеры/цены/доступы (§10.4)
- ☐ Список юзеров → карточка: доступные модули · токены (есть/списано) · деньги ($) · купленные модули.
- ◐ Из админки **вкл/выкл модуль** юзеру (готово, раздел «Доступ к модулям» в карточке); блок внутри модуля — TODO.
- ☑ Управление ценой модуля из админки: **2 цены** (доступ $/мес + действие ⚡) — вкладка «Цены», priceStore (из данных, не из кода).
- ☑ **Скидки**: годовая «−X%» из админки (единый ANNUAL_DISCOUNT), авто-пересчёт.
- ☐ **Роли** создаются только из админки (sudo); роль на карточке юзера; админские роли.
- ☐ **Вложенные юзеры** (суб-юзеры под своим админом) — видно и управляемо из админки.
- ☐ Цена **чтения картинки** — в админку (§10.5).

### БЛОК D. Управление аккаунтами (§10.10)
- ☐ Свести управление аккаунтами под админку (поверх текущего Менеджера профилей).

### БЛОК E. Деплой (сегодня)
- ☐ Задеплоить сайт как есть → ссылка заказчику + Андрею + доступ в админку.
- ☐ Авто-деплой по пушу (GitHub Actions) — из §9.14, доделать.

## 🔌 API/MCP для «мозгов» (§10.3) `[параллельно, высокий приоритет]`
- ☑ **API по закрытому ключу** (Bearer aii_live_sk_…): создать цель/кампанию/задачу — /api/v1, ключи в профиле.
- ☑ **API/MCP «что умеет модуль»**: GET /api/v1/capabilities + /api/v1/mcp (манифест инструментов).
- ☑ **API-доступ к модулю**: POST /api/v1/modules/:key/run + /estimate (стоимость+время). Docs: docs/API-v1.md.

## 🖼️ Анализ картинок (§10.5)
- ☐ Вытягивать и **анализировать** картинку (vision, контекст канала); генерация — нет.
- ☐ Чекбокс «анализировать изображения» в модуле; цена ~×4, зависит от числа картинок.

## 🌐 Лендинг (§10.6)
- ◐ Два варианта подачи через `?v=` (platform/marketing); карусель картинок — TODO.
- ☑ Два позиционирования через **GET-параметр** `?v=marketing` (SEO).
- ☐ Позиционирование: автоматический конвейер/маркетинг-сервис; одно слово вместо трёх.
- ☑ «Подробнее» у модулей → /module/:key (ModuleLandingPage); «комбайн» убран.
- ☐ Готовые наборы как результат + **калькулятор**; убрать «комбайн».
- ◐ Отзывы/кейсы — переработать: отзывы ☑ (7 шт., автокарусель scroll-snap с точками, пауза при наведении); кейсы — TODO.

## 📚 Learning + документация (§10.7)
- ◐ Страница **«Обучение» (Learning)** — вся инфо по работе, собрана из доков Help Center (/panel/learning); скрины — TODO.
- ☑ Дока **собирается из HELP_DOCS** (один источник: правишь описание — обновляется и в «?», и в Обучении).

## 🧩 Прочее
- ☐ Название: закрепить Мурмекс / Мурмексграм (домены). **[❓]**
- ☐ SIM/безопасность: сворачивать закупку при малейшем риске, эскалация заказчику/Андрею (§10.9).

## 🔒 Ждёт ответа бизнеса
- Модель под капотом (GPT-4/4.5) · цена за токен/картинку · стратегия миграции JSON→Supabase ·
  Мейлинг Supabase vs наш модуль (§8.4/§9.11) · финальное имя.

## 📅 Недели 2–3 (после недели 1)
- ☐ Фокус модуль-за-модулем: один модуль до 100% (вариации/ошибки/всплывашки) → раскатать паттерн (~80%) → блоки.
- ☐ Админка: статистика/мониторинг (нагрузка, запросы, аккаунты активные/неактивные/падающие + почему).
- ☐ Парсинг: протестировать (работает 70–99%, оставить как есть, вернуться позже).
- ☐ Тест оплаты и модулей глазами юзера (фокус-группа), все user-кейсы.
