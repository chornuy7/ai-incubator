# Роадмап по созвону 17.07.2026 (демо + правки)

> Источник: запись созвона `2026-07-17 18-05-50.srt` (кросс-тест Ильи и Николая + заказчик).
> Формат: замечание → почему → где в коде (по индекс-карте) → статус.
> Статусы: `☐` не начато · `◐` в работе · `☑` готово (этой сессией/Николаем) · `🧠` требует архитектурного решения.

---

## 0. ГЛАВНОЕ: архитектура оркестрации (🧠 «залезть в мозги клоду»)

Заказчик прямым текстом: *«цели → компании → задачи, прям как уровни; автоматизация под компанию или под задачу; нельзя запустить задачу к цели напрямую»*. Текущая имплементация «кривая, одно ломает другое». Фиксируем целевую модель — на неё завязано ~половина правок ниже.

```
ЦЕЛЬ (солнце)        — стратегия: что и к какому сроку. Оркестрирует кампании.
  └─ КАМПАНИЯ (небо)  — исполнение под целью: закреплённые аккаунты + НАСТРОЕННЫЙ модуль (пресет).
       └─ ЗАДАЧА (земля) — один запуск модуля под кампанией. Ручные задачи — тоже сюда.
            └─ МОДУЛЬ  — инструмент. Настраивается пресетом.
  АВТОМАТИЗАЦИЯ        — планировщик. Крепится к КАМПАНИИ или к ЗАДАЧЕ. Не к «голому» модулю.
```

**Правила модели:**
1. Задачу **нельзя** запустить «к цели» напрямую — только через кампанию (сейчас в модуле есть «выбор цели» — заменить на «выбор кампании»).
2. Кампания **обязана настраивать модуль** (через пресет), а не только выбирать аккаунты. Сейчас кампания только аккаунты берёт — «нафиг нужна неконтролируемая кампания».
3. Автоматизация **не существует сама по себе**: только под настроенную кампанию или под задачу (внутри модуля). Нельзя автоматизировать ненастроенный модуль.
4. AI-промты/этапы/база знаний — берутся из **ЦЕЛИ** (кампания их наследует).
5. Аккаунт «плавает» в общем пуле ИЛИ закреплён за кампанией (не за «ролью»). Закрепление = замочек + имя кампании.

**Файлы модели:** `server/goals.js`, `server/lib/campaign.js`, `server/campaignsRoutes.js`, `server/campaignSchedules.js`, `server/modules/registry.js`, `server/modules/workers.js`, `server/automation/*`, `server/leads.js`; фронт — `GoalsPage.tsx`, `CampaignPage.tsx`, `AutomationPage.tsx`, `TasksPage.tsx`, `LiveModule.tsx`, `AccountsPage.tsx`.

---

## 1. Резервация «роль» → «кампания» + замочек (🧠 ядро модели)

- ☑ Уже сделано ранее: аккаунт на паузе показывает модуль/«вручную».
- ☑ В менеджере колонка **«Кампания»** вместо «Роль» — показывает, под какой кампанией работает аккаунт (или «в общем пуле»). Фильтр тоже по кампаниям (+ «в общем пуле»). NB: `accountsMeta.role` не мигрировали — связь берётся из самой кампании (`pinnedMap`), чужой файл не тронут. Проверено LIVE.
- ☑ **Замочек** рядом с именем кампании: закреплён → 🔒 + имя (подсказка «вышел из общего пула»), используется без лока → открытый замок. Проверено LIVE.
- ☑ Присвоение аккаунта кампании: галочка «присвоить с закреплением» = лок, без неё — «использовать без лока». Проверено LIVE (после снятия галочки `pinned:false`, аккаунт остаётся доступен другим).
- ☑ Смена кампании одному аккаунту — пункт меню строки «Кампания аккаунта» → дропдаун со списком кампаний и опцией «вернуть в общий пул». Аккаунт автоматически убирается из прежней кампании, bulk-переезд не задействован. Проверено LIVE.

## 2. Менеджер аккаунтов (Lane A)

- ☑ Меню статусов (активные/в работе/прогрев) + скрытые кнопки запуск/пауза/стоп (Николай).
- ☑ **Чекбокс-заголовок 3 состояния** (`TriStateCheckbox`, indeterminate через ref): клик по semi снимает весь выбор, подсказка объясняет поведение. Кнопка «Снять» убрана. Проверено LIVE (indeterminate=true → клик → выбор пуст).
- ☑ «**В корзину**» перенесена в крайний правый край панели (`ml-auto`) — на место бывшей «Снять». Проверено LIVE.
- ◐ Кнопка «**Управление**» добавлена в панель массовых действий (открывает обзор выбранного). Полноценный мульти-просмотр — в §3 вместе с переводом обзора во вьюшку.
- ☑ **Запрет удаления аккаунтов «в работе»**: занятые/в прогреве не уходят в корзину — понятный отказ «Сначала остановите работу», на кнопке видно «(N в работе)». Проверено LIVE: тост показан, аккаунт остался в списке (баг воспроизводился — теперь нет).
- ◐ Корзина: «**Восстановить выбранные**» — массовое восстановление с подтверждением. Код готов и типизирован, но LIVE не прогнан: корзина пуста, а положить туда аккаунт мешает (правильно) новый guard «в работе».
- ◐ Фильтр в корзине «**только живые**» (отсекает invalid/reauth). Код готов; LIVE не прогнан по той же причине — пустая корзина.
- ☑ **Score виден всегда** (раньше — только у проблемных), цвет по банду + подсказка, где можно/нельзя: <40 «нужен прогрев, мейлинг недоступен», 40–70 «только Консервативный, мейлинг недоступен», >70 «все модули, включая мейлинг». Проверено LIVE (Trust 75 → «доступны все модули»).

## 3. Обзор аккаунта = вьюшка (не модалка), как почта/WhatsApp (Lane A/B)

- ☐ Открывать управление аккаунтом **панелью/страницей**, не модалкой поверх (перегруз).
- ☐ Слева — **колонка-список аккаунтов** (с фильтром), справа — табы (профиль/прокси/каналы/статус/действия).
- ☐ **Таб сохраняется** при переключении аккаунтов (централизованное состояние табов) + чекбокс «сбрасывать на профиль».
- ☐ Мульти-просмотр (выбрал N → «Управление»): раскладка как почта — список слева, детали+табы справа.
- ☐ Каналы/чаты **не стакаются** между аккаунтами — у каждого только свои (сейчас суммируются — баг) (`accountStats.js`, `AccountManagementModal.tsx`).
- ☐ **Превью картинок/видео**: не хранить оригинал; тянуть из Telegram по запросу и показывать, либо хранить ужатую копию ≤5 КБ (100×200, урезанная палитра). Возможно — только ссылки/подгрузка on-demand из сессии (`neuroDialogs/*`, `accountStats.js`).
- ☐ Переключение диалогов/групп **внутри блока**, не снаружи; список с фильтром; убрать стакинг.

## 4. Цели (🧠 + Lane B)

- ☑ Каналы/группы цели через FolderPicker (эта сессия).
- ◐ **Дедлайн цели** (опционально): поле-дата в форме + бейдж на карточке («до DD.MM» / «истёк»). Backend `goals.js` (поле `deadline` + чистая `isGoalExpired`, конец дня включительно), зеркало на фронте. **Запуск новых задач под просроченную цель заблокирован** (POST /tasks → 409 `goalExpired`). Проверено LIVE (409 «цель просрочена — работа остановлена»). Осталось: останавливать УЖЕ идущие задачи по истечении (Lane A — оркестрация/воркеры).
- ☑ **Цель по лидам** (`leadTarget`): поле в форме + прогресс-бар на карточке (лидов к цели N/цель, считается из CRM по `goalId`). Проверено LIVE (2/5).
- ☐ **Время выполнения** цели (опционально) — покрывается дедлайном; отдельная длительность при необходимости.
- ☑ **База знаний с файлами**: загрузка картинок/документов в КБ цели (data-URL, до 3 МБ, белый список типов), превью картинок и ссылка «открыть файл», удаление записи убирает файл с диска. Backend `server/kbFiles.js` + роуты upload/отдача; защита от обхода каталога. Проверено LIVE + 5 тестов.
- ☑ Создание/редактирование цели — **полноэкранная вьюшка**, не модалка: список скрывается, форма занимает страницу, кнопка «← Назад к целям», действия «Отмена/Сохранить» внизу. Проверено LIVE (создание и редактирование, оверлея модалки нет, возврат к списку работает).
- ☑ **Список кампаний** внутри/под целью: на карточке цели блок «Кампании цели (N)» со статусной точкой и переходом. Проверено LIVE.

## 5. Кампании (🧠 ядро — «сырое, переделать»)

- ☑ **Список кампаний** + «Создать кампанию» → полноэкранная вьюшка с «← Назад» (не модалка). Проверено LIVE.
- ☑ Кампания принадлежит цели и **обязана настраивать модуль** — создание без `moduleKey` отклоняется (`server/campaigns.js`). Проверено LIVE.
- ☑ **Выбор аккаунтов — 3 режима**: «Числом из пула» · «Выбрать вручную» · «Группой (папкой)». Свободные считаются с учётом чужих закреплений; у группы видно «N/M свободны». Выбранных вручную можно сохранить как группу прямо из формы. Проверено LIVE.
- ☑ Закреплённые аккаунты **выходят из общего пула**: закрепление живёт в самой кампании (персистентно, переживает рестарт), попытка забрать чужой закреплённый → 409; `pinned:false` = «использовать без лока»; завершённая кампания и удаление освобождают. Проверено LIVE. NB: миграция `accountsMeta.role` не понадобилась — привязка хранится в кампании.

## 6. Автоматизация (🧠 — «полный бред, переделать»)

- ☑ Все модули в списке (вкл. парсинг) + FolderPicker (эта сессия).
- ☑ Правило открывается **вьюшкой** (не модалка): «← Назад к автоматизации», свой заголовок, действия внизу карточки. Проверено LIVE (оверлея нет, возврат работает).
- ☑ Внутри — **выбор кампании** в `AutomationPage` (модуль и аккаунты берутся из неё; настройки правила перекрывают пресет). Проверено LIVE.
- ☑ Автоматизация крепится **к кампании** (правило `campaignId`): модуль/аккаунты/пресет/цель берутся из неё; без кампании и без модуля → 400. Проверено LIVE + тесты `resolveRuleTarget`.
- ☑ Настройка автоматизации **прямо в модуле** (блок «Запуск»): сворачиваемый «Запуск по расписанию» — однократно / ежедневно / каждые N минут; правило забирает текущие настройки модуля и выбранную кампанию. Проверено LIVE (блок и режимы рендерятся).
- ☑ Задача автоматизации узнаваема в Дашборде: планировщик ставит `initiator = «Автоматизация: <имя правила>»` и `goalId` из кампании (раньше задачи были безымянными).

## 7. Модули — порядок и запуск (Lane B)

- ☑ Убраны «Остановить»/прогресс из «Выполнение», всегда «Начать» (эта сессия).
- ☑ **AI-промты подняты наверх** — сразу под выбор аккаунтов (основа модуля). Эмодзи-палитра тоже вверх. Проверено LIVE (`LiveModule.tsx`).
- ☑ **Тайминги/задержки — вниз** (после блока «Запуск», меняются редко). Проверено LIVE.
- ☑ Блок «**История сообщений**» убран. Результаты остаются только для парсера/GGR (фактический вывод задачи); логи выполнения — в Дашборде задач.
- ☑ «**Чёрный список каналов**» — компактным сворачиваемым блоком внутри секции каналов (`BlacklistEditor` prop `compact`). Проверено LIVE (разворачивается по клику).
- ☑ **Пресеты модуля**: сохранение через модалку с **цветовой меткой** (6 цветов) и **владельцем** (персональные — Маша/Паша). Чип пресета в «Запуске» показывает цветную полосу+точку и бейдж владельца. Backend хранит `color`/`owner` (`server/modules/routes.js`), тип `ModulePreset` расширен (`modulesApi.ts`), UI — `SavePresetModal`+`LaunchPanel`. Проверено LIVE (сохранение из UI → чип с цветом «Голубой» + «Паша»). Осталось (не критично): автоматизация = расписание — это §6.
- ☑ В модуле «выбор цели» → «**выбор кампании**»; при выбранной кампании цель наследуется от неё, прямой выбор цели скрыт. Мягкая миграция: пока кампаний нет — прямой выбор цели работает. Проверено LIVE.
- ☑ **«Выполнение» → нижняя прижатая панель** (`sticky bottom-0`, всегда видна): статус/прогресс текущей задачи + предупреждение + прыжок в Дашборд **с фильтром по этому модулю** (`?module=`). Проверено LIVE (панель закреплена у низа вьюпорта при скролле). Полный блок «Запуск» остаётся выше; отдельная панель — быстрый доступ.
- ☑ Кнопка «В Дашборде задач» → открыть Дашборд **отфильтрованным по модулю** (не одну задачу) (`LiveModule.tsx` → `TasksPage.tsx` через query-параметр `?module=`). Сделано: ссылка передаёт `?module=<key>`, дашборд предфильтрует (опция добавляется, даже если задач модуля ещё нет). Проверено LIVE.
- ☑ **Проверка уникальности задачи перед запуском**: POST `/:moduleKey/tasks` не создаёт вторую **активную** задачу с той же подписью (аккаунты + цель + цели/каналы) — возвращает 409 с id дубля (обход флагом `allowDuplicate`). Чистые хелперы `server/lib/taskDedup.js` (`taskSignature`/`findDuplicateActiveTask`) + тесты. Проверено LIVE (повторный запуск → 409 «идентичная задача уже на паузе»).

## 8. Дашборд задач → вьюшка задачи (Lane B)

- ☑ Массовые старт/пауза/стоп + прогресс + фильтр по целям (эта сессия).
- ☑ Заход в задачу — **отдельная вьюшка** `/panel/tasks/:id?m=<moduleKey>` (не модалка). Кнопка «← Назад к задачам», заголовок = модуль + номер + инициатор. Управление (пауза/стоп/продолжить/перезапуск) прямо во вьюшке, живые логи (опрос 3с). `TaskDetailModal` заменён на `TaskDetailPage` (экспорт из `TasksPage.tsx`), роут в `App.tsx`. Проверено LIVE (клик из списка → вьюшка → назад). Заодно `moduleTitle` теперь знает mailing/autoposting.
- ☑ Во вьюшке — полные логи + **результаты**. Для **AIR** — понятный рейтинг по каждому аккаунту (аккаунт · балл со шкалой и цветом · статус «валиден», сортировка по баллу), для остальных — общая таблица. Проверено LIVE (балл 75 → зелёная шкала 75%).
- ☐ **Редактирование задачи** во вьюшке (сейчас нет).
- ☑ Починена путаница «**цель**» vs «**каналы цели**» в деталях: «Цель кампании» (Goal) и «Каналов / чатов» + блок «Каналы / чаты — где работает модуль». Проверено LIVE.
- ☑ AIR: понятный **рейтинг по каждому аккаунту** во вьюшке задачи (шкала + цвет по баллу, статус бейджем) вместо сырой строки. Проверено LIVE.

## 9. НейроДиалоги / Лиды / CRM (Lane B)

- ☑ Добавить **цель** в НейроДиалоги: селектор цели кампании (`goalId`) в блоке ИИ-настроек. Диалог привязывается к цели (наследует этапы/базу знаний, лиды идут к этой цели), выбор персистится в localStorage. Проверено LIVE (выбор цели → тег + сохранение).
- ☑ **Статусы лида** — воронка прогрева: холодный → только написал → прогретый → заинтересованный → горячий (+ терминальные цель/закрыт). Горячий = **мгновенный алерт**: тост «🔥 Горячий лид!» при переходе + постоянный баннер и подсветка карточки, пока есть горячие. Легаси-статус `answered` мигрирует в `warm`. Backend `leads.js` (LEAD_STATUSES/leadPriority/ACTIVE), `leadsApi.ts`, `LeadsPage.tsx`, `AnalyticsPage.tsx`; тесты обновлены. Проверено LIVE (contacted→hot: тост+баннер+isHot).
  - Попутно исправлен баг: `fetchLeads` слал `?goalId=undefined` → бэкенд фильтровал всех лидов в ноль, список CRM всегда был пустым. Теперь пустые фильтры отбрасываются.
- ◐ Лиды **автоматически** попадают в CRM: upsert-эндпоинт `POST /api/leads/upsert` (по goalId+peer, статус только вперёд по воронке — `advanceLeadStatus`, терминальные не откатываются) + вызов из НейроДиалогов при ручном ответе под целью (fire-and-forget, статус `contacted`). Backend+логика проверены LIVE (curl: new→contacted, →warm, попытка cold остаётся warm) и юнит-тестами (164/164). Осталось: авто-апдейт из воркера авто-ответчика (Lane A).
- ◐ **Редактор сообщений** в НейроДиалогах: панель форматирования над полем ответа (жирный/курсив/ссылка, обёртка выделения в Markdown) + backend `sendDialogMessage` шлёт с `parseMode: md` (фолбек на плейн). Логика форматирования проверена (зеркало LIVE-проверенного MessageComposer); панель рендерится в открытом диалоге (нужна живая сессия). Осталось: редактор в обзоре аккаунта (Lane A).

## 10. Прокси (Lane A) — частично сделано

- ☑ Реальная проверка (TCP+гео выхода), авто-статус/страна, карточка деталей (эта сессия).
- ☐ **Разобраться с «выход»** (зелёная метка exit-гео — объяснить/переименовать понятнее) (`ProxiesPage.tsx`, `proxies.js` — уже exit geo).
- ☐ В «сменить прокси» и при добавлении аккаунта — **выбор из уже существующих** прокси (мобильные + новые) (*прежний запрос заказчика*).
- ☐ Новый прокси на новый аккаунт → аккаунт получает **имя** и прокси **попадает в базу прокси** (`AddAccountWizard.tsx`, `ProxyPoolModal.tsx`, `proxies.js`).
- ☐ Больше настроек прокси (под аккаунт).

## 11. Mailing / Autoposting (Lane B) — сырые

- ◐ Mailing: **медиа (фото/видео/ссылки) + редактор сообщения** с Telegram-форматированием (жирный/курсив/код/ссылка) — общий компонент `MessageComposer`; **единый отправитель** (создаёт только админ). Backend `runMailing` шлёт текст+медиа (`sendComposedMessage`: файлы с подписью, ссылки в текст, Markdown с фолбеком). **Паритет настроек**: уровень защиты + пресет задержек (множители пауз, уже потреблялись бэкендом — теперь есть в UI). Проверено LIVE (тулбар форматирует, медиа-чип с превью, сегменты защиты/задержек). Осталось: конвертация телефон↔username, папки для номеров.
- ◐ Autoposting: **медиа + текст-редактор** (тот же `MessageComposer`), создаёт только админ. Проверено LIVE. Осталось: запланированные посты, автоматизация, редактирование ботов.

## 12. RBAC / роли и группы (Lane B — Николай)

- ☑ Гейтинг по блокам модуля + шаблоны ролей + Help по блокам (Николай).
- ☑ **Группировка аккаунтов**: сущность групп (`server/accountGroups.js`, API `/api/account-groups`), ресурс прав `accountGroups` в `roles.js` + доступ через группу на фронте (`isAccountAllowed`, точечный deny сильнее). Группы создаются из формы кампании. Проверено LIVE + 5 тестов. Матрица групп в `RolesPage` готова: каталог RBAC отдаёт группы с числом аккаунтов, тумблер «Доступ/Убрать» сохраняется в роль (проверено LIVE: «Оператор → accountGroups: {grp_...: allow}»). NB: `accountsMeta.js` не тронут — группа сама держит список аккаунтов.
- ☑ Каналы — доступ через папки: модель `folderChannels` (folderId → выданные каналы), выбор каналов папки в матрице ролей и реальное применение в `FolderPicker` (пусто = все каналы папки). Было сделано ранее — сверено по коду.
- ◐ **Защита прогрева**: массовый стоп в Дашборде задач защищён — прогрев останавливает только супер-админ (иначе отказ с пояснением), объём ≥100 требует **второго барьера** (ввод числа задач «прочитал и уверен»), в подтверждении явно пишем, сколько из них прогрева и что прогресс будет потерян. Правила и пороги — в `server/lib/safetyLimits.js` (`MASS_ACTION`, чистые `massStopConfirmSteps`/`canStopWarming`/`containsWarming`) + зеркало на фронте; 3 теста. Осталось: те же барьеры в менеджере аккаунтов (`AccountsPage.tsx` — дорожка Ильи) и красная пометка настройки в «Ролях».

## 13. Help Center (Lane B) — частично сделано

- ☑ Сайдбар (сужает страницу), убрано «ИИ активен» (эта сессия).
- ◐ **Автоскролл к блоку** при открытии помощи по блоку модуля (Николай начал; доводим) (`HelpCenterDrawer.tsx`).

## 14. Инфраструктура

- ◐ **Деплой**: Илья залил артефакты — раздача фронта из Express, systemd-юнит, nginx-конфиг, `deploy/deploy.sh`, `docs/DEPLOY.md`. Осталось: авто-деплой по пушу (GitHub Actions) — сейчас деплой ручной командой.

---

## Порядок работ (предложение)

**Фаза A — фундамент модели (🧠, разблокирует ~половину):**
1. Утвердить схему §0. 2. Резерв→Кампания + замочек (§1). 3. Кампания настраивает модуль + 3 режима выбора аккаунтов (§5). 4. Автоматизация под кампанию/задачу + вьюшка (§6). 5. Модуль: «выбор кампании» вместо «цели», проверка уникальности задачи (§7).

**Фаза B — вьюшки вместо модалок:**
6. Задача → страница `/panel/tasks/:id` (§8). 7. Цель/кампания создание/редактирование — вьюшки (§4,5). 8. Обзор аккаунта — панель почта/WhatsApp с сохранением табов (§3).

**Фаза C — UX модулей и менеджера:**
9. AI-промты вверх, история/чёрный список вниз/убрать, пресеты цветные (§7). 10. «Выполнение» — нижняя панель + фильтр в дашборд (§7). 11. Чекбокс 3-состояния, корзина/управление, запрет удаления «в работе» (§2).

**Фаза D — данные и защита:**
12. Score аккаунта + ограничения (§2). 13. Дедлайн/лиды/файлы КБ у цели (§4). 14. Лид-статусы + алерты + CRM-автозаполнение (§9). 15. Защита прогрева super-admin + лимиты массстопа (§12).

**Фаза E — модули-контент:** 16. Прокси-выбор + имя аккаунта (§10). 17. Mailing/Autoposting медиа+редактор (§11). 18. AIR/парсинг детальные результаты во вьюшке (§8). 19. Превью картинок on-demand (§3). 20. CI/CD (§14).
