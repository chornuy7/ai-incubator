# Задачи по ТЗ 04.08.2026 — сверка «сделано / нет»

> Источник: [TZ-2026-08-04.md](./TZ-2026-08-04.md) + [ROADMAP-2026-08-04.md](./ROADMAP-2026-08-04.md).
> Статусы: ✅ есть · ◐ частично (нужна доработка) · ☐ новое · 🔒 внешнее/процесс.
> Свод учитывает работу, сделанную до 04.08 (профили, баланс $, логи, мониторинг и т.д.).

## 1. Организация работы (Jira, статусы) — 🔒 процесс
Jira, статусы Backlog…Done, AI создаёт → AI Testing, ручной перевод в Done, ограниченное
число In Progress. Это процесс, не код. **Правило принято к работе.**

## 2. Сайт и страницы модулей
- 2.1 Одноколоночная страница модуля на всю ширину; «Как работает»+«Возможности» в 2 колонки; одинаковый блок оплаты; 2–3 блока оплаты по длине — ☐ **новое** (сейчас другая вёрстка).
- 2.2 Состояния оплаты: гость/без подписки → оплата; подписчик → апгрейд (срок/пакет/токены). Годовая скидка из админки (15%) — ◐: скидка из админки ✅; **апгрейд-состояние — ☐ новое**; «Кабинет вместо Войти» ✅.

## 3. Подписки, токены, цены
- 3.1 Подарочные токены: отдельное поле на модуль/тариф, суммируются, из админки — ☐ **новое**.
- 3.2 Калькулятор: себестоимость подарочных токенов + число действий за них по модулю — ☐ **новое** (цены действий ✅ есть).
- 3.3 Экономика: доходы/расходы/маржа, пересчёт в реальном времени, по серверам — ☐ **новое (крупное)**.
- 3.4 Источник покупки в истории: внешняя платёжка vs внутренний кошелёк, колонка `Source`/цвет — ◐: «Деньги $ / Токены / Планы» и «$ в Базе оплат» ✅; **различение external-payment vs wallet — ☐ доработка**.

## 4. Пользователи и субпользователи
- Инфо одной колонкой + выравнивание — ☐ новое (вёрстка карточки).
- «Пополнить кошелёк» → «Пополнить токены» где начисляются токены — ☐ мелкое.
- Ручное начисление токенов (в т.ч. тест) — ✅ (кнопка «+», суффикс ⚡).
- Полная инфа + история действий по юзеру — ✅ (журнал «все действия»).
- Уникальный ID — ✅ (`usr_…` / profiles). Поиск по имени/email — ✅; **по ID — ☐ добавить**.
- Блокировка пользователя — ✅ («Отключить» закрывает вход+сессию).
- 4.1 owner-ссылка на владельца, sync статусов правилами БД, субюзер видит только оплаченные владельцем модули, роли субов → в user-панель — ◐: **owner/profiles + вложенность ✅**; правила доступа субов и перенос ролей в user-панель — ☐ новое.
- 4.2 Баланс суба: общий владельца / индивидуальный лимит токенов при добавлении + калькулятор действий — ☐ **новое**.

## 5. Админ-панель
- 5.1 Статистика: отмена прошлого запроса при смене периода, loading-состояние, кэш периодов, оптимизация Supabase-связей — ◐: **индексы/оптимизация БД ✅**; отмена/loading/кэш на фронте — ☐ новое.
- 5.2 Автообновление 10–30с в мониторинге и задачах; объединить отчёты; монитор = техраздел; причина ошибки в «Проблемах»; кликабельные карточки аккаунтов — ◐: **мониторинг RPS/CPU+авто-3с ✅**; остальное — ☐ новое.
- 5.3 Один переключатель доступа вместо двух галочек; показывать клиента+модули; вход под клиентом; внутренние роли клиента → в user-панель — ☐ **новое** (вход под клиентом частично).
- 5.4 Убрать из админки распределение аккаунтов; ресурсы/группы/распределение → в рабочую зону клиента — ☐ **новое**.

## 6. Парсер и база каналов (в осн. зона Ильи)
Сохранять парсинг в свою БД, отдавать кэш, дата обновления, ревизия раз в неделю,
сервисные аккаунты, фоновый парсинг, нейтральное имя «Парсер», сессии парсера в админку,
массовый импорт сессий — ☐ **новое (крупный блок)**.

## 7. Логи, статистика, безопасность
- Логи действий в БД; админ видит все; user-админ видит свои+менеджеров — ◐: **логи в БД + «все действия» ✅**; разрез «свои+менеджеры» в user-панели — ☐ новое.
- Группировать однотипные действия по модулям; `Total` действий+токенов на модуль — ☐ новое.
- Обзор аккаунта: только один аккаунт; не хранить тут переписки TG — ☐ новое.

## 8. Поддержка и Help Center
Тикеты интегрировать с админкой; создание/ответ/история; быстрый виджет справа снизу;
кнопку Help Center вниз; не открывать поверх открытого; починить навигацию/категории —
☐ **новое**.

## 9. Обучение и документация
- Пошаговый формат по каждому модулю; отдельные изображения блоков (не один скрин); HQ;
  **увеличение по клику** — ◐: **зум по клику ✅** (лайтбокс), витрина модуля ✅; полный
  пошаговый разбор каждого модуля с картинками блоков — ☐ новое.

## 10. Пользовательская панель и навигация
Скрыть нерабочие разделы (Аналитика/Цели/Агенты/Компании) без удаления кода; скрыть
«Создать компанию»; сгруппировать боковое меню; без адаптива; проверить на ~1400px;
**два окружения dev/prod** — ☐ **новое**.

## 11. Интерфейс запуска модуля
Блокировать запуск до заполнения обязательных; сообщать чего не хватает; разделить
обязательное/необязательное; пошаговый roadmap запуска; нижняя панель на всю ширину;
убрать неработающую «Готово»; убрать дубли кнопок; тайминги до запуска; переименовать
непонятные поля — ☐ **новое (крупный UX-блок)**.

## 12. Папки, цели, чёрный список
Починить добавление/удаление в папках; убрать лишнее «Сохранить в папку»; починить ЧС;
перед запуском показывать выбранный аккаунт и число целей — ☐ **новое**.

## 13. Распределение процентов
Не давать сумму >100%; ограничение в поле; починить ведущий ноль; авто-распределение
100% поровну; кнопка-замок; перераспределение остатка — ☐ **новое**.

## 14. Аккаунты и прокси
Массовый импорт (sessions/JSON/data + поиск 2FA); прокси из базы/построчно/вручную; один
прокси на несколько аккаунтов; уведомление об успехе; время последней проверки; починить
проверки спамблока/статусов; автообновление — ◐: **импорт Ильи влит**; доработки — ☐ новое.

## 15. Подготовка к запуску
Проверить все модули; тест каждой подписки с нового аккаунта; платёжка; нагрузочный тест;
мощнее сервер; фокус-группа 3–4 чел.; скрыть неготовое — ☐ **фаза запуска**.

## 16. Незавершённые вопросы (блокеры/заметки)
- Автоматизация авторизации сессий парсера — 🔒 отдельно.
- CRM не завершена — 🔒.
- TG-сессии не перенесены в БД (шифрование) — 🔒.
- Задачи воркеров живут в памяти процесса — 🔒 (архитектурное).
- **Вручную внести админ-аккаунты** — совпадает с финалом переезда на profiles (см. ниже).
- **Обновить подписку Supabase на след. неделе** — 🔒 оплата (free-план).

---

## Что уже сделано и совпадает с новым ТЗ (не переделывать)
✅ owner/profiles + вложенность (§4.1) · ✅ логи в БД + «все действия» (§7) · ✅ блокировка
юзера (§4) · ✅ уникальный ID + ручное начисление токенов (§4) · ✅ «Деньги$/Токены/Планы»
и $ в Базе оплат (§3.4, частично) · ✅ мониторинг RPS/CPU + авто (§5.2) · ✅ зум по клику
в витрине (§9) · ✅ годовая скидка из админки (§2.2) · ✅ индексы/оптимизация БД (§5.1).

## Хвост переезда на profiles (в работе, почти финал)
- ⚠️ `drop table users` (Этап 4) — SQL ещё не применён; нужен Auth-пароль для `ya.lonk777`.
- Совпадает с §16 «вручную внести админ-аккаунты».
